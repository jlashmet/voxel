namespace MountingForce.Story
{
    public enum StoryStepType
    {
        Wait,
        MoveActor,
        FaceActor,
        FacePoint,
        Dialogue,
        Camera,
        Sound,
        Parallel
    }
}
