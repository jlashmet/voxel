name: Edits client final inventory
on:
  push:
    branches: [refactor/system-boundaries-foundation-storage]
permissions:
  contents: read
jobs:
  inventory:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          clean: false
          fetch-depth: 1
      - shell: bash
        run: |
          set -euo pipefail
          git reset --hard HEAD
          git clean -ffdx -e Library/
          for p in 'EventPacketReceiver.TryApply' 'AlterationBatchReceiver.TryApply' 'new ClientNetworkRuntime' 'new ClientAuthoritativeEventQueue' 'AuthoritativeAlterationValidator.Validate'; do
            echo ===$p===
            grep -RInF "$p" Assets Packages --include='*.cs' | sort || true
          done
          echo ===CoreEditsImports===
          grep -RIn 'using VoxelEngine.Core.Edits;' Assets Packages --include='*.cs' | sort || true
