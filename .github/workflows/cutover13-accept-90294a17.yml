name: Cutover 13 accept 90294a17

on:
  push:
    branches:
      - refactor/system-boundaries-foundation-storage
    paths:
      - .github/workflows/cutover13-accept-90294a17.yml

jobs:
  static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 90294a1797c4fdc7adf99857f214f8821b8015d1
      - name: Verify renderer collision vegetation boundaries
        shell: bash
        run: |
          ! grep -q '^using VoxelEngine\..*Runtime' Assets/Scenes/Showcase/VoxelShowcase.cs
          ! grep -q 'VoxelRenderBridge\|VoxelWorldView\|ProceduralTreeDamageService' Assets/Scenes/Showcase/VoxelShowcase.cs
          grep -q 'VegetationComposition.TreeDamage' Assets/Scenes/Showcase/VoxelShowcase.cs
          test -f Assets/VoxelEngine/Vegetation/Api/ITreeDamageService.cs
          test -f Assets/VoxelEngine/Vegetation/Runtime/TreeDamageService.cs
          test -f Assets/VoxelEngine/Collision/Api/DdaTraversal.cs
          test ! -e Assets/VoxelEngine/Collision/Runtime/DdaTraversal.cs
          grep -q '^using VoxelEngine.Collision.Api;' Assets/Tests/PlayMode/VisualCollisionParityTests.cs
          grep -q '^using VoxelEngine.Collision.Runtime;' Assets/Tests/PlayMode/VisualCollisionParityTests.cs
          python3 - <<'PY'
          from pathlib import Path
          import json,re
          root=Path('Assets/Scenes/Showcase')
          rows=[]
          for p in sorted(root.glob('*.cs')):
              refs=sorted(set(re.findall(r'^using\s+(VoxelEngine\.[\w.]*Runtime(?:\.[\w.]+)?)\s*;',p.read_text(errors='ignore'),re.M)))
              if refs: rows.append((p.name,refs))
          print(f'RUNTIME_COUPLED_FILES={len(rows)}')
          for n,r in rows: print(n+': '+', '.join(r))
          if len(rows)!=4: raise SystemExit('expected 4 Runtime-coupled Showcase files')
          if any(n=='VoxelShowcase.cs' for n,_ in rows): raise SystemExit('VoxelShowcase still Runtime-coupled')
          data=json.loads((root/'VoxelEngine.Showcase.asmdef').read_text())
          runtime=sorted(r for r in data['references'] if r.startswith('VoxelEngine.') and r.endswith('.Runtime'))
          print(f'RUNTIME_ASMDEF_REFS={len(runtime)}')
          for r in runtime: print('ASMDEF '+r)
          if len(runtime)!=7: raise SystemExit('expected 7 Runtime asmdef refs')
          if 'VoxelEngine.Collision.Runtime' in runtime or 'VoxelEngine.Vegetation.Runtime' in runtime:
              raise SystemExit('Collision/Vegetation Runtime ref remains')
          PY

  editmode:
    runs-on: [self-hosted, macOS]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 90294a1797c4fdc7adf99857f214f8821b8015d1
      - name: Run Unity EditMode suite
        shell: bash
        run: |
          mkdir -p Artifacts/Cutover13-90294a17
          tools/unity-run.sh -batchmode -nographics -job-worker-count 1 \
            -projectPath "$GITHUB_WORKSPACE" -runTests -testPlatform EditMode \
            -testResults "$GITHUB_WORKSPACE/Artifacts/Cutover13-90294a17/results.xml" \
            -logFile "$GITHUB_WORKSPACE/Artifacts/Cutover13-90294a17/unity.log" || true
          test -s Artifacts/Cutover13-90294a17/results.xml
      - name: Verify parity
        shell: bash
        run: |
          python3 - <<'PY'
          import xml.etree.ElementTree as ET
          root=ET.parse('Artifacts/Cutover13-90294a17/results.xml').getroot()
          cases=root.findall('.//test-case')
          failed=sorted(c.get('fullname') or c.get('name') for c in cases if c.get('result')=='Failed')
          expected=sorted([
          'VoxelEngine.Tests.PlayMode.ChainCombatEnemyAIV8Tests.CurrentCascadeLabBootsWithEnemyAIStack',
          'VoxelEngine.Tests.PlayMode.ChainCombatExecutionPlanV9Tests.BattleResetClearsSharedPlanHistoryAndGhostBackToStart',
          'VoxelEngine.Tests.PlayMode.ChainCombatExecutionPlanV9Tests.PlannerBootsWithGhostBoardAndVisualLayer',
          'VoxelEngine.Tests.PlayMode.ChainCombatExecutionPlanV9Tests.SharedPlanExecutionRequiresEveryLivingPlayerReady',
          'VoxelEngine.Tests.PlayMode.ChainCombatMechanicsV2Tests.CascadeLabControllerBootsFourPlayerBattle',
          'VoxelEngine.Tests.PlayMode.ChainCombatPlanApprovalV9Tests.CommittingApprovedPlanPreservesReadyForEnemyHandoff',
          'VoxelEngine.Tests.PlayMode.ChainCombatPlanApprovalV9Tests.EditingAnApprovedPlanRevokesEveryReadyApproval',
          'VoxelEngine.Tests.PlayMode.ChainCombatPrototypeTests.ControllerBootsPlayableBattlePresentation',
          'VoxelEngine.Tests.EditMode.KentridgeAnchorUndercroftTests.AnchorUndercroftsOccupyPubAndWarehouseSupportFacesWithoutAddingGameplayStructures',
          'VoxelEngine.Tests.EditMode.KentridgeGenerationTests.DistrictTerracesCreateNeighbourhoodScaleShelves',
          'VoxelEngine.Tests.EditMode.KentridgeInfrastructureTests.UrbanInfrastructureStaysSeparateFromStableGameplayStructures',
          'VoxelEngine.Tests.EditMode.KentridgeUrbanFabricTests.UrbanFabricCatalogueRealizesAllEightBlocksAsIndividualBuildings',
          'VoxelEngine.Tests.EditMode.SurfaceRingBandTests.RingsDoNotReachBeyondTheStreamingRadius'])
          passed=sum(c.get('result')=='Passed' for c in cases)
          print(f'TOTAL={len(cases)} PASSED={passed} FAILED={len(failed)}')
          for n in failed: print('FAILED '+n)
          if (len(cases),passed,len(failed))!=(387,374,13): raise SystemExit('unexpected counts')
          if failed!=expected: raise SystemExit('failure set changed')
          PY
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cutover13-90294a17-editmode
          path: Artifacts/Cutover13-90294a17/
