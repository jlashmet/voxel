name: Apply renderer T2 lazy invalidation patch

on:
  push:
    branches:
      - feature/renderer-lod-hole-regression
    paths:
      - tools/apply-renderer-t2-lazy-invalidation.py
      - .github/workflows/apply-renderer-t2-lazy-invalidation.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply assertion-based source patch
        run: python3 tools/apply-renderer-t2-lazy-invalidation.py
      - name: Validate patch
        run: git diff --check
      - name: Commit lazy invalidation
        shell: bash
        run: |
          if git diff --quiet; then
            echo "Patch already applied"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            Assets/VoxelEngine/Rendering/Runtime/SurfaceExtraction/CpuTransvoxelChunkCache.cs \
            Assets/Tests/EditMode/SurfaceDemandSchedulingArchitectureTests.cs
          git commit -m "Keep cold renderer invalidations out of build queue"
          git push origin HEAD:feature/renderer-lod-hole-regression
