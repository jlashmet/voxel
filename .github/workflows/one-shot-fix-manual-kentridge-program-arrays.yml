name: One-shot Fix Manual Kentridge Program Arrays

on:
  push:
    branches:
      - refactor/system-boundaries-foundation-storage
    paths:
      - .github/workflows/one-shot-fix-manual-kentridge-program-arrays.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: refactor/system-boundaries-foundation-storage
          fetch-depth: 0
      - name: Canonicalize manual program arrays
        run: python .github/scripts/one_shot_fix_manual_kentridge_program_arrays.py
      - name: Verify exact source scope
        run: |
          git diff --stat
          test "$(git diff --name-only | sort | tr '\n' ' ')" = "Packages/com.mountingforce.worldgen/Runtime/Voxel/KentridgeFrontagePathCatalogue.cs Packages/com.mountingforce.worldgen/Runtime/Voxel/KentridgeTerraceSupportCatalogue.cs Packages/com.mountingforce.worldgen/Runtime/Voxel/KentridgeUrbanSidewalkCatalogue.cs "
      - name: Commit and self-clean
        run: |
          rm .github/scripts/one_shot_fix_manual_kentridge_program_arrays.py
          rm .github/workflows/one-shot-fix-manual-kentridge-program-arrays.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: canonicalize manual kentridge program arrays"
          git push origin HEAD:refactor/system-boundaries-foundation-storage
