# Experiment 004 — Dedicated scene production quality

## Hypothesis

The first feature-specific environmental clue scene is sufficient production-quality standalone-player proof once debug marker colors are removed.

## Action / source SHA

Inspected the full-resolution `worldbuilder` module-player capture from exact feature SHA `dc1bab0cad0170b448fef055e53842e30e6149a3`, targeted request run `33405791094`.

## Result

The feature is semantically readable, but the capture is still `prototype/blockout quality`: a large flat slab, box-like facade, isolated flattened spheres for displaced stones, minimal construction detail, and almost no environmental integration. The clue chain exists but reads as a validation tableau rather than believable generated-world evidence. This fails the `AGENTS.md` production-quality visual bar even though the CI run is green.

## Verdict

Hypothesis falsified. Planner/discovery behavior is not implicated; the demonstrated defect is presentation/composition only.

## Next step

Keep reusable semantics unchanged and enrich only the module-local validation composition with a worn approach, layered masonry construction, rubble/moss/ivy, and forest-edge framing while preserving the same non-glowing environmental clue language. If that materially different presentation fix still fails the same visual-quality symptom, isolate a minimum presentation repro/root cause before another visual fix.
