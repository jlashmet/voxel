# Plan

## Scope and current reconciliation
Reconcile only the prior Mountain Dragon assignment. The last compatibility-relevant master sync was `b56436f198702fa91bfaccec69a30df25a52a2cd`, merged into `fixes/agent-4` through synchronization PR #334 and producing feature source `450f275d3966fb69834ac41c7177aece3fea3355`. Current master is now `5c9a715a42735a0b573f436af1e3700dff2f591c`; its only advance from `b56436f...` is the Mounting Force town-art-direction closure/art material, with no `.github` workflow, validation-planner/runner, `AGENTS.md`, `SceneIssues/README.md`, renderer, or Mountain Dragon compatibility change. The live comparison is 13 commits ahead, 1 behind, with merge base `b56436f...`. Per `SceneIssues/README.md`, merge current master when compatibility requires it and always before final promotion; do not churn the feature SHA for this unrelated master-only documentation/art advance while exact-SHA validation is blocked. The exclusive agent-4 diff remains limited to Mountain Dragon Showcase composition/traversal, reusable Cutscenes and WorldBuilder mountain/road capabilities, focused/module-local validation, startup-bake provenance, the accepted bake, and this SceneIssue evidence. Do not import renderer, Kentridge/GameSystem, CI-planner, or other SceneIssue work.

## Accepted behavior and current-source evidence
The repository-owner waiver remains authoritative for the known shared near-surface renderer corruption. Mountain Dragon composition/gameplay remains accepted: substantial mountain, shared-road ascent, supported summit dragon placeholder, proximity cutscene, and exact dialogue `Hello, I'm Mr. Dragon.`. On exact current production source `450f275d...`, run `34200864820` attempts 1 and 2 both passed every persistent/EditMode phase, including `VoxelEngine.Rendering.Tests.EditMode`, and passed the requested `ShowcaseStartupBakeArtifactTests.CurrentSourceBakeExportsPayloadAndMatchingManifest` regression. Before each job timeout, module-local built-player validation completed successfully for Mountain Dragon and CharacterMotor. The generated accepted bake remains byte-identical to the tracked payload: 13,310,800 bytes, SHA-256 `c0f6f5bb0651bac0088b1e12d3a16956816fc62f5fe2dcb83461f9ba7c60cf3b`, Git blob `0d99d521613eff4ef5f1349695624d790aa81442`, manifest signature `BE8FDFF3`.

## Hypotheses and result
1. **The prior renderer step-four failure still blocks agent-4.** Falsified: current-source persistent validation includes Rendering EditMode with zero failures after the master repair.
2. **Repository-derived validation fan-out cannot complete inside the single-test workflow budget.** Confirmed: the current-source plan expands to 52 modules, 55 tests, and 35 player validations. Both attempts of run `34200864820` were cancelled in the automatic module-validation step at the workflow's ~20-minute job ceiling after 11/35 player validations, with no product assertion failure and no complete module-validation summary.

## Remaining gate
Required assignment-specific exact-SHA validation is therefore not green. Do not issue a third retry, weaken coverage, or edit CI/planner infrastructure from agent-4. Promotion remains blocked until repository validation infrastructure on master can complete the required derived plan. When such a relevant master change lands, merge current master into `fixes/agent-4`, rerun exact-SHA validation, and only after green evidence refresh closure metadata and use the normal final PR + immediate auto-merge path.
