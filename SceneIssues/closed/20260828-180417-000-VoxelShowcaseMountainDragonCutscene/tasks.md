# Tasks

## Accepted feature work
- [x] Implement a substantial authored mountain landmark through reusable WorldBuilder landform concepts.
- [x] Compose a continuous winding ascent through the shared road resolver / terrain-corridor path rather than scene-local voxel writing.
- [x] Place the large placeholder dragon at the supported summit through reusable WorldBuilder composition.
- [x] Trigger the reusable cutscene/dialogue flow from normal proximity and present the exact dialogue `Hello, I'm Mr. Dragon.`.
- [x] Exercise mountain, path, dragon placement, proximity, dialogue, and traversal with focused regressions and module-local validation scenes.
- [x] Validate the accepted implementation in a standalone built player; exact run `34024289067` completed 92/92 grounded route waypoints, summit proximity, exact dialogue, and startup payload/manifest export.
- [x] Record the repository-owner closure override: remaining torn/floating near-surface presentation corruption belongs to shared voxel rendering and is outside agent-4 ownership.

## Master reconciliation
- [x] Rebuild `fixes/agent-4` directly on current master instead of merging the polluted 357-commit branch history.
- [x] Carry only still-missing Mountain Dragon Showcase, reusable Cutscenes, WorldBuilder mountain/road, traversal, regression, module-local validation, startup-bake provenance, and evidence-route work.
- [x] Preserve current-master overlap rather than replacing it with stale branch copies.
- [x] Exclude unrelated Kentridge/GameSystem work, renderer experiments, CI-planner changes, and other SceneIssues.
- [x] Reject branch CI classifier `a67758fa`; private implementation changes in an API assembly can affect unchanged dependents.
- [x] Repair reused/missing Unity `.meta` identities and point evidence-route tests at the authoritative closed SceneIssue path.
- [x] Review the master-to-feature diff and keep it assignment-scoped.
- [x] Let old request `8c4cee6d...` / run `34175607847` reach terminal without replacement and classify its product compile failure on obsolete polluted source.
- [x] Remove superseded `WorldBuilderMountainLandmarkMaterialCatalogue` and `.meta`; its removed `MountainLandmarkSpec` contracts are obsolete and no accepted Mountain Dragon path consumes it.

## Revalidation and promotion
- [ ] Run exact-SHA targeted CI for the corrected reconciled feature source using `ci-test/fixes/agent-4`.
- [ ] Confirm the generated startup payload/manifest matches the corrected source; update tracked payload only through the repository-sanctioned binary-transport path.
- [ ] Revalidate the final payload-bearing exact feature SHA.
- [ ] Record final exact-SHA evidence and refresh closure metadata truthfully.
- [ ] Open a new normal `fixes/agent-4` -> `master` PR, enable auto-merge immediately, and require the PR `affected` gate.
- [ ] Confirm the PR merged, the closed SceneIssue is visible on `origin/master`, and no legacy open path remains.
