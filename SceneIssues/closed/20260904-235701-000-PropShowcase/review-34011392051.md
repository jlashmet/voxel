# Exact presentation-quality run 34011392051: targeted-CI planning timeout

Request `9d488a58cedc6d5e7b70dda2112f2027840c1dca`, source `6b036f2bdc93fc1bfcf610ce856038c7e87df4aa`, run `34011392051`, job `101427741462`, completed FAILURE at the workflow's 20-minute limit. The request was not replaced while queued/running.

Artifact `9982838761` (`single-test-34011392051`, SHA256 `14156543032f99baf0cd10187afe08d51fa76b409711fcd3f11b121d0d28b57c`) was downloaded and inspected.

## Proven cause
`ModuleValidation/plan.json` selected 48 modules, 52 test entries and 23 player validations because `Assets/Scenes/PropShowcase.unity` was classified as an unknown production path and placed in `fallbackPaths`. The safe fallback therefore selected every discovered module. This is incorrect ownership: repository guidance explicitly treats top-level showcase scenes as integration consumers, not module-local owners.

The persistent test summary completed successfully before the timeout (`exit_code=0`, `status=passed`), including the requested `Game.Structures.Tests.PropShowcasePresentationQualityTests`. The run then spent the remainder of the job serially executing unrelated module players. It reached the canonical Kentridge player near the timeout and was cancelled; the subsequent SceneIssue replay ran while the job was already cancelling and produced no durable SceneIssue capture directory. This is not a green exact-SHA run and supplies no new visual acceptance.

## Repair
`tools/module-validation-plan.py` now treats top-level `Assets/Scenes/*.unity` as integration-only while still attaching Kentridge once for any production diff. Unknown production paths retain broad safe fallback behavior. Focused planner regressions cover scene-only, scene-plus-real-module, and unknown-path cases.

The same artifact also demonstrated that two validation scenes owned by one module wrote to the same `Players/<module>` output directory, so later scenes overwrote earlier logs/screenshots. `tools/run-module-validation.py` now keys each player artifact directory by module plus scene; focused tests prove same-module targets remain distinct and deterministic.

Fresh exact-SHA CI is required. No test, player, visual, resource, closure or merge criterion is waived by this infrastructure repair.
