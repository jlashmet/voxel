# Exact repaired-source run 34007356710: compile rejection

Request `57ab96ca508e70a4d768aa5ddefc6b7343bb531c`, source `9697d365c986a070f6a78db2af99e8c0f449df15`, run `34007356710`, job `101416917259`, completed FAILURE. This request was not replaced while queued/running.

The run reached repository planning and Python orchestration tests, then both required module validation and standalone SceneIssue replay failed during Unity script compilation. No Unity test result or screenshot from this run is acceptance evidence.

Exact compiler failures:
- `PropMaterialCompositionTests.cs`: `NonParallelizableAttribute` / `NonParallelizable` was unavailable to the module-owned test assembly.
- `PropShowcase.cs`: production assembly `VoxelEngine.Showcase` referenced `PropShowcaseCaptureStress`, but that helper lived under `SceneRuntime/Validation` behind the separate `VoxelEngine.Showcase.Validation` asmdef. Production cannot reference its validation assembly.

Repair `2154840bf2ccae7db9b5a03b3912d333d019e29e` removes the unsupported test attribute and moves the capture-only helper into the production SceneRuntime assembly while leaving its use limited to capture-harness execution. The validation assembly still consumes production; the dependency direction is no longer reversed.

Subsequent production presentation work at `2b7e30e1efb3dc8d63a82923ca42101f7ab36a9f` adds shared framed thin-surface detail, detailed Door/SecretDoor/Trapdoor proxy meshes, semantic decoration light/particle presentation, and truthful voxel publication readiness. These changes have not yet passed Unity or built-player validation.
