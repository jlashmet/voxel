# Exact material-fix run: rejected acceptance

Source `de0aa1fb4221b06f8f63e6f22fc26ffba77defc8`; unchanged request `e83a7fd822dab1c40d59f0f84ccd65937071fd28`; workflow run `34003328146`; job `101405981993`. The job started 2026-09-06T02:32:34Z and the workflow completed FAILURE at 02:37:23Z. It was not replaced while queued/running.

Artifact `9981244713` (`single-test-34003328146`) was downloaded and inspected. Archive SHA256: `5db168672dc4f65f49a57c887752b12b1b1d09316be183a4d4e9c5970785550c`.

## Mechanical result
`ModuleValidation/Results/Tests/Persistent/persistent-playmode-0.txt` reports 4 passed, 0 failed/skipped/inconclusive for `VoxelEngine.Showcase.Tests.PlayMode`, including the material-mode lifecycle regression. This is an individual-phase result, not a successful required run.

`persistent.log:143759` starts the second PlayMode phase before the editor has left play mode. Lines 143771-143775 throw `InvalidOperationException: This cannot be used during play mode` from `EditorSceneManager.SaveCurrentModifiedScenesIfUserWantsTo` / `SaveModifiedSceneTask`. The final persistent summary is exit 2 / failed. This independently supports the existing process-isolation repair at `79b6a2f4`; do not retry the unrepaired source.

Standalone replay produced 7 timed captures plus verification-final.png. Its log reaches completion with 44 switches and peakOwned=1, but this older source does not contain the frame-separated resource probe. Those counts do not prove memory flatness.

## Direct visual review
Reviewed the original PNGs (and a contact sheet only for navigation):
- `frame_000_t000.0.png`: the initial anvil is not visible yet, although the browser reports READY. Treat initial publication/loading as unproven.
- `frame_001_t010.0.png`: Game Table has an elevated three-quarter silhouette and material shading rather than normal-coverage colours. This confirms the material-mode correction; it does not establish complete visual acceptance.
- `frame_002_t020.0.png`: Merchant Sign is visible on the semantic front, but is a featureless mustard rectangle against a plain support surface: prototype/blockout quality.
- `frame_003_t030.0.png` / `frame_004_t040.0.png`: Forge Hearth reads as disconnected box-like bars; no finished emissive/fire presentation is visible. Prototype/blockout quality.
- `frame_005_t050.0.png`: Door is a plain blue box-like slab. `frame_006_t060.0.png` / `verification-final.png` show Trapdoor with the same upright presentation. Prototype/blockout quality; inspect production kind/geometry/mount routing rather than decorating a fake preview.

Overall classification: **prototype/blockout quality**, rejected. Tiny, large, ceiling and procedural selections are logged but not adequately captured by this replay's ten-second interval; owned scenarios' three-second capture cadence must supply that evidence. No acceptance criterion is waived and no closure is authorized by this result.

## Materials ownership repair
`Assets/Game/Composition/Materials` previously had neither its own test assembly nor a local validation scene. Add a local scene that invokes the real production PropShowcase component (no reconstructed prop geometry) and a 43-second material-focused scenario through voxel, thin and procedural representatives. Its scene setup is intentionally the same production consumer as the browser, not a parallel material/rendering implementation. New module-owned EditMode tests verify actual canonical material response, cached instance reuse, and rejection of unknown IDs through `GameMaterialComposition.TryGetProceduralMaterial`.

These new tests/scenes require fresh exact-source CI. They do not fix the recorded art/presentation defects. Final master integration and PR + auto-merge remain gated on every required acceptance item.
