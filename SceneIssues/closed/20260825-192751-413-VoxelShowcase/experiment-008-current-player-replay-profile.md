# Experiment 008 — current player replay and residual performance profile

**Hypothesis** — Restoring the validated exact-ring GPU cutover should remove the saved-view coverage failure and lift the original sub-100-FPS state, but the current player may still spend substantial time converging non-visible geometry after the marked view itself is complete.

**What was performed** — Revalidated the production cutover on the current feature lineage with request `8bb029535005fbb9fde1365e39c7b41461ecc407`; `ci/single-test` completed successfully in Actions run `32991459621` for `VoxelEngine.Tests.EditMode.GpuLod2CutoverPolicyTests.SceneIssue20260825192751413ProductionGpuCutoverIsEnabledForExactRings`. Then ran the exact saved-camera replay from CI commit `c7bc806567c007f3cbc0310942a8a799ad88627a` in Actions run `32991641843`. The workflow freshly baked `VoxelShowcase` in 203 seconds, built the standalone player, verified the frozen saved pose, ran uncapped for 90 seconds, captured eight screenshots, and uploaded the full player/bake/FPS/surface evidence. The original marked screenshot was compared with the replay frames. The original overlay was `FPS 105 / MIN 105 / MAX 166`; the converged replay frame showed approximately `FPS 163 / MIN 131 / MAX 187`. From 60–89 seconds the surface log stayed at `visible=709`, `missingMax=0`, `drops=0`, and `reappeared=0`. The final 20 one-second FPS samples ranged about 154–184 FPS and averaged about 168 FPS. The scheduler telemetry remained modest (typical per-frame scheduler/admission maxima around 0.6–1.1 ms), but ring residency and arena population continued increasing through the end of the 90-second run, showing ongoing background convergence after the visible view was complete.

A separate production moving regression request was then issued from feature head `b53dc0255767d15a78f24efdceb1b2a339c60410`: CI commit `c59ca85fc9e81b09577b5a6f6c3d143d42438446` requests `VoxelEngine.Tests.PlayMode.ShowcaseTraversalPerformanceTests.ContinuousPlayerTraversalNeverStuttersOrOpensNearFarGap`. GitHub admitted it as Actions run `32993732467`. It remains queued. After the previous self-hosted job finished, repeated repository-wide checks showed six queued self-hosted workflows and zero active workflows, so the current delay is runner availability, not a traversal failure or a rejected request.

**Result** — The exact saved pose no longer reproduces the reported missing geometry or sub-100-FPS state once the view converges. The current fix therefore materially improves the captured failure. However, the replay is still only roughly 160–170 FPS in the settled marked view, far below the capture's requested ~1000-FPS headroom, and it continues background residency/build publication after visible coverage has stabilized. This is not sufficient evidence to close the issue.

**What was learned** — The remaining question is now narrower than the original report. Visible coverage is stable, and the restored GPU exact-ring path is green. The next measurements must separate moving-streaming cost from converged draw cost before another production optimization is selected. `ShowcaseTraversalPerformanceTests.ContinuousPlayerTraversalNeverStuttersOrOpensNearFarGap` exercises 420 moving frames across multiple region boundaries; `ShowcaseTraversalPerformanceTests.ConvergedTerrainAndCastleRetainHighSteadyStateRenderThroughput` isolates `camera.Render()` after idle convergence and logs the steady median/p95/max. If steady rendering is already near its historical target while whole-player FPS stays near ~167, the next optimization belongs in background convergence/scheduling rather than the raster submission path. If steady render time itself is high, optimize the render path instead.

**Next** — Keep `c59ca85fc9e81b09577b5a6f6c3d143d42438446` unchanged until the self-hosted runner accepts and completes it. Then run the converged steady-throughput regression on the same feature lineage. Do not close or terminalize the SceneIssue until both measurements are current and the remaining gap to the requested performance headroom is either fixed or supported by repository acceptance criteria and measured evidence.
