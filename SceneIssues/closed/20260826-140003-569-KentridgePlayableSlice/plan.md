# Plan

- Evidence: one saved camera, no marked circles, so the whole captured pub interior is the acceptance region. The capture requires a female bartender, several tables/chairs, shelves behind the bar, windows, and a somewhat larger room.
- Hypotheses: (1) Pub-specific furnishing is missing; confirmed because shared hospitality code said Pub kept a pre-existing bar while the Pub role actually emitted only an exterior sign. (2) Wrong role selection; rejected because the settlement resolves stable `KentridgeRole.Pub`. (3) Windowless/undersized shell; windows rejected as root cause because Pub already uses `WindowTreatment.Warm` and the generated shell carves windows; size was a secondary gap.
- Fix: enlarge only Pub from 112x92 to 118x96 dm; add a production-program bar, two shelves, three total tables, six chairs, and a female bartender prop behind the bar while preserving the front/central aisle.
- Regression: `ProductionPubCarriesCapturedBarInteriorRequirementsWithoutLeakingToInn` asserts Pub size/window treatment and bar/table/chair/shelf/bartender signatures plus no Pub-only geometry in Inn. Existing all-role production coverage evaluates every generated program and enforces primitive budgets.
- Verification: exact source `dcd0bccbef6dc8076a25650acbc3c45df16b42a6`; final request `03f83f820a23e4c1ab8fd384461758a8148278bc`; PlayMode regression, saved-pose player replay, and required artifact upload all green. Direct replay review shows the requested furnishings and window/opening treatment.
- Blast radius/cost: Kentridge Pub only; generic shell/window and other roles are unchanged. Cost is a bounded set of additional `EmitBox` primitives for one role. Verification and closure bookkeeping are complete; remaining integration is current-master merge and non-force master advance.
