# Plan

- **Captured defect / acceptance:** one VoxelShowcase capture, no marked circles. Nearby E must visibly open the castle gate over time without deleting it; two physical leaves remain. Centre clearance is a behavioral safety condition.
- **Hypotheses:** (1) E/proximity rejects interaction — falsified by production `TryInteract`. (2) renderer invalidation loses correct geometry — falsified because the old operation explicitly cleared the authored gate and the legacy test required emptiness. (3) opening is deletion — confirmed.
- **Runtime evidence / discriminator:** the static retained-leaf prototype was rejected when its expected leaf read `Empty`. The animated regression proves substantial closed/mid/final poses, timber+iron leaves on both sides, a clear lane, and one-shot interaction. Earlier green CI isolated noncanonical editor/offscreen evidence; action-aware standalone replay on final run `33133498765` now shows both leaves swung inward at the exact saved pose.
- **Selected fix:** snapshot the authored gate; restore pose zero on the interaction frame; rotate both material-preserving halves inward over 0.9 s / 9 bounded poses. Evidence-only `replayAction: "interact"` synchronizes the showcase motor via public `TeleportTo`, invokes the same public `TryInteract`, and holds the recorded camera.
- **Regression:** `NearbyInteractionAnimatesBothGateLeavesAndKeepsCentrePassageClear` exercises production `TryInteract`; closed/mid/final states differ, both leaves retain wood+iron, the lane is empty, and interaction remains one-shot. Exact request `b531a609…` is `ci/single-test=success` after its one infrastructure rerun.
- **Blast radius / cost:** gate-local ShowcaseWorld state plus one showcase driver; replay action is opt-in and absent for other captures. Nine bounded gate poses; no ongoing work after completion; no renderer/storage ownership changes.
- **Remaining gates:** move open→pending→closed, merge latest master, verify feature-only diff, then non-force push exact feature head to master.
