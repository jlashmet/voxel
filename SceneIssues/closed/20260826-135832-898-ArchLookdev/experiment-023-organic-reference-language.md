# Experiment 023 — organic reference language

**Observed falsifier.** Exact request `d4d8b3a56962a89bc417c5d896aefdebccc0dc07`, run `33152121570`, passed the final-presentation regression and 45-second player replay. Direct inspection of `RealPlayer/verification-final.png` still rejects the product: the denser ivy remains a repeated angular five-lobed card pattern, while the enlarged lavender radial rosettes read as icons rather than the reference's delicate integrated blossoms.

**Competing hypotheses.** Placement/topology/count are rejected by the green exact run and clean sliver-free frame. More scale/density alone is also rejected by this run. The remaining defect is silhouette/depth/color language.

**Action.** Add one bounded post-topology finish that keeps all exact indices/counts but repacks each support into a compact overlapping core, reshapes the same 17-vertex leaves into softer seed-varied asymmetric English ivy with stronger depth tilt, and reconstructs the same five seven-vertex petals per head as irregular overlapping warm-white/blush blossoms. Reduce each head from oversized rosette scale and keep five heads tightly anchored per bouquet.

**Regression / falsifier.** `ArchReferenceGrowthOrganicFinishPassTests.OrganicFinishBuildsOverlappingIvyAndDelicateBouquetsAcrossRebuild` requires exact 2,484/77 topology, no long triangles/stems, support fidelity, bounded leaf radius/spread with overlap ratio >0.80, delicate flower radius/anchor bounds, warm/blush petal bias, unchanged 128 leaves / 30 heads / 3 draws / <=4,096 vertices, and rebuild stability. Reject even if green if the saved pose still reads as repeated cards/rosettes or remains visibly below the reference.

**Blast radius / cost.** ArchLookdev-only one-shot mutation after topology cleanup; one component/coroutine during construction only, no new mesh vertices/renderers/draws/per-leaf objects and no steady-state work.
