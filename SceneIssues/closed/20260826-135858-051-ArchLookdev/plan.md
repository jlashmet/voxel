# Plan

## Reopen / acceptance
- Original ask: carry the tuned ArchLookdev hero arch into production Kentridge.
- Prior closure was rejected because proof came from `ArchLookdev`, not `KentridgePlayableSlice`. Final proof therefore had to be the exact built Kentridge player at normal player height, with recognizable building context and readable segmented projecting voussoirs.
- Reopened metadata has no formal captures/marked circles; this is a whole-frame integration/readability defect using semantic Warehouse role 14.

## Hypotheses / discriminators
1. **Presentation failure — supported.** Production landmarks emitted twelve zero-radius `MasonryJoint` capsules, so count coverage could pass while segmentation disappeared visually.
2. **Composition/access failure — falsified.** The exact-scene production-host traversal resolves the Warehouse public approach and walks through the generated entrance successfully.
3. **Evidence staging failure — supported and fixed.** The harness first ignored the canonical scene-issue switch, then waited forever for gameplay control during unattended opening dialogue. Runtime logs discriminated both failures.

## Selected fix / regressions
- Reusable `FramedArchedOpening` keeps both clearance carves and now gives its twelve hero joints a minimum 1 dm radius; glazed/window arches remain continuous.
- `KentridgeInteriorScaleTests.ProductionCatalogue_LandmarkEntrancesCarryReadableHeroVoussoirJoints` rejects zero-width joints and treatment spillover.
- `KentridgeHeroArchPlayableSceneTests.GeneratedWarehouseHeroArch_IsReachableThroughProductionPlayerHost` loads the exact scene and drives the real production player through the entrance.
- Metadata-gated evidence harness uses the canonical switch, releases only the authored opening during unattended capture, then stages/approaches Warehouse role 14 with the real motor and normal eye-height camera.

## Blast radius / cost
- Only landmarks already opting into `FramedArchedOpening` change; glazed/window arches and unrelated programs do not.
- Primitive count remains twelve surface-detail capsules per hero entrance; only their narrow radius-1-dm footprint grows.
- Evidence logic is SceneIssue-metadata gated, so ordinary gameplay has no new per-frame work.

## Validation result
- Exact tested source: `8fbb5510a962296c1de5cfcc01bed6d319c7d593`; transport: `6902b44ca8f2fe382432df6d2d7448b48b18cc0c`; run `33274034740` green.
- Focused traversal passed in 13.44s. Built player logged `ARCH_EVIDENCE armed/approach/arrived`, completed 60s with zero harness assertions, and final normal-height frames visibly show the segmented Warehouse arch in Kentridge context.
- Acceptance gates complete; proceed only with pending/closed bookkeeping and current-master merge.
