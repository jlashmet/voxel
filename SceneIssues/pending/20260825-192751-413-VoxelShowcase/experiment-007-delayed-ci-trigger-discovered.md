# Experiment 007 — connector CI requests are delayed, not suppressed

**Hypothesis** — The earlier connector-authored CI requests may have been admitted to GitHub Actions after a long queue/event delay rather than being permanently suppressed.

**What was performed** — Re-queried the Actions run history for `ci-test/fixes/agent-2` while resuming feature head `51c11767ef88a094ccf9d10a9c0976c3f55c8577`. Request commit `6c0317b29b13f165235c936f31565e113f0eab25`, created at 16:11 UTC, had subsequently produced `Tests (single)` run `32989108800` at 16:34 UTC. Its job log shows the exact filter `VoxelEngine.Tests.EditMode.GpuLod2CutoverPolicyTests.SceneIssue20260825192751413ProductionGpuCutoverIsEnabledForExactRings`, Unity 6000.5.6f1, status 0, and `Executed 1 test case(s).` The workflow conclusion was success. The still-newer low-level request `9ce7b8049f65108bf2134679f35f26a98f1cc161` never produced a run, so it cannot be final evidence. The assigned CI branch was reset to current feature head and a fresh authoritative request `8bb029535005fbb9fde1365e39c7b41461ecc407` was issued. A CI-branch-only exact-camera replay request was also added at `c7bc806567c007f3cbc0310942a8a799ad88627a` using the existing guarded player-capture/`--scene-issue` path.

**Result** — The production cutover regression has one real green Unity execution on the production-fix lineage, disproving the prior claim that connector-authored request commits never create Actions events. The repository currently shows delayed admission for contents-API pushes; the newest authoritative request has not yet reached a terminal state and therefore supersedes the older green run for completion purposes.

**What was learned** — The CI mechanism is usable from this session but can have substantial event latency. Do not create retry branches or substitute historical runs; keep polling only the newest request on `ci-test/fixes/agent-2`. The exact replay can run independently on that same CI branch without modifying the feature branch or creating another SceneIssue capture.

**Next** — Require `8bb029535005fbb9fde1365e39c7b41461ecc407` to complete with `ci/single-test=success`. Then use the existing VoxelShowcase traversal regression for moving real-player coverage/performance, inspect the exact saved-camera replay artifacts from the `192751` one-shot, and close only if those current-head results are clean.
