# Far-World Visibility Implementation Plan

## Acceptance and ownership

Keep terrain, semantic structures, settlements, forests, landmark natural features, and ordinary scatter visually coherent from the resident voxel boundary through the configured horizon without making distant voxel regions resident. World truth remains deterministic CPU data; WorldBuilder stays renderer-neutral; VoxelEngine Rendering consumes render-ready contracts; scene thresholds/named-content policy remain in composition. Built-player evidence, not editor screenshots, is required for visible acceptance.

Required final proof includes: 12 km guaranteed terrain coverage through camera snap phases; never-resident landmark visibility; stable semantic/proxy/cluster handoffs; deterministic settlement/vegetation clustering and persistent state propagation; whole-range terrain material/lighting continuity; and measured CPU/GPU/memory/draw cost against the device matrix.

## Current hypotheses / discriminators

- **Coverage:** the old `innerRadius * 2` heuristic under-covered because it ignored actual spacing and camera snap loss. T001 now selects the minimum ring count from worst-case snapped extent.
- **Fallback retirement:** sampled heights are not equivalent to published authoritative coverage because the startup fallback occupies the final ring mesh. T002 keeps that mesh fallback-owned until every lower required ring is current/published and the final ring has current samples ready for atomic publication.
- **Diagnostics:** validation needs publication truth, not configured potential coverage. T003 reads current contiguous authoritative publication and fallback state without feeding those diagnostics back into world/render authority.
- **Far terrain fidelity:** the near renderer already uses application-semantic-free material presentation rows plus deterministic world/voxel position and surface normal. T003A restores those broad presentation inputs to the far path before any geometry-density change; T003B adds filtered world-space/triplanar detail without changing geometry/residency; T003C remains conditional on measured silhouette loss.
- **Visual validation blocker:** T003A automation is green, but its standalone-player captures never leave the `Loading Kentridge...` screen. A current-master exact-SHA replay is queued as run `33822302367`; until that run produces actual terrain frames, prior green automation is compile/runtime evidence only and cannot satisfy visual acceptance.
- **Semantic structure path:** existing generic far rendering/selection/cache infrastructure is substantially present, but production Showcase/Kentridge runtimes still need planning-time `StructureFarPresentation` records, aggregate HLOD integration, readiness-aware handoff, and legacy semantic-fallback separation. T004 establishes the renderer-neutral WorldBuilder record first; later phases must consume it rather than discover structures from voxels.
- **Vegetation path:** deterministic sector queries, stable instance IDs, `TreeWorldState` revisions, and deterministic canopy-cluster construction already exist. Remaining work is presentation-tier integration and persistence invalidation, not a second vegetation truth source.

## Material results

- Exact-SHA run `33801450701` passed module validation and standalone-player replay for source `4b9622ace9e4ca27ecf5da25900524341cffd8fe`, clearing the previous shared Rendering blocker.
- T001 now derives spacing, half-extent, snap loss, per-phase/guaranteed coverage, and minimum ring count. Shipped ~409.6 m / 96-cell / 12 km requires six rings; five guarantee only ~9.63 km. `VoxelFarTerrainCoverageTests` covers snap phases and the max-ring failure case.
- T001 exact-SHA run `33806714712` completed successfully for source `82ba46b7450435d278482fa90391fa5cba381f50`. The CI transport was not replaced while that request was queued/running.
- T002 root cause is isolated: the old completion path rebuilt the final ring mesh and disabled fallback solely when that ring's job finished, allowing an intermediate publication hole. `FarTerrainStartupCoverage` now models contiguous publication/retirement, and regressions prove early final-ring readiness cannot retire fallback and the legal transition cannot shrink coverage.
- Runtime integration tracks authoritative publication separately from sampled heights, protects the fallback-owned final mesh from normal rebuild/refresh, and atomically publishes the final ring only after all lower current rings plus guaranteed final coverage are ready.
- T003 adds `VoxelFarTerrain.CoverageSnapshot` plus `CoverageDiagnostics` and per-ring spacing diagnostics. The snapshot reports requested radius, current guaranteed contiguous authoritative radius, configured ring count, and actual startup-fallback state; it is read-only presentation/debug data. Focused coverage tests prove configured six-ring/12 km diagnostics before publication.
- `VoxelShowcase.DescribeFarTerrain()` now formats requested radius, authoritative radius, ring count/spacings, and fallback state into the existing scene diagnostic surface. An intermediate connector edit accidentally rewrote the full file from truncated content; commit `da14c4d2bc437dd1747de271f634db30f2b9b770` restored the exact prior blob, and the reapplied diagnostics commit `6fbee714aaa52038a7aa436c1b048f851c3ff473` was verified to change only the intended `DescribeFarTerrain()` lines.
- Exact-SHA run `33810025742` succeeded for source `ab7028bc51ca9cb3b282e812ea997befaeae8d0e`; its automatic module validation and standalone-player SceneIssue replay both passed. This validates T002/T003 on their combined source.
- T003A preserves the existing shared `TerrainMaterialSet.SurfaceAt(...)` height-family rule rather than inventing a far-only slope/biome policy. `TerrainQuery.SlopeAt(...)` remains deterministic renderer-neutral terrain information; focused tests cover representative elevation bands and canonical slope derivation.
- Rendering already owns semantic-free `MaterialPresentationDefinition` rows. `VoxelMaterialPresentationInstaller` now publishes the installed material albedo/sampling/surface/variation rows as renderer-global shader state at the installation boundary, so far-world consumers do not depend on the near-surface pass having rendered first.
- `VoxelEngine/FarTerrain` resolves the existing clipmap vertex albedo to an installed opaque material row once per vertex and uses deterministic voxel/world coordinates. Commit `036a317bf99742db1e84a3681929ce42841ff893` extends this with per-fragment fine/macro world-space detail, triplanar-capable procedural response, derivative-footprint filtering as an analytic mip selector, filtered detail-normal response, and roughness variation. The path adds no far vertex-density or residency control, so T003B improves shading without increasing clipmap geometry/resident voxels.
- `TerrainPresentationInputTests` proves representative elevation-family selection and slope determinism independently of Showcase. `FarTerrainPresentationContractTests` verifies the far shader consumes renderer material rows and now also rejects geometry-density/residency coupling while requiring derivative-filtered detail. T003B remains unchecked until exact-SHA CI and built-player distance captures prove it visually.
- Exact-SHA run `33811477011` succeeded for feature source `9fc11335a016caf94fb856a407b0f85922f0f1f8` through CI head `99fc1b755f9ee37731242a566a16b7c8c4937e97`. Automatic module validation and standalone-player replay both completed successfully, so T003A compiles and runs through the production validation path.
- The durable screenshots from run `33811477011` are not valid T003A visual proof: `showcase-000-t013.6s-stationary.png`, `showcase-001-t023.6s-stationary.png`, and `verification-final.png` all show only `Loading Kentridge...`. The player log continues rendering/scheduler activity rather than crashing, so visual acceptance remains blocked pending a replay that reaches the rendered world; acceptance is not reduced.
- Per `master-sync-required.md`, older master `81ffa4bbc76c3feb6e0bde2376065b4144f3f10a` was merged into agent-7 as verified two-parent merge `aa67a372e47f56536f12f274302d5b1c9bc7f615`.
- On 2026-09-03 current `origin/master` `c7774f8f3455481f003898bbe473789348cd4f66` was merged into `fixes/agent-7` through sync PR #244, producing synchronized feature source `0e1bb862e72f63a526b6b22fecd9e853a6231703`. The exact-SHA T003A replay request was built directly from that source as CI transport commit `0517f25f1da1814c9d3d6bfa1993399fbf3a9859`; run `33822302367` is queued and must not be replaced while queued/running.
- T004 is implemented at feature commit `771c04440333f83da9a534756575565b6018e3fc`. `StructureFarPresentation` lives in WorldBuilder Architecture and contains stable structure/cluster identity, canonical world footprint/height/facing, archetype/massing keys, an architecture/material-family key, semantic `StructureVisibilityClass`, and a deterministic FNV-style revision over planning/architecture inputs. `StructureFarPresentationResolver` takes explicit semantic cluster/visibility inputs and never derives importance from coordinates. `StructureFarPresentationTests` covers value-equivalent deterministic output, site geometry/facing/height agreement, and coordinate-independent semantic visibility. Exact-SHA CI is still pending because the earlier T003A replay owns the CI transport queue.

## Current head and remaining gates

T001-T003 have exact-SHA green evidence. T003A implementation and focused regressions have exact-SHA compile/runtime green evidence, but T003A remains unchecked because required built-player visual proof is blocked by prior loading-screen-only captures; current-master replay `33822302367` is queued on exact source `0e1bb862e72f63a526b6b22fecd9e853a6231703` and must be left alone. T003B and T004 are implemented on the feature branch but remain unchecked pending exact-SHA CI; T003B additionally requires built-player proof at the specified distance bands. T003C remains conditional on demonstrated silhouette loss. Continue the next unchecked non-blocked task while the queued replay owns CI transport, and do not close until all required T001-T033 requirements, built-player visual proof, and budget gates are complete. All future targeted validation must use only `ci-test/fixes/agent-7`; final promotion is PR + auto-merge, never a direct push of the exact feature head to `origin/master`.
