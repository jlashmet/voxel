# 25 Multiplayer end-to-end gameplay validation — implementation plan

**Acceptance:** prove the production packaged-player multiplayer loop with separate processes: formation/entry, shared authoritative gameplay, interruption/reconnect identity continuity, explicit leave, configured capacity/join-in-progress/repeated reconnect, persistence/rehost policy, and durable exact-SHA evidence.

**Ownership / architecture:** shared built-player validation infrastructure plus Kentridge playable-composition validation. No gameplay authority, alternate transport, or test-only networking runtime. Production semantics remain in `SessionOrchestration`, `Sessions`, `GameplayReplication`, and `Continuity`; validation drives public player/session inputs and observes read-only semantic truth.

## Observed state / hypotheses

- **H1: System25 is integration validation over existing production systems. Supported.** Session formation owns Host/Join; party/session queries expose stable member/slot/character identity; network admission binds transient connections to those identities; Continuity owns reconnect/recovery.
- **H2: a parallel test networking/runtime layer is needed. Rejected.** Public production seams already represent formation, identity, reconnect, readiness, and leave.
- Shared `mode: multiProcess` is complete for build-once role launch/wait/kill/relaunch, isolated role state/logs, automatic source-SHA + executable-SHA256 verification before gameplay waits, bounded semantic milestones, and per-target artifacts.
- Correctness audit tightened reconnect evidence: ephemeral HOME/temp/config/cache are attempt-scoped while only the role durable state root survives relaunch; sequential waits consume milestones monotonically; player payloads cannot override harness-owned role/attempt attribution.
- Release-tier discovery is structural: ordinary production diffs use smoke targets; `<Module>/Validation/Release/` targets run when changed and from the generic scheduled/manual release workflow.
- Exact harness/tooling proof: feature `496e3f9d7a88658029aa332b3596caf86e1cabb2`, request `bbb63019d00a93b44cff24c7a2b20d7ae12e461e`, run `33936350595`.
- Exact build-identity proof: feature `73c3bafd0268fcc80c453d33900f6848e7571153`, request `71b21322f8d3bb776553e5d915bc10d2c0664695`, run `33937957149`; player SHA-256 `8129fbc54fb83c5ac219204df3c9fd2c7482d9c1225daa87c67447cec9db27ae`, `identityVerified: true` before gameplay acceptance.
- **Harness correctness exact proof:** feature `b2d1847ec109eb8be7a631c084bd60230c5932a2` passed request `382b9c33a2f4edabd7bf46c598e1ee1d9eea0291` in run `33986100313`; this closes T25-004 and T25-008. Feature `1727641603ba4645a798b3ca246bc8d9130afb95` then passed request `06585b896b23c38a7c929dd3294211c68ca494a2` in run `33987833161`, closing T25-009. Independent harness work is fully exact-SHA green.
- **T25-007 design boundary is resolved but not integrated:** use `IPartySessionQuery` for durable roster identity, `IGameplayReplicationReadState` / `GameplaySynchronizationStatus` for revision/readiness/current semantic state, and `IContinuityQuery` for recovery state. The final composed player diagnostic must reuse System24's production composition/read-only surface; no mutation method, socket id, transport handle, or second privileged diagnostic seam is justified.
- **T25-050 complete:** static audit confirms the System25 harness only controls production player processes, identity, state isolation, semantic waits, and logs; it contains no direct socket injection, alternate transport, gameplay mutation authority, or privileged command seam.
- **Current-master compatibility merged and proven:** `origin/master` `3654c13f72ed157c53b340443a766795d772f596` changed the Kentridge playable asmdef, so it was compatibility-relevant. Verified merge commit `72fe27a85f458b047d21f1f2325ff768619bca90` has parents `b3e6d9d778e4544b37a808f23340be6d99a41a90` and `3654c13f...`; compared with master it contains only the 15 System25-owned files, with no System24 import. Exact feature `94ab660260d9b066f030f702500aba092b321a98` then passed request `4625c82ab1710411723aa4afdecc646826f8fe51` in run `33992072202`, including repository-selected automatic module validation and artifact publication. This is compatibility evidence only, not final multiplayer acceptance.
- **External prerequisite remains blocked:** System24 is still unmerged. Its first exact feature `f6b3ace316f7122b48135ea04c0f04078049d9a5`, request `64296df9f805d2690942d5f302e07a32f3a8b823`, run `33984774287` failed with Kentridge composition compile errors. Exact feature `d0971511dee5affc0064adcc83f6c2e9d7b7b050`, request `a344880385e357aa0a7cef83a5aeb5b1a59a51d5`, run `33988814815` exposed a second compile layer. System24's owner has since committed the compile disambiguations, production `PrimaryPressed` combat path, focused combat regressions, enemy-turn-only combat AI, and physical-primary-input scenario work through `e724343d0f2a2d05631e1305f344ba94f832e94f`; its binding gameplay/save/diagnostic/built-player checklist and replacement exact-SHA proof are still incomplete. System25 must not copy, repair, or substitute that unmerged production composition/diagnostic path.

## Selected work / remaining gates

1. Keep the independent harness foundation stable; its correctness tasks and current-master compatibility are exact-SHA proven and require no further changes unless a production scenario demonstrates a defect.
2. Monitor System24 until its production composition compiles, passes built-player acceptance, closes, and merges. Do not patch or duplicate its owned Kentridge production route from System25.
3. Immediately after System24 lands, merge current master again if needed and reuse its production Kentridge entry/read-only diagnostic seam. Compose real multiplayer formation behind `ISessionFormationService`; validation must never receive UTP/socket handles.
4. Add authority + two-client smoke validation under the owning Kentridge validation surface. Prove durable `PartyMemberId -> PlayerSlot -> CharacterId`, baseline revision convergence, contention/conservation, vitality, progression, unexpected interruption/reconnect/current-state recovery, and explicit leave.
5. Add structural `Validation/Release/` scenarios for configured capacity, join-in-progress, repeated reconnect, and persisted rehost.
6. Run repository-selected final exact-SHA validation, inspect durable separate-process artifacts, complete every task/acceptance item, populate closure fields and move open -> closed. Merge latest master, then promote only through PR + auto-merge and satisfy the required affected gate.

**Current branch state:** current master is merged and the resulting System25 compatibility head `94ab660260d9b066f030f702500aba092b321a98` passed exact request `4625c82ab1710411723aa4afdecc646826f8fe51` in run `33992072202`. Subsequent SceneIssue bookkeeping does not substitute for final exact-SHA production multiplayer validation after System24 integration.

**Blast radius / cost:** validation tooling/workflows plus Kentridge validation composition only; production authority remains untouched. Smoke is authority + two clients; expensive cases stay in release targets.
