# 25 Multiplayer end-to-end gameplay validation — implementation plan

**Acceptance:** one exact production build with separate authority/client OS processes proving formation, durable identity + baseline convergence, contention/conservation, combat/vitality, progression, interruption/reconnect/current-state recovery, explicit leave, configured capacity, join-in-progress, repeated reconnect, persisted rehost, automatic selection, and final exact-SHA evidence. Every `tasks.md` item remains binding.

**Ownership / architecture:** use only production Application/Sessions/Net/Kentridge/GameplayReplication/Persistence/gameplay seams plus generic validation orchestration. No fake networking, parallel authority, privileged mutation command, alternate transport, or weakened freshness rule. `Game.WorldObjects.Runtime` remains headless with module-local unit coverage. Application owns the session lifecycle; Kentridge Playable owns separate-process runtime proof and structural release coverage under its module `Validation/Release/` surface.

## Material results

Earlier exact runs proved generic process isolation/build identity, provider/UTP admission, joined-party startup, durable roster identity, replication readiness, and repository-owned smoke/release selection. Exact source `6401622dab168f474aa2abc19eb8828e36cbaa07` narrowed the remaining Sessions failure to a test-only `IReadOnlyList.Count` assertion; `6a34fc66d728b90d83379cff81671065b0b44976` corrected it without production behavior changes.

Exact source `ae08ee1c97bbfdc8106e525db58147e285a4c54e`, run `34202599000`, passed persistent EditMode gates and proved real three-process topology/baseline, exactly-once contention, shared progression, and Continuity interruption before timing out on combat. `b541b59e51675d656117f46beecf39801266ae4e` corrected the discovered Application lifecycle defect by keeping the Running session graph ticking after `InGame`; `a69ba7a2629f9e27d7a9f07e68479c85aef061be` adds the focused regression.

Exact source `1bd01c2effe23b3f6c9b3e889486d5e7b7a45b98`, request `89cf8233aa0739ce25a0904eb2756d2417475e32`, run `34212118646`, job `102015303375`, artifact `10052221929`, digest `sha256:00d07ea1b0f60bd68e52bcd123f6125e5f2452ed61df494d339e0969f3b4120c`, passed all selected persistent EditMode assemblies including `InGameContinuesAdvancingRunningSession` and again reached topology/contention/progression/Continuity in the built player. Authority then logged `[KentridgeCombat] battle-complete ... winner=Enemy actions=3 turns=1` before any authenticated host combat action could be accepted. Root cause isolation showed the three encounter-owned bandits entered before the persistent player, all actors had 6 Vitality, and 3 x 2-damage enemy turns killed the player before its first turn; additionally the generic AI driver must not consume player turns. The feature ports the established enemy-only AI and Kentridge-owned 40-player/6-bandit Vitality semantics without weakening System25 validation.

Exact source `55fe435637cbee35454b7c839ff17c500fac2633`, request `e4b4010a1a2e93770e3a1bf0e271ea6fbe34599c`, run `34218169823`, job `102034779084`, artifact `10053235307`, digest `sha256:779a04afa7fbde066ef9ac2728c1a04cbd64f3c4e946ad377e3fff026037f2a5`, passed every selected persistent EditMode assembly and advanced the exact three-process smoke through topology/baseline, exactly-once contention, shared progression, Continuity interruption, and authenticated host combat. Authority emitted `combat-vitality-converged` for `enemy:kentridge-forest-bandit-1` at `4/6` with `appliedCombatActions=1`; client B converged on the same current Vitality. A fresh client-A process then rejoined with the same durable member `gamesystem25-topology:member:2`, character `kentridge-player-2`, and slot `1`, and authority observed `continuity-recovered`. The run failed only when the recovered client attempted its local session graph while GameplayReplication had transiently re-entered resynchronization: `KentridgeReplicatedPartyScreenQuery` served cached Active party projections even though global `IGameplayReplicationReadState.GameplayReady` was false, then `KentridgeReplicatedClientSessionGraphFactory.Compose` correctly rejected those incomplete bindings. Production fix `a45df5a5749530ceaf41c33efb386d6a11ea438b` suppresses party presentation while global replicated gameplay state is not ready, so Application waits rather than consuming its one startup attempt from stale projections. Regression `fd0199fa280c2d897bb908d431e808665ef1524b` preserves cached Active projections across a readiness drop and proves the party query becomes unavailable until readiness recovers.

## Current discriminator / remaining gates

Hypothesis A: gating replicated party presentation on global GameplayReady lets recovered client A remain in the normal joined-party wait path during transient repair, then start its local graph once the exact current projection set is complete; the smoke should continue through recovered 4/6 Vitality/current inventory/progression and explicit leave. Hypothesis B: exact execution reaches a narrower reconnect repair, current-state, or leave defect. Next experiment: exact-SHA targeted CI from the current feature head; only matching built-player milestones may close runtime boxes.

After smoke succeeds, verify structural release capacity/JIP/reconnect/rehost evidence, complete every remaining checkbox, record final exact-SHA evidence, close `open -> closed`, reconcile current master, then PR + immediate auto-merge and monitor the required `affected`/standalone Kentridge gate until merged closure is visible on master.
