# Experiment 053 — close survey active; full-app publication tail

## Exact source / run
- Feature source: `b59fe1a7d20ab819bb9df56a4c7577492df3453d`
- CI transport: `c150b12e7539bf908ea590cd8befc450a2056e9b`
- Workflow run: `34196421399`
- Artifact: `single-test-34196421399` (`10045139036`)

## What passed
Persistent repository-derived tests pass, including the explicitly requested macro-world PlayMode regression. The target-property fix is effective: both built players are process-clean, the close-oblique settlement helper emits `close-survey`, and real local plus macro-road `CharacterMotor` traversal executes.

The focused Kentridge module player advances materially farther than the prior exact run: it captures Moordell, macro-road traversal, road-arrival, and Rossdam under strict coverage, then begins the Rossdam lake detour. Rossdam content is therefore physically present and the close-survey composition can converge on the current production renderer.

## Remaining exact failure
The focused 180-second module scenario still ends before `capture-ready target=macro-network-overview`; it reaches lake content readiness but not the later Fairy/Orc/ridge/network sequence.

The full 180-second SceneIssue replay is also acceptance-red. It reaches Rossdam with the same close survey and all authored Rossdam content settled, but strict presentation coverage stops at `missingVisible=5`. Tail telemetry remains under `NoSlot` pressure with approximately nine requests in flight and the oldest request aging to ~37 seconds while publication stops advancing. No runtime exception, forced generation, radius widening, budget change, or storage-content failure is involved.

## Discriminator
The extra full-profile components were inspected. Rossdam/capture/residency diagnostics are observational and their legacy 70m pose checks are dormant under the active 31m close survey. The Application start companion only transitions the real app from MainMenu to New Game. The content-demand and close-survey helpers are shared by the focused player. No full-profile-only agent-6 mutator explains the five-chunk tail.

While this run executed, `master` advanced to `b56436f198702fa91bfaccec69a30df25a52a2cd`. Its renderer-related change is test-only (`GpuQueuedBatchCancellationTests`); the rest is HouseShowcase/Structures work. Repo workflow still requires synchronizing it before final classification.

## Next proof
Merge current master into agent-6 without altering macro-world behavior, then exact-SHA rerun the same repository-derived module/player and SceneIssue replay. If the same clean `NoSlot` publication tail survives on the synchronized source, keep the SceneIssue open and record the shared renderer liveness prerequisite rather than weakening coverage, budgets, residency, or adding an agent-6 renderer patch.
