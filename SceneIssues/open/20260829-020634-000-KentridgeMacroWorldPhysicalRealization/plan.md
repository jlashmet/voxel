# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. Keep the source-backed Mounting Force/Kentridge graph authoritative through shared WorldBuilder APIs; physically realize settlements, terrain-aware hard routes, reusable geography, Rossdam Lake, Southern Ridge/pass, real CharacterMotor traversal, readable built-player evidence, and bounded cost. `AGENTS.md`, `SceneIssues/README.md`, and `SceneIssues/feature-readme.md` govern this feature.

## Proven results
- Foundation: 20 hard routes, 6 settlements, 16 generic buildings, deterministic geography, bounded road slopes, authoritative storage, substantial lake/ridge, explicit constrained routes, and two-stage presentation readiness.
- `RefreshPending` is isolated in a residency partial, and already-accepted X/Z columns additionally queue only feature-bearing Y-regions from semantic catalogue bounds. No horizontal interest radius, device budget, scheduler, or scene-specific preload policy changes.
- Reuse audit is complete: shared physical intent/planner/physical catalogue/water catalogue contain semantic inputs and no Kentridge/Rossdam policy. Existing generic `a`/`b` blocked-water fixture independently proves planner rejection and semantic `GoAround` reuse without Kentridge adapters.
- Exact run `33346099006` proves ordinary production `ShowcaseWorld.StepStreaming` makes a real authored upper feature Y-region resident without moving the viewer into that layer.
- Experiment 018 found presentation readiness could become true before authored upper layers were feature-published. The shared readiness predicate now uses the same cached semantic feature-layer query as residency; exact source `9d51fb9a947af76d0b8005c35288a7007dd6d9e6`, run `33354287850`, proves the lower-ready/upper-missing regression green.
- Experiment 019 exact run `33359142139` proves every Rossdam semantic building maps to a production structure placement with roughly 1.03–1.20M authoritative structure voxels, grounded at production minimum Y with full authored wall-height occupancy. Generation/storage/grounding are rejected as the surviving visual owner.
- Experiment 020 is complete. After several compile-boundary corrections, exact readiness-aligned run `33370392969` proves all four Rossdam authored bounds intersect the real survey frustum and each intersects four ready visible production solid chunks at source step 2 after all four target columns are content-settled. The full-resolution capture still shows one unmistakable complete building. Generation, grounding, readiness, target ownership, frustum intersection, and coarse renderer publication are rejected.
- Exact run `33372188117` is workflow-green but closure-red: vertically lowering the validation survey camera to 45 m inherited the old 70 m rotation and visibly cropped/mis-aimed the settlement. Only Moordell/Rossdam fit before the 60 s cutoff.
- Exact run `33373258471` is product-red in the focused focus-ray-dolly regression (`0.027976°` versus `<0.01°`) while its built-player step succeeds. The second materially different camera correction still leaves the one-readable-building/cropped-settlement symptom, so experiment 021 was required before another correction.
- Experiment 021 isolates the minimal framing root cause. The generic four-plot footprint spans X 51.6 m and Z 48.4 m; along the diagonal survey axis that is about 35.36 m half-span. The exact scene lens is 58°. Even the original flat-terrain camera-to-focus vertical separation is about 62 m, giving only ~34.37 m half-span at 58°. Experiment 020's `GeometryUtility.TestPlanesAABB` proved intersection, not full containment; all four buildings can therefore be reported in-frustum while outer structures are cropped. Lowering the camera necessarily worsens containment.
- The current correction keeps the established 70 m semantic survey pose and production LOD/streaming unchanged, widens only validation settlement surveys to 72°, and restores the normal lens immediately outside settlement evidence. The focused regression encodes both the demonstrated 58° envelope failure and the 72° containment margin.

## Current gate
Exact feature code source `5ca4d29efc8c538806f078e3393aa941526a5ede` is represented by the sole CI wrapper `72639b4b36559aed26af056ea6b09706fe37cc01` on `ci-test/fixes/agent-6`, run `33376804313`. At the latest check the self-hosted macOS job is queued with no runner assigned and no steps started. Do not replace or alter that queued request.

When it completes: classify the focused lens-containment regression first, then inspect full-resolution Moordell/Rossdam evidence. If framing is finally closure-quality, continue to the independent 60-second sequencing/cost gate; if not, do not make another camera tweak without a new discriminator tied to the observed failure.

## Remaining gates
1. Prove all four readable authored blockouts at Moordell, Rossdam, Fairy Village, and Orc Village with streets/open space and road arrival/exit in full-resolution built-player evidence.
2. Preserve player-height road and real CharacterMotor evidence; capture substantial Rossdam water plus constrained route, Southern Ridge/pass, differentiated countryside, and final macro-network overview.
3. The supported replay remains fixed at 60 s. Current completed telemetry reaches Moordell around t42 and starts Rossdam around t44; if correctness still exceeds the cap after framing is fixed, quantify target phase cost and make only an acceptance-required validation-sequence/performance correction. Do not weaken readiness, pre-generate targets, widen residency, or extend replay duration.
4. Quantify added feature Y-region residency and final world-build/route/feature/CPU/FPS/memory/streaming/render/far-field cost against budgets.
5. Re-fetch current master and re-check exact feature diff immediately before final targeted CI.
6. Only after every checkbox is green: move `open -> closed`, set `status=fixed` and `resolvedUtc`, merge current master, and non-force push that exact feature head to master; if master advances, fetch/merge/retry.
