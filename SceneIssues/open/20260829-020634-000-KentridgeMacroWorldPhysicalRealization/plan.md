# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. The source-backed Mounting Force/Kentridge graph remains topology authority while shared WorldBuilder APIs must physically realize every settlement, contiguous terrain-aware hard routes, reusable geography, a substantial lake/ridge with explicit route responses, real CharacterMotor traversal, closure-quality built-player visuals, and measured streaming/render cost. `SceneIssues/README.md` is the current repository workflow authority.

## Material results / hypotheses
- Proven foundation: 20 hard routes, 6 settlements, 16 generic buildings, 6 regions, deterministic geography, bounded road slopes, authoritative storage, and two-stage presentation readiness.
- Rejected evidence strategies: broad residency, serial building-centre pinning, an external camera helper, lake-first evidence ordering, and replay >60 s. Normal production streaming demand must remain authoritative.
- Earlier product evidence showed the old Rossdam lake missed the direct Rossdam road; moving the accepted `900 x 450 x 24 dm` Between-region south by 210 dm restored a real obstruction, while a 2 dm water surface sheet reduced primitive scan work from 16.59M toward ~9.28M cells without shrinking the carved basin.
- Exact-SHA run `33301222246` is product-red: six focused tests pass, but the southward Rossdam footprint newly blocks hard route `forest -> fighting-area-1` and the planner correctly rejects it because no semantic solution is authored. Its 60 s standalone player remains process-clean with zero harness assertions.
- The failure is not a planner defect: existing Rossdam-intersecting routes use explicit `GoAround` semantics. The smallest correction is to author the same dry-ground solution for the newly intersecting forest approach and pin it behaviorally.

## Selected remediation
- Preserve Rossdam Lake's semantic relationship, accepted dimensions, southward footprint, full-depth basin carve, and 2 dm surface presentation.
- Add only `forest -> fighting-area-1` → `rossdam-lake` `GoAround` intent with existing dry-road clearance semantics; add a focused regression proving the route is geography-constrained and every road-corridor tile remains outside the lake.
- Keep evidence order `Moordell -> Rossdam -> lake -> Fairy -> Orc -> Southern Ridge/pass -> network`, real content-settled + renderer-coverage gates, grounded streaming demand, near-overhead settlement framing, player-height road arrival, and the 60 s cap.

## Blast radius / remaining gate
Current master `e95324aeaef619cb49d84bf2b07f770184bead81` is the merge base and the feature is ahead-only. This remediation changes one Kentridge route constraint plus one focused regression; it does not alter load radius, device budget, CharacterMotor, topology, lake geometry, shared planner behavior, or another SceneIssue. Request one new exact-SHA PlayMode CI on the existing agent-6 transport, inspect the focused result and full standalone evidence/telemetry, then promote only if every remaining task and acceptance criterion is proven.