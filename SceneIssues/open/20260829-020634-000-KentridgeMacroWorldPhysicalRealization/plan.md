# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. The source-backed Mounting Force/Kentridge graph remains topology authority while shared WorldBuilder APIs must physically realize every settlement, contiguous terrain-aware hard routes, reusable geography, a substantial lake/ridge with explicit route responses, real CharacterMotor traversal, closure-quality built-player visuals, and measured streaming/render cost. `SceneIssues/README.md` is the repository workflow authority.

## Material results / hypotheses
- Proven foundation: 20 hard routes, 6 settlements, 16 generic buildings, 6 regions, deterministic geography, bounded road slopes, authoritative storage, and two-stage presentation readiness.
- Earlier evidence proved the old Rossdam lake missed the direct Rossdam road. The accepted `900 x 450 x 24 dm` lake was moved south 210 dm so the direct Rossdam approach genuinely intersects it; a 2 dm water sheet keeps the full carved basin while reducing primitive scan work toward ~9.28M cells.
- Run `33301222246` then exposed `forest -> fighting-area-1` intersecting the moved lake without semantic intent. An explicit dry-ground `GoAround` plus focused regression was added.
- Exact-SHA run `33302489798` is product-red: the physical acceptance test now reaches `BuildAround` but reports `No dry detour could be built around region 'rossdam-lake'`. The standalone 60 s process exits cleanly, but the focused gate fails and capture telemetry reports `SURFACE visible=0`, so it is not closure evidence.
- Hypothesis A: the moved lake contains a route endpoint/corridor, making any dry `GoAround` impossible. Hypothesis B: both endpoints are dry but the reusable four-side rectangular detour candidates are insufficient. Authoritative node coordinates discriminate A vs B; changing clearance does not.

## Next discriminator / selected-fix rule
Resolve physical centres for `forest` and `fighting-area-1` from the source-backed layout and compare them with resolved Rossdam bounds including road half-width. If either endpoint is inside, adjust only Kentridge lake placement enough to keep both endpoint corridors dry while retaining exact accepted lake dimensions and genuine direct-Rossdam obstruction. If endpoints are dry, fix the reusable detour solver minimally and regression-test affected consumers/cost before changing Kentridge intent again.

## Blast radius / remaining gate
Current master `e95324aeaef619cb49d84bf2b07f770184bead81` remains the merge base and the feature was ahead-only at the last check. No load radius, device budget, CharacterMotor, topology, or unrelated SceneIssue changes are permitted. After the discriminator and focused regression pass, use one new exact-SHA PlayMode request on the existing agent-6 CI transport with the supported 60 s replay; inspect NUnit, standalone screenshots, streaming/render telemetry, and final cost before promotion.