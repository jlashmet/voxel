# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. The source-backed Mounting Force/Kentridge graph remains topology authority while shared WorldBuilder APIs must physically realize every settlement, contiguous terrain-aware hard routes, reusable geography, a substantial lake/ridge with explicit route responses, real CharacterMotor traversal, closure-quality built-player visuals, and measured streaming/render cost. `SceneIssues/README.md` is the repository workflow authority.

## Material results
- Proven foundation: 20 hard routes, 6 settlements, 16 generic buildings, 6 regions, deterministic geography, bounded road slopes, authoritative storage, and two-stage presentation readiness.
- Earlier evidence proved the old Rossdam lake missed the direct Rossdam road. The accepted `900 x 450 x 24 dm` lake was moved south; its water surface was reduced to 2 dm while preserving the full carved basin.
- Run `33301222246` exposed `forest -> fighting-area-1` intersecting that moved lake. Adding `GoAround` produced exact-SHA run `33302489798`, which is product-red because no dry detour can start from an endpoint already inside the blocker.
- Authoritative grid coordinates prove endpoint engulfment, not a shared-planner defect: `forest=(0,2)`, `fighting-area-1=(0,3)`, `overworld-moordell=(1,3)`, `overworld-to-rossdam=(-1,4)` at 800 dm/cell. Current fixed-seed lake centre is `(-302,+2595)dm` relative to Kentridge; road-expanded half-bounds are `468 x 243 dm`, so `fighting-area-1=(0,+2400)` is inside (`302,195`).

## Selected remediation
Keep exact resolved dimensions/depth and reusable planner semantics. Change only Kentridge Rossdam nominal offsets from `(-300,-210)` to `(-400,-155)`, resolving to `(-402,+2650)dm`. This leaves `fighting-area-1` dry (`dz=250 > 243`), keeps its outbound `fighting-area-1 -> fighting-area-2` road genuinely constrained, and keeps the direct Rossdam route constrained on the `x=-800` leg (`dx=398 < 468`). Remove the accidental `forest -> fighting-area-1` lake constraint; replace its temporary regression with endpoint-dry, inbound-dry, outbound-constrained, and Rossdam-constrained assertions.

## Blast radius / remaining gate
Remediation is Kentridge intent/test only: no shared planner, topology, load radius, device budget, CharacterMotor, lake dimensions, or water-scan volume change. Pre-CI fetch shows master still `e95324aeaef619cb49d84bf2b07f770184bead81`; feature source `fa8544903880e0d9675e5e7960b974db6244abc1` is ahead-only. Request the focused endpoint-safe Rossdam PlayMode regression on that exact source with the required 60 s scene replay using only existing `ci-test/fixes/agent-6`; inspect NUnit, standalone full-resolution captures, streaming/render telemetry, and final cost before promotion.