# Plan

## Acceptance authority
`issue.json` is the contract (`captures: []`). Preserve the source-backed Mounting Force graph while delivering physical settlements, terrain-aware hard routes, reusable geography, Rossdam Lake, Southern Ridge/pass, CharacterMotor traversal, durable built-player evidence, and bounded cost. Follow `AGENTS.md`, `SceneIssues/README.md`, and `feature-readme.md`.

## Proven results
- Production physical planning covers 20 hard routes, 6 settlements, 16 generic buildings, deterministic geography, constrained-route solutions, lake/ridge realization, bounded slopes, storage, and feature-aware vertical residency/readiness. Reuse is proven by independent non-Kentridge blocked-water, spatial-reservation, and synthetic generic-blockout fixtures.
- The playable ownership fix remains scene-local: the intended Kentridge catalogue consumes the one-shot macro selection before `ShowcaseWorld` constructs its temporary bootstrap catalogue. Exact source `d36d96e...` passed focused ownership/module/player validation; runtime catalogue contains 480 macro-aware definitions.
- The demonstrated generic-blockout throughput defect was a retained solid full-footprint foundation slab. Four bounded perimeter plinth boxes preserve grounding/footprint/material/support/collision/bounds. The independent 100x80x60 fixture reduces foundation box volume 82,432 -> 29,440 (64.3%), timber body 416,000 -> 71,552 (82.8%), and combined foundation+timber work 79.7%; roof work is unchanged.
- Master renderer restoration was explicitly synchronized into this branch at merge `5185b5acddd3d35ec29730ea73d30ae796a705bf` from master `f5593cc1236ba3963fc5713a11df35292628e97d`.
- Exact-SHA run `33800775569` on feature source `5185b5acddd3d35ec29730ea73d30ae796a705bf` is green: repository-derived module validation passed, the requested perimeter-foundation regression passed, and the standalone player process completed. This closes the previously blocked plinth behavioral gate and supports acceptance (9).
- Because strict publication remained false under the first elevated survey after materially different executable states, experiment 037 isolated the discrete residency/metric-radius mismatch. The Kentridge-only coverage installer keeps radius-3 streaming and all device/scheduler/generation policy unchanged while narrowing only the strict near-surface completeness ring to the conservative guaranteed 102.4 m disk. `KentridgeStreamingCoveragePolicyTests` independently verifies that geometry.
- Exact-SHA run `33804821454` on feature source `50bc9533b811ac587d24503844d3d2c73d0c37bf` is green for that radius repair: the requested independent regression, repository-derived module validation, and 180-second player process all passed. Runtime renderer diagnostics prove the configured policy is active: step1 remains `0-57.6m`, step2 ends at `102.4m`, and the step4/step8 bands collapse at `102.4m`.
- The same run still did not satisfy visual acceptance. Immediately before the macro evidence driver entered Moordell, renderer diagnostics showed `missingVisible=0` and `coreAbsent=0`. On the first elevated Moordell survey frame, `coreAbsent` jumped to 2,178 and then 2,922 while GPU requests remained admission-bound and strict coverage never recovered. This is a new, narrower failure boundary rather than evidence that the radius policy failed.
- Experiment 038 isolates that boundary to validation composition. `KentridgePlayableSlice.Update` streams around `CharacterMotor.EyePosition`, while the macro evidence driver had pinned the motor to settlement ground but moved the actual renderer camera roughly 70 m above a different survey point. The shipped `StepAutoSurvey` path already defines the correct contract by keeping `CharacterMotor.Position = camera - eyeHeight`, so Storage and rendering consume one 3D demand point.
- The selected correction stays scene-local to the evidence automation: give the driver explicit pre-slice execution order, resolve one survey camera world position, pin `CharacterMotor.EyePosition` to that exact position before streaming, and reuse the same position for the rendered camera. `KentridgeMacroWorldSurveyStreamingAlignmentTests` independently asserts the ordering and shared 3D region contract. No shared renderer/world API, horizontal interest radius, generation/device budget, far-field radius, or strict publication semantic changes.
- Partial cost evidence remains context only: median FPS 103.9 after 30 s, median mean-frame 9.61 ms, worker-p95 median 3.018 ms, admission-total median 1.1605 ms, and ~26.99 MiB cumulative render-arena uploads over 114 calls. Final process RSS/working-set, managed/native heap, GPU-memory footprint, and completed multi-target steady-state evidence are still required for acceptance (11).

## Selected fix under validation
The validated 102.4 m Kentridge coverage-radius policy remains retained. The current executable change under validation is the narrower survey-demand alignment in `KentridgeMacroWorldEvidenceDriver`:

1. `[DefaultExecutionOrder(-100)]` makes the validation driver establish its demand pose before the ordinary playable slice performs `StepStreaming`, removing reliance on incidental MonoBehaviour order.
2. `ResolveSurveyCameraPosition` derives the single authored elevated camera point from target DM coordinates, terrain height, and survey height.
3. `ResolveSurveyMotorPosition` places the CharacterMotor so `EyePosition` equals that camera point, matching the existing shipped `StepAutoSurvey` contract.
4. `ApplySurveyCamera` consumes the same resolved position rather than recomputing an independent pose.
5. `KentridgeMacroWorldSurveyStreamingAlignmentTests.ElevatedSurveyPinsStreamingDemandToRenderedCameraBeforeSliceStreaming` proves explicit ordering, exact eye/camera coincidence, and identical `ShowcaseWorld.RegionAt(...)` ownership.

Rejected alternatives remain: do not weaken `HasCompletePublishedNearSurfaceCoverage`, widen Kentridge load radius, change renderer GPU admission/publication semantics, modify generation/device/scheduler budgets, hardcode observed resident radii, or issue an identical retry. If the strict-coverage symptom survives this materially different composition fix, isolate the next failure boundary before any further production change.

Experiments 030-037 retain prior CI/cost/equivalence/acceptance evidence. `experiment-038-elevated-survey-streaming-alignment.md` is the current root-cause isolate and selected-fix rationale.

## Next gates
1. Submit the current exact feature SHA through `ci-test/fixes/agent-6`, targeting `KentridgeMacroWorldSurveyStreamingAlignmentTests.ElevatedSurveyPinsStreamingDemandToRenderedCameraBeforeSliceStreaming` with the required 180-second SceneIssue replay. Do not replace it while queued/running.
2. Require repository-derived module validation plus strict built-player progression beyond Moordell. The player must eliminate the survey-owned nonresident-core stall and produce evidence for Rossdam, Fairy, Orc, lake/constrained route, Southern Ridge/pass, macro network, and CharacterMotor road traversal before those visual criteria can be checked.
3. Inspect full-resolution built-player evidence for readable grounded settlement blockouts/streets/road arrivals, substantial Rossdam water and constrained route, ridge/pass relationship, differentiated countryside, final network overview, and traversal. Do not close on process success alone.
4. Quantify final multi-target residency/convergence, feature work, CPU/FPS/render/far-field behavior, and process/managed/native/GPU memory footprint against budgets. Prior measurements remain partial.
5. If the same strict-coverage symptom remains after this alignment fix, record the completed failure and isolate the new minimal boundary before another fix; do not weaken acceptance.
6. Do not close until every task/acceptance checkbox is validated. After green exact-SHA gates, move open -> closed, set `status=fixed` and `resolvedUtc`, merge then-current `origin/master`, revalidate affected work if required, and non-force push the exact final feature head to `origin/master`; fetch/merge/retry if master advances.
