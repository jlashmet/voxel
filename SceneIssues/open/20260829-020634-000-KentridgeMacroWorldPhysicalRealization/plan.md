# Plan

## Contract and ownership
Resume only `20260829-020634-000-KentridgeMacroWorldPhysicalRealization`. Keep the source-backed Mounting Force macro graph authoritative and deliver physical settlements, contiguous terrain-aware routes, reusable regional geography, Rossdam Lake, Southern Ridge/pass, real CharacterMotor traversal, durable built-player evidence, and bounded runtime cost. Affected ownership is WorldBuilder macro physical output, Showcase presentation readiness, and Kentridge Playable integration. Agent-6 has no Rendering production diff; do not weaken shared renderer acceptance or duplicate renderer ownership.

## Current evidence
Exact run `34192047180` on source `5bfe34d5fdab75e1b714fa120c93da122feb1472` proved persistent tests and the requested macro regression green, then exposed a validation-only `EvidenceTarget` field/property reflection error. Source `2056c882a09e50faf612ef070e6a71ba789b9290` fixes that by validated `PropertyInfo` access.

Exact run `34196421399` on source `b59fe1a7d20ab819bb9df56a4c7577492df3453d` proves that fix and the close-oblique survey are active: both players are exception-free, local and macro-road CharacterMotor traversal run, and the focused Kentridge player strictly captures Moordell and Rossdam before reaching the lake detour. The focused 180-second scenario still ends before Fairy/Orc/ridge/network evidence. The full SceneIssue replay reaches settled Rossdam content but stalls at `missingVisible=5` under sustained GPU `NoSlot` pressure with publication no longer advancing; the extra full-profile agent-6 helpers are observational or shared with the focused player, so no agent-6-only mutator explains that tail.

During that run `master` advanced to `b56436f198702fa91bfaccec69a30df25a52a2cd`; its renderer change is test-only and remaining changes are HouseShowcase/Structures. It was reconciled by two-parent merge `a5af427f99142e2a20a49e6ece1c47a5e566316c` without changing macro-world behavior. Experiment 053 records the discriminator.

## Selected next proof
Exact-SHA validate the synchronized branch. Require repository-derived tests/players, active Rossdam close survey, real CharacterMotor traversal, final `macro-network-overview`, and the 180-second SceneIssue settlement/geography/network evidence without runtime exceptions. Do not raise budgets, widen residency, force generation, weaken `HasCompletePublishedNearSurfaceCoverage`, or add speculative renderer changes.

If the same clean five-chunk/`NoSlot` publication tail survives on the synchronized exact source, keep this SceneIssue open and record the shared renderer liveness prerequisite rather than closing or opening a PR. If all gates pass, inspect durable full-resolution captures, record final convergence/FPS/CPU/GPU/streaming/memory evidence, complete every checklist item, move only this issue `open -> closed`, re-fetch/merge current master if needed, then open `fixes/agent-6 -> master`, enable auto-merge, and monitor the required PR `affected` gate until merged.
