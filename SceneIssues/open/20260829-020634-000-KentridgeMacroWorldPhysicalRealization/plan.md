# Plan

## Contract and ownership
Resume only `20260829-020634-000-KentridgeMacroWorldPhysicalRealization`. Keep the source-backed Mounting Force macro graph authoritative and deliver physical settlements, contiguous terrain-aware routes, reusable regional geography, Rossdam Lake, Southern Ridge/pass, real CharacterMotor traversal, durable built-player evidence, and bounded runtime cost. Primary ownership remains WorldBuilder macro physical output, Showcase readiness, and Kentridge Playable integration. Shared Rendering changes are allowed only when this feature's exact acceptance exposes a demonstrated correctness defect and the narrow production fix is required to unblock it.

## Current evidence and root cause
Prior exact source `abff52bba0c04c3940d98d6c24ebab2b081258ec`, transport `522fe183329e6ad645664e4bf1f88515b51ae5dc`, run `34213075572`, confirms repository module tests/requested macro regression complete, then real `KentridgeMacroWorldValidation` runs 180 seconds and fails only because `MACROEVIDENCE capture-ready target=macro-network-overview` is never reached. It advances through Application New Game, CharacterMotor, Moordell, Rossdam, and `rossdam-lake-detour`; the full replay also runs 180 seconds. Synchronized run `34200648642` showed settled lake content while publication stayed nearly flat under >100k retryable `NoSlot` allocation failures.

Minimal repro/code inspection isolates the liveness cycle: starting a replacement advances the arena's Desired generation. If replacement allocation exhausts, old live pages remain allocated, but pressure ranking rejects them because `live.generation != desired`. Those obsolete pages can therefore pin the capacity required by their own replacement indefinitely. `GpuSolidChunkCache.AcknowledgeGpuEviction` already acknowledges the published live generation, so retiring stale live pages preserves host identity semantics.

## Selected correction and remaining gates
On this feature branch, allocation pressure only (the coordinator's unfiltered `sourceStep == 0` pressure path) now permits stale live generations to be retired and ranks them ahead of ordinary off-screen victims. Filtered capacity pressure retains the existing current-generation/off-screen rule. No page budget, residency radius, generation authority, or strict coverage assertion changes. A real compute regression exercises visible current vs visible stale live generations through production pressure dispatch.

Current master `5c9a715a42735a0b573f436af1e3700dff2f591c` is already merged; it has no newer renderer correction. Run exact-SHA CI on the final synchronized feature head. Require every derived module test/player plus the 180-second replay to pass, including Fairy/Orc/ridge/network evidence and final `macro-network-overview`. Then inspect full-resolution captures as `production-quality`, record convergence/FPS/CPU/GPU/streaming/memory against unchanged budgets, complete all checklist items, close only this issue, open a fresh `fixes/agent-6 -> master` PR, enable auto-merge, and monitor `affected` until merged.
