# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. Keep the source-backed Mounting Force/Kentridge graph authoritative through shared WorldBuilder APIs; physically realize settlements, terrain-aware hard routes, reusable geography, Rossdam Lake, Southern Ridge/pass, real CharacterMotor traversal, readable built-player evidence, and bounded cost. `SceneIssues/README.md` is the workflow authority; `SceneIssues/feature-readme.md` is absent.

## Proven results
- Foundation: 20 hard routes, 6 settlements, 16 generic buildings, deterministic geography, bounded road slopes, authoritative storage, substantial lake/ridge, explicit constrained routes, and two-stage presentation readiness.
- Global feature-scheduler preemption is rejected: exact run `33318399738` proves Rossdam columns 1/3 in region `(1,10)` settle under uninterrupted normal 3 ms streaming once the validation driver retains demand.
- The same exact run is PlayMode `1/1` green and standalone-clean, with zero harness assertions, no swap growth, and captures through ridge/pass.
- Visual acceptance is still red. Moordell now reads as a multi-building blockout, but Rossdam shows only one readable building and Fairy/Orc show roads without their four authored blockouts. Lake evidence is a thin distant water strip. The 60 s harness reaches ridge/pass but exits before `macro-network-overview`.

## Competing hypotheses / discriminator
H1: authoritative storage lacks some remote building voxels despite plan/count readiness. H2: building voxels exist, but render-publication readiness/capture occurs before the renderer has consumed their feature mutations. Camera/frustum alone is unlikely because the settlement camera encloses all four planned centres.

Next discriminator: exercise the real production world at Rossdam/Fairy/Orc building centres after `IsPresentationColumnContentSettled`. Query authoritative storage for solid structure voxels at each planned blockout. If storage is missing, fix macro feature generation/placement. If storage is present, fix the renderer-publication readiness/capture gate rather than generation or residency.

## Remaining gates
Add the smallest behavioral regression for the proven storage-vs-render invariant. Improve lake framing without changing authored lake dimensions or budgets. Recover the final network capture by removing only redundant validation dwell after publication is truly proven; do not raise the 60 s cap or skip targets.

Then refresh/merge current master, freeze source, and reuse only `ci-test/fixes/agent-6` for exact PlayMode + 60 s real-player CI. Promote only if every required target is captured, Moordell/Rossdam/Fairy/Orc each visibly show >=4 grounded readable blockouts, lake/ridge/constrained route/network are clear, CharacterMotor evidence remains real, runtime is clean, and cost remains within existing budgets.
