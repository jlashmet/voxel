# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. Preserve the source-backed Mounting Force/Kentridge graph while physically realizing settlements, terrain-aware hard routes, reusable geography, Rossdam Lake, Southern Ridge/pass, CharacterMotor traversal, readable built-player evidence, and bounded cost. `AGENTS.md`, `SceneIssues/README.md`, and `SceneIssues/feature-readme.md` govern this feature.

## Proven results
- Foundation: 20 hard routes, 6 settlements, 16 generic buildings, deterministic geography, bounded slopes, semantic constrained routes, lake/ridge realization, feature-aware vertical residency/readiness, and production evidence sequencing.
- Experiments 019-025 ruled out missing planned structures, coarse visibility/readiness, and settlement survey framing. Experiment 023 replaced the costly solid blockout fallback with bounded hollow wall shells and retained an independent cost regression.
- Experiment 026 exact run `33417092425` proved Moordell and Rossdam authored centres were visible and render-ready while authoritative top-solid sampling still matched procedural terrain.
- Agent 7's shared spatial reservation system is integrated without moving geography/route ownership out of `TopDownWorldPhysicalPlanner`; an independent non-Kentridge fixture proves road/building conflicts are rejected.
- Spatial-reservation integration was exact-SHA green at source `7033ee755ae6bac7cc3c1cc3c83bb4ee2e7d5f5e` in run `33441865025`.

## Experiment 027 root cause and correction
Run `33448157883` proved the repeated sky/fog symptom originated in deterministic hard-route planning around `southern-ridge`, before structure publication. After the endpoint-gate/settlement-approach mismatch was isolated, source-backed geometry showed the Orc generic north gate overlapped the corridor-expanded ridge by 20dm. The durable correction stayed in scene composition: preserve the ridge dimensions and move its authored centre 30dm north; shared `GoAround` remains strict by default, with only the explicit Kentridge ridge-shoulder relationship using `EndpointEscape`.

Run `33461949335` then proved planner recovery: all 20 routes / 824 route tiles and all 16 generic buildings plan, and the standalone player reaches feature generation. Its remaining focused failure was test-only: the storage regression sampled the intentionally hollow centre of the four-wall generic shell. The regression now probes an authored perimeter wall without changing production geometry.

Exact source `b500683169ed0ea2f1e4997ce83560f78093f8e0` passed the focused storage test, repository-derived module validation, and standalone replay in run `33464366092` attempt 3 (artifact `9784884651`); attempts 1-2 were the already-proven runner free-memory-floor infrastructure failure. The 60-second artifact is behaviorally valid but not visual closure evidence: the existing driver reports seven targets in order (`moordell`, `rossdam`, `rossdam-lake`, `fairy-village`, `orc-village`, `southern-ridge`, `macro-network-overview`), and Rossdam only reaches readiness around t=59s.

The unchanged 180-second replay then passed all three pipeline stages on exact feature source `3e34713ceb3708846018f4cfeb02f6165a11e6d9` in run `33469175243` (artifact `9786132406`), and all seven target steps complete by roughly t=80s. That closes the timeout task, not visual acceptance: Fairy and Orc captures still do not show readable authored settlement shells; the current lake and ridge/pass frames are also not closure-quality, while the macro network frame clearly shows multiple contiguous routes across differentiated terrain.

Because the settlement visual symptom survived materially different geometry/framing fixes, production changes remain root-cause gated. Exact source `89c312802fb37ec79520937d15fce6db83aaeb13` passed the signed streaming discriminator, automatic module validation, and standalone replay in run `33474641146` (artifact `9788028184`). That fixture streams from positive-Z Moordell into negative-Z Fairy with production `ShowcaseWorld.StepStreaming` and proves Fairy's authored timber shell is retained at `int3(1780,233,-1321)` after one feature drain step. Therefore negative-coordinate residency, `FeatureRegionBuild`, authoritative mutation publication, and the Kentridge macro catalogue itself are not the missing-content boundary.

The remaining compact discriminator is the playable slice's second catalogue composition boundary. Feature head `cdc48695d5309abdf879a6fabadd731abc3aa2b7` adds only an independent PlayMode fixture that mirrors the real `KentridgePlayableSlice` composition (`KentridgeCombinedVoxelCatalogue` + Hightown + regional corridor), then streams Fairy and requires the same perimeter timber wall. Exact CI request commit `bb9d680afe5643fb2b744eddc1f4f80a27403e56` has `cdc48695...` as its immediate parent and is queued as run `33476499718` on the sole permitted `ci-test/fixes/agent-6` transport.

## Current blocker
The self-hosted macOS runner is temporarily unavailable: after the preceding validation completed, GitHub reported zero workflows in progress and five repository workflows queued, including agent-6 run `33476499718`. Do not replace, cancel, rebase, or duplicate that queued request. Continue only read-only acceptance analysis until it reaches a terminal state.

## Next validation
1. Monitor `33476499718` without replacing it. If the playable three-catalogue fixture fails, isolate/fix only the catalogue-composition owner. If it passes, the next built-player discriminator is validation-only: replace the known-invalid hollow-centre survey probe with a perimeter timber-wall/roof storage probe at the actual Fairy/Orc capture frame, without changing gameplay, camera, streaming, or generation.
2. Only after the actual-player storage boundary is resolved may scene-specific evidence framing change. Fairy/Orc must visibly show grounded shells; Rossdam lake must show substantial water plus its constrained route; Southern Ridge/pass must visibly establish the barrier/pass relationship. The macro-network overview is already visually useful but remains subject to final exact-SHA replay.
3. Extract final route/water/feature/streaming/render/FPS/memory telemetry and quantify vertical-residency blast radius without changing horizontal interest/device budgets. Existing 180-second reference metrics include 20 routes, 824 route tiles, 5 constrained routes, 16 generic buildings; steady-state FPS is >200 on the captured runner, but final-SHA measurements remain required.
4. Re-fetch current master immediately before the final exact-SHA gate. After every criterion is green, move the issue open->closed, set `status=fixed` / `resolvedUtc`, merge current master, and non-force promote the exact feature head.