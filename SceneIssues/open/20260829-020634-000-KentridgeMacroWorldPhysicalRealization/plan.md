# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. The source-backed Mounting Force/Kentridge macro graph must remain topology authority while WorldBuilder physically realizes every settlement, contiguous terrain-aware hard routes, reusable regional geography, a substantial bounded lake and ridge/pass with explicit route solutions, representative real CharacterMotor traversal, and world-scale streaming/rendering cost inside existing budgets. Closure additionally requires exact built-player visual evidence. `SceneIssues/feature-readme.md` is absent on current master, so `SceneIssues/README.md` is the available repository workflow authority.

## Established results
- Production regressions repeatedly produce 20 hard routes, 6 settlements, 16 generic buildings, 6 authored regions, 5 geography-constrained routes, and max road rise 2 voxels. Storage acceptance proves all generic building shells reach authoritative voxel storage.
- Two-stage terrain/feature publication is real. Broad residency readiness and serial building-centre pinning were rejected because they delay or churn normal streaming demand.
- Validation-only realtime dialogue dismissal reaches gameplay early enough for CharacterMotor evidence without changing normal gameplay.
- Rossdam's original carved lake cost was too high. The bounded first pass now deterministically resolves to exactly `900 x 450 dm`, `24 dm` depth, and `16,591,536` aggregate carve/fill primitive bounding cells, below the 17M regression ceiling.
- Exact player evidence proves local and generated-road CharacterMotor movement and progressively proves remote settlement publication. Earlier artifacts were deliberately rejected when framing or target completion was insufficient.
- Run `33293664047` is editor-infrastructure-red (native Burst child-domain crash with no NUnit XML), while its built player is clean but closure-incomplete. It proves the separate validation helper did not control target ordering/framing and exposes a queued-screenshot camera race. Experiment 013 records the rejection.

## Implemented final-candidate remediation
- Keep production generation budgets, load radius, CharacterMotor, renderer/device budgets, macro topology, hard-route semantics, and the repository-supported 60-second replay cap unchanged.
- `KentridgeMacroWorldEvidenceDriver` directly owns target order: `Moordell -> Rossdam lake -> Rossdam settlement -> Fairy -> Orc -> Southern Ridge/pass -> network`. Every target still waits for real content settlement plus renderer coverage; no synthetic completion or skipped gate exists.
- Generic settlement surveys use a near-overhead camera derived from actual settlement blockout bounds while the motor/streaming demand remains grounded at semantic focus.
- Moordell road-arrival mode remains active through the queued screenshot frame so the named road-arrival capture stays at player height.
- The ineffective competing helper was removed; one dormant validation driver now owns scheduling/framing.

## Master refresh / blast radius
Current `origin/master` `e95324aeaef619cb49d84bf2b07f770184bead81` is merged into `fixes/agent-6` by merge commit `0cea93d647e5860795dd4938e3b8d8ab5b1bbc6c`. Comparison is ahead-only (`behind=0`) and the diff contains only this assignment's Kentridge/WorldBuilder implementation, its focused PlayMode regressions, and this SceneIssue folder. No unrelated SceneIssue, feature-branch `.github/test-request.json`, CI workflow, device budget, streaming radius, or CharacterMotor change is present.

Known rejected-run resource observations remain bounded: run `33293664047` editor crash `elapsed=32s peak=3448MB systemFree=27274MB swapGrowth=0MB`; its built player remains generally >60 FPS after startup and often >100 FPS with zero harness assertions. Final telemetry must be measured from the closure candidate rather than inferred from rejected evidence.

## Remaining gate
Refresh the final documentation state, re-compare exact feature source to current master, confirm the existing `ci-test/fixes/agent-6` transport is idle, then advance only that transport with the supported 60-second exact request. The exact run must classify the focused result, complete every built-player target, visibly show four readable blockouts in each generic settlement, preserve player-height Moordell road arrival, show roads/network/lake/ridge/pass/constrained-route behavior and real CharacterMotor traversal, remain runtime-clean, and provide final build/route/streaming/frame/memory telemetry. Only after those checks are green may every acceptance/task checkbox be completed and the assignment move open -> pending -> closed.