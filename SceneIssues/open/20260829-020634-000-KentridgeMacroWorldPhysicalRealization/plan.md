# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. Preserve the source-backed Mounting Force/Kentridge graph while physically realizing settlements, terrain-aware hard routes, reusable geography, Rossdam Lake, Southern Ridge/pass, CharacterMotor traversal, readable built-player evidence, and bounded cost. `AGENTS.md`, `SceneIssues/README.md`, and `SceneIssues/feature-readme.md` govern this feature.

## Proven results
- Foundation: 20 hard routes, 6 settlements, 16 generic buildings, deterministic geography, bounded slopes, semantic constrained routes, lake/ridge realization, feature-aware vertical residency/readiness, and production evidence sequencing.
- Shared spatial reservations are integrated without moving geography/route ownership out of `TopDownWorldPhysicalPlanner`; an independent non-Kentridge fixture proves road/building conflicts are rejected. Exact source `7033ee755ae6bac7cc3c1cc3c83bb4ee2e7d5f5e` passed run `33441865025`.
- Experiment 027 isolated the repeated sky/fog symptom to an authored Southern Ridge/Orc arrival conflict, not feature generation. The scene-composition correction preserves shared strict route semantics. Exact source `b500683169ed0ea2f1e4997ce83560f78093f8e0` passed focused storage, module validation, and player replay in `33464366092` attempt 3.
- Exact source `3e34713ceb3708846018f4cfeb02f6165a11e6d9` passed the unchanged 180-second replay in `33469175243`; all seven targets complete by ~80s. Macro-network evidence is useful, but Fairy/Orc settlement, lake, and ridge/pass frames are not closure-quality.
- Exact source `89c312802fb37ec79520937d15fce6db83aaeb13` passed signed streaming in `33474641146`, proving negative-Z Fairy authored storage survives production streaming/publication.
- Exact source `cdc48695d5309abdf879a6fabadd731abc3aa2b7` passed the playable three-catalogue composition discriminator in `33476499718`, ruling out the final playable catalogue combine.

## Current discriminator
The remaining settlement-content hypothesis is the actual built-player capture boundary. The prior capture diagnostic sampled each intentionally hollow building centre, so its top-solid result could not distinguish missing authored shells from valid hollow geometry. Feature source `fb4bc6dad612f759987db5163d0d7b9e1b664405` changes validation only: at the real Moordell/Rossdam/Fairy/Orc survey frames it reads the same authored back timber-wall and roof voxels proven by production storage acceptance, using signed region addressing. Gameplay, camera, streaming, generation, and budgets are unchanged.

Exact CI request `b2226c2e824dcd51c0fd30a6421bf2b1df06c15c` has `fb4bc6da...` as its immediate parent and is queued as run `33480730488` on the sole permitted `ci-test/fixes/agent-6` transport. Do not replace it while queued/running.

## Next validation
1. If `33480730488` is product-red, fix only the demonstrated cause. If infrastructure-red, retry the proven infrastructure failure on the same transport after terminal state.
2. If Fairy/Orc report authored shell+roof storage at the capture frame, treat missing-content hypotheses as falsified and change only scene-specific evidence framing. Require readable grounded settlements, substantial lake + constrained route, and clear ridge/pass relationship.
3. Extract final route/water/feature/streaming/render/FPS/memory telemetry and quantify vertical-residency blast radius without changing horizontal interest/device budgets.
4. Merge current master before the final exact-SHA gate. Close only after every acceptance/checklist item is green, then move open->closed, set `status=fixed` / `resolvedUtc`, and non-force promote the exact feature head.