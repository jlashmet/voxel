# Plan

## Contract and ownership
Resume only `20260829-020634-000-KentridgeMacroWorldPhysicalRealization`. Keep the source-backed Mounting Force macro graph authoritative and deliver physical settlements, contiguous terrain-aware routes, reusable regional geography, Rossdam Lake, Southern Ridge/pass, real CharacterMotor traversal, durable built-player evidence, and bounded runtime cost. Affected ownership is WorldBuilder macro physical output, Showcase presentation readiness, and Kentridge Playable integration. Agent-6 has no Rendering production diff; do not weaken shared renderer acceptance or duplicate renderer ownership.

## Current exact evidence
Exact run `34192047180` exposed and source `2056c882a09e50faf612ef070e6a71ba789b9290` fixed the validation-only `EvidenceTarget` field/property reflection error. Exact run `34196421399` on source `b59fe1a7d20ab819bb9df56a4c7577492df3453d` proved the close-oblique survey and property fix active, but the full application still ended at Rossdam with a five-chunk `NoSlot` publication tail.

Current master `b56436f198702fa91bfaccec69a30df25a52a2cd` was then reconciled by clean two-parent merge `a5af427f99142e2a20a49e6ece1c47a5e566316c`, with no macro-world conflict and no agent-6 Rendering production change. The synchronized exact feature source is `0169a1eeeed943eec88667d23996420a11cea867`.

Exact synchronized request `7ce512c0383d758ea470c162ca3d1fdc89f87693`, run `34200648642`, materially advances acceptance. Persistent repository tests and the requested macro PlayMode regression are green. Both the focused module player and full SceneIssue replay are runtime-clean through real Application New Game, local CharacterMotor traversal, strict Moordell capture, macro-road traversal, road-arrival capture, and strict Rossdam capture. The previous Rossdam five-chunk tail is therefore no longer the first blocker.

The first incomplete target is now `rossdam-lake-detour`. Content settles, but strict production GPU publication does not converge. In the full application, from lake content-ready (~160.7s) to replay end, publication moves only `3691 -> 3693` while `missingVisible` is `387 -> 391`, `NoSlot` grows `128147 -> 131936`, and oldest GPU work ages to ~32.2s. In the focused player, publication moves only `3296 -> 3297` while `missingVisible` is `386 -> 391`, `NoSlot` grows `127817 -> 129153`, and oldest work ages to ~33.6s. The module scenario correctly fails because its required `MACROEVIDENCE capture-ready target=macro-network-overview` is never reached. Experiment 054 records the synchronized discriminator.

## Remaining prerequisite and gates
The lake target uses the same resolved world point for the renderer camera and CharacterMotor streaming authority, so this synchronized failure does not reproduce the old validation-demand mismatch. Current master has no newer production renderer correction. The demonstrated remaining prerequisite is shared production GPU publication liveness after a cold macro-view relocation under the existing budgets.

Keep this SceneIssue open. Do not raise budgets, widen residency, force generation, weaken `HasCompletePublishedNearSurfaceCoverage`, add a Kentridge-only renderer workaround, or start another SceneIssue from agent-6. When the shared renderer prerequisite lands on master, merge current master into `fixes/agent-6` and rerun exact-SHA validation. Require lake-detour capture, Fairy/Orc/ridge/network evidence, final `macro-network-overview`, all repository-derived module players, and the full 180-second SceneIssue replay without exceptions.

After those gates are green, inspect durable full-resolution captures; record final convergence/FPS/CPU/GPU/streaming/memory evidence against existing budgets; complete every checklist item; move only this issue `open -> closed` with fixed metadata; re-fetch/merge current master if it advanced; create a fresh `fixes/agent-6 -> master` PR; enable auto-merge; and monitor the required PR `affected` gate until merged. Do not reuse already-merged PR #311.
