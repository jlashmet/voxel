# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. The source-backed Mounting Force/Kentridge graph remains topology authority while shared WorldBuilder APIs must physically realize every settlement, contiguous terrain-aware hard routes, reusable geography, a substantial lake/ridge with explicit route responses, real CharacterMotor traversal, closure-quality built-player visuals, and measured streaming/render cost. `SceneIssues/README.md` is the repository workflow authority; this checkout does not contain `SceneIssues/feature-readme.md`.

## Material results
- Proven foundation: 20 hard routes, 6 settlements, 16 generic buildings, 6 regions, deterministic geography, bounded road slopes, authoritative storage, and two-stage presentation readiness.
- Earlier evidence proved the old Rossdam lake missed the direct Rossdam road. The accepted `900 x 450 x 24 dm` lake was moved south; its water surface was reduced to 2 dm while preserving the full carved basin.
- Run `33301222246` exposed `forest -> fighting-area-1` intersecting that moved lake. Adding `GoAround` produced exact-SHA run `33302489798`, which is product-red because no dry detour can start from an endpoint already inside the blocker.
- Authoritative grid coordinates proved endpoint engulfment, not a shared-planner defect. The selected `(-400,-155)` Rossdam nominal offsets keep `fighting-area-1` dry while preserving genuine outbound-north and Rossdam approach detours with the same accepted lake dimensions/depth.
- Exact feature `cb4e1b0fa1464da214d799ba55750bea38159143` was tested through wrapper `e5bdf88a387c486b72be5d8950fb6d1458a5aad9` in run `33304172039`. Exact attribution is proven by the wrapper parent. Focused PlayMode is `1/1` green and the 60 s standalone process is technically clean, but closure visuals are red: the driver remains at Moordell in the 39 s, 49 s and 59 s captures and never publishes Rossdam/lake/Fairy/Orc/ridge/network evidence.
- The evidence driver requires exactly the four authored generic-building centres for Moordell. Shared telemetry's fifth content column is not an extra driver requirement, so weakening the four-column content-settled gate would be incorrect.
- `FeatureRegionBuild` already slices a primitive into storage-block tiles, so the lake is not an atomic frame stall. The shared Showcase streaming queue nevertheless retains one active region build until completion. Once a lake-overlap region starts, it continues consuming the feature share after demand moves to Moordell.
- Geometry discriminates overlap from starvation: Moordell is centred near `(1970,3720)dm` with generic building centres at roughly +/-190 dm; the resolved lake ends near `x=1218dm`. The lake does not occupy a Moordell building column, but its regions are within the normal 3-region residency disc. Therefore a remote active lake build can delay an independent nearer Moordell building region.

## Selected remediation
Make the existing Showcase feature scheduler demand-responsive without changing generation semantics or budgets. Preserve resumable `FeatureRegionBuild` state when the camera moves, but allow a nearer unfinished feature region to preempt a farther active region; resume the paused build later. Region-local feature order remains unchanged, and work in separate regions is already independent by the generation contract. Readiness must continue to treat paused builds as unsettled, eviction/disposal must release paused build state, and no work may be cancelled/restarted merely to switch demand.

Add a focused behavioral regression that starts expensive resumable work in one region, introduces/targets a nearer pending feature region, and proves the nearer region can complete without discarding or restarting the remote build; then prove the remote build resumes to completion. The test must also cover cleanup/readiness for paused builds. Do not increase `FeatureTilesPerSlice`, `FeatureBudgetShare`, load radius, device budget, or replay duration.

## Evidence schedule and blast radius
Preserve the accepted target order exactly: `Moordell -> Rossdam -> Rossdam lake -> Fairy -> Orc -> Southern Ridge/pass -> network`. Keep streaming demand grounded at the current semantic target while survey camera framing may move independently. Run `33304172039` already shows assertion failures `0`, no swap growth, peak RSS `5,571,344 KB`, late FPS mostly well above 60, and converged visible coverage; final cost acceptance still requires a closure-quality run that advances through every target. Current master remains `e95324aeaef619cb49d84bf2b07f770184bead81`; do not request another exact-SHA CI until the demand-responsive feature scheduler and its regression are committed and the branch is re-compared with current master.