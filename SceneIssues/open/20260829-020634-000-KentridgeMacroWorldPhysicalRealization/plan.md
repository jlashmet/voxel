# Plan

## Observed behavior / acceptance
`captures` is empty, so the feature note is the repro contract. Closure requires the source-backed Kentridge macro graph to produce deterministic physical settlements, continuous terrain-aware hard routes, reusable regional constraints, a substantial lake/ridge with route response, real CharacterMotor traversal, closure-quality built-player evidence, and measured cost. `SceneIssues/feature-readme.md` is absent; canonical `SceneIssues/README.md` governs.

## Current hypotheses and results
1. **Macro generation/streaming is wholly missing.** Rejected: production acceptance has repeatedly produced 20 hard routes and 16 generic buildings; routes and geography render in the built player.
2. **Duplicate Rossdam water ownership caused the clipped water sheet.** Confirmed and fixed: standalone callers retain generic water, while Kentridge combined composition gives Rossdam water only to the carved-basin catalogue.
3. **One terrain sample buried generic buildings.** Confirmed and fixed in catalogue authoring: each padded footprint now uses a bounded 5x5 relief sample; foundation spans sampled relief and shell/roof are offset above the sampled high point.
4. **The new all-building exclusion regression would expose any remaining semantic overlap.** Confirmed. Exact request `a3f2d6d6652abac2dcf9061f9dda51b9e6ecb52b`, run `33279138597`, failed specifically because `orc-village building 3` overlaps `southern-ridge`. The nested production acceptance passed first: 20 routes, 16 buildings, 5 constrained routes, max rise 2 voxels.
5. **That failure was Rossdam or infrastructure.** Rejected. Full NUnit evidence proves a real Orc/ridge product conflict.
6. **The remaining visual problem is only camera framing.** Rejected again by full-resolution artifact `9722783657`: Fairy/Orc settlement cameras are close enough that a present 13x10 m near-side blockout would dominate the frame, yet no shells render.
7. **The Southern Ridge fix / final persisted storage is still the blocker.** Rejected. Exact request `c1a21b76cdc548436a32bd0866f26a2448a67286`, run `33283034449`, is green for source `0bbc9150f36281c0f951d9c75a60b318842fba46`; the production storage test reaches all expected persisted macro regions, route traversal metadata, and settlement shell material probes.
8. **Green storage therefore proves visual acceptance.** Rejected. Full-resolution artifact `9723674189` from the same exact-SHA run still shows no readable shells at Fairy Village or Orc Village, fewer than four obvious shells at Moordell, and no four-building settlement read at Rossdam.
9. **Persisted shell voxels are dropped by RegionLoader or the near-field mesher.** Rejected at the first production boundary. `RegionLoader` only manages logical residency, while the authoritative near-field cache consumes committed region data and invalidations. Production `ShowcaseWorld.FinishRegion()` publishes terrain first and only then queues feature realization; `CompleteFeatureBuild()` publishes the later feature commit/invalidation. A renderer-only readiness gate can therefore capture the terrain-only publication before settlement shells are committed/remeshed.
10. **Waiting for all current residency content fixes that race without material cost.** Rejected by exact-SHA run `33288421041` (request commit `4a00dc022631e62628f59a944c5410767dc9904d`, source `f13bd8cf0e9e2bfcc4dfda3077eda391e61aefa4`). The regression is green and proves the two-stage publication boundary, but full-radius `IsCurrentDemandContentSettled` delays opening until ~40 s and produces no custom macro target screenshots within the 60 s evidence replay. The focused regression recorded 18,908,465 feature voxel writes across five radius-1 demanded regions and a 1302.13 ms final blocking feature step. Player telemetry peaked at ~92.9% CPU / 5708.5 MB RSS with zero swap growth. This is a real cost/acceptance failure, not closure-quality green evidence.

## Selected remediation / next discriminator
- Keep the source-backed topology, existing four-shell settlement program, Southern Ridge extent fix, authored route solutions, normal streaming radius, and evidence replay duration unchanged.
- Preserve the two-stage publication contract, but scope readiness to **presentation-relevant region columns**, not the entire residency disk. A point/column is not stable while its generated surface/camera layers can still receive queued/in-flight/deferred feature publication.
- Replace broad `ShowcaseWorld.IsCurrentDemandContentSettled(...)` with a reusable bounded point/column readiness query that checks only the surface-layer span for the queried X/Z (plus the explicit point Y layer if distinct). It must not generate, widen residency, scan world history, traverse voxels/meshes, or allocate.
- Opening preload should wait only for the generated content columns relevant to the opening/pub presentation before accepting renderer coverage; it must not wait for unrelated radius-3 countryside.
- Evidence capture should wait for the content actually being shown: the road/motor column for traversal, all four building-centre columns for each generic settlement, and the semantic focus column for lake/ridge/network targets, followed by four consecutive complete renderer near-surface publications.
- Refactor the production regression to prove terrain-only publication leaves the queried settlement column unready, final feature publication makes that column ready, and the expected Moordell timber shell is committed in authoritative storage. Keep the nested macro physical/storage acceptance in the same targeted test.
- The next exact built `KentridgePlayableSlice` run is the renderer/mesh-visible discriminator. Closure requires custom full-resolution target captures showing four readable blockouts at every generic settlement plus roads/lake/ridge/pass and real CharacterMotor traversal.
- Do not add arbitrary delays, replay-time inflation, scene-local geometry, camera masks, eager remote generation, direct scene voxel writes, or streaming-radius expansion.

## Blast radius / cost
- `fixes/agent-6` was refreshed from `origin/master` `d4b31a700bae19b08ef765e874e26026620bde0e` with merge `2c6c501b4823fbb3b5a33ef30ac0e3ba119b1ab0` before run `33288421041`. Re-check current master before the next final CI/promotion.
- Remediation remains one reusable `ShowcaseWorld` readiness boundary plus two Kentridge consumers, one production regression, and assignment metadata. No `CharacterMotor`, streaming radius, unrelated SceneIssue, or CI transport logic changes.
- Broad failed gate cost is now measured, not hypothetical: radius-1 test demand produced 18,908,465 feature writes and 1302.13 ms last blocking feature work; player peak was ~92.9% CPU and ~5.57 GiB RSS. The next implementation must avoid requiring unrelated demand completion while preserving those existing generation semantics.
- Presentation-scoped readiness is expected to inspect at most the capped surface layers for one X/Z column per semantic check. Settlement evidence uses four building columns; no per-frame whole-world scan.
- Existing terrain-relief work remains bounded to 25 samples x 16 buildings = 400 deterministic catalogue-build queries.
- Re-measure final player CPU/GPU/frame/memory/streaming telemetry and compare feature/mesh/voxel counts to the existing four-shell program. The prior ~73 s / ~5.6 GB / zero-swap run is a comparison point, not acceptance proof.

## Remaining gate
Implement and regress presentation-scoped readiness, self-review the exact feature diff against current master, then issue only the designated exact-SHA target on `ci-test/fixes/agent-6` after confirming no request is queued/running. Reject closure unless focused behavior/storage/readiness validation is green **and** full-resolution built `KentridgePlayableSlice` evidence visibly shows four readable blockouts at Moordell, Rossdam, Fairy Village, and Orc Village, a clean substantial lake, readable ridge/pass response, connected roads without large holes, and real CharacterMotor traversal. Record final runtime cost. Only then complete pending metadata, move open -> pending -> closed, set `status=fixed`/`resolvedUtc`, merge current master, and non-force promote the exact feature head.
