# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. The source-backed Mounting Force/Kentridge graph remains topology authority while shared WorldBuilder APIs must physically realize every settlement, contiguous terrain-aware hard routes, reusable geography, a substantial lake/ridge with explicit route responses, real CharacterMotor traversal, closure-quality built-player visuals, and measured streaming/render cost. `SceneIssues/README.md` is the repository workflow authority; this checkout does not contain `SceneIssues/feature-readme.md`.

## Material results / hypotheses
- Proven foundation: 20 hard routes, 6 settlements, 16 generic buildings, deterministic geography, bounded road slopes, authoritative storage, substantial Rossdam Lake, Southern Ridge/pass, and two-stage presentation readiness.
- H1: the validation driver's former macro-road-first teleport caused unrelated nearby feature work to compete before Moordell's four real building columns settled. Supported by exact run `33304172039`: focused PlayMode `1/1` green and standalone clean, but evidence remained at Moordell through 59 s.
- H2: shared Showcase feature scheduling requires global preemption. Falsified: `FeatureRegionBuild` is already tile-resumable, and changing scheduler semantics would affect every Showcase scene for a validation-only sequencing defect.
- H3: the generic real-player smoke harness steals active Rossdam demand during the macro validation profile. Proven from exact run `33311348299`: Rossdam is stable at `visible=275`, `missingVisible=0`, `residentGround=116.6m`, `coverage=True` at t=49; the harness writes `AutoSurvey` at t=50, residency immediately evicts away from Rossdam and falls to `visible=0`, `residentGround=14.2m`, `coverage=False`, then spends several seconds reloading. This is a validation-automation ownership conflict, not evidence that production feature scheduling needs broader preemption.
- H4: the named Moordell survey capture is visually valid once its readiness gate opens. Falsified by the full-resolution artifact from run `33311348299`: `macro-moordell.png` is a player-height/horizon frame with only tiny distant blockouts. `ScreenCapture.CaptureScreenshot` queues the frame, but Moordell's special continuation switches `LateUpdate` to macro-road behavior immediately after capture-ready, so the survey camera is not held for the queued screenshot frame.

## Selected fix / regression
Keep the accepted dormant `kentridge-macro-world` sequence: local CharacterMotor proof -> fully settled/captured Moordell -> real macro-road CharacterMotor traversal -> player-height Moordell road arrival -> Rossdam -> lake -> Fairy -> Orc -> ridge/pass -> network. All real content/renderer gates, normal load radius/budgets, and the 60 s cap remain unchanged.

The smallest correction is validation-profile-only: reassert `AutoSurvey=false` and `AutoRecede=false` in the macro driver's `LateUpdate`, after ordinary `Update` writers have run, so a later generic harness write cannot make `KentridgePlayableSlice` move the CharacterMotor away from an active macro target on the next frame. Also keep applying the Moordell survey camera during the already-existing `TargetPostCaptureSeconds` dwell, so its queued screenshot renders the intended readable settlement survey before the macro-road continuation takes over. Add throttled diagnostics over the existing target content columns so any still-pending Rossdam building reports its exact centre and X/Z presentation region. Do not change `ShowcaseWorld` feature scheduling unless the corrected exact run proves a real product convergence defect remains.

`KentridgeMacroWorldEvidenceSequenceTests` remains the focused behavioral surface. Extend it to prove validation ownership suppresses competing survey/recede flags, the post-capture Moordell state remains survey-held until its existing dwell expires, and the accepted Moordell -> macro-road -> road-arrival -> later-target order is unchanged. The diagnostic path must stay bounded to the existing four settlement content columns and must not generate, expand residency, or scan world history.

## Latest exact-CI diagnosis
Exact source `3993431c740b6a2da16b1270e1ede22831e50955` through wrapper `b8089e2ca66a6942797869c41c3c7939682f5712` produced green workflow run `33311348299`, but artifact inspection is closure-red. Moordell and road captures exist; Rossdam reaches stable renderer coverage but never logs aggregate content-ready before the 60 s cutoff. The same artifact identifies two deterministic validation-driver defects: the smoke-harness `AutoSurvey` handoff at t=50 disrupts Rossdam residency, and the queued Moordell survey screenshot loses its survey camera during the immediate macro-road continuation.

The corrected run must remove both disruptions and name any remaining unsettled Rossdam content column directly. If all four columns settle once ownership is retained, no production scheduler change is justified. If one still stalls under uninterrupted normal 3 ms real-player streaming, use the per-column diagnostic to make the smallest source-level publication fix without weakening readiness, increasing residency, bypassing normal budgets, or skipping targets.

## Remaining gates / cost
Implement the validation-only ownership guard, survey-camera dwell, and bounded diagnostics; extend the focused regression; and record the experiment result. Re-inspect full-resolution Moordell/Rossdam settlement framing for readable multi-building geometry rather than telemetry-only readiness. Measure final lake/route/feature/CPU/FPS/memory/streaming/render telemetry against the existing budgets.

Freeze the final feature SHA only after task/experiment metadata is current, re-fetch and merge current master if needed, then use only `ci-test/fixes/agent-6` for the final exact-SHA PlayMode + real-player request. Inspect NUnit XML, every full-resolution capture, standalone log, route/lake/feature telemetry, CPU/FPS, memory, streaming convergence, and render/far-field coverage. Promote only if every required macro target is captured inside the supported 60 s replay, settlement geometry is visibly readable, runtime assertions/exceptions are absent, cost remains bounded, and every acceptance/task checkbox is satisfied.
