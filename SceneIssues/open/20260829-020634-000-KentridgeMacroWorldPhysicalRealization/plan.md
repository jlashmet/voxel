# Plan

## Acceptance authority
`issue.json` is the contract (`captures: []`). Preserve the source-backed Mounting Force graph while delivering physical settlements, terrain-aware hard routes, reusable geography, Rossdam Lake, Southern Ridge/pass, CharacterMotor traversal, durable built-player evidence, and bounded cost. Follow `AGENTS.md`, `SceneIssues/README.md`, and `feature-readme.md`.

## Proven results
- Production physical planning covers 20 hard routes, 6 settlements, 16 generic buildings, deterministic geography, constrained-route solutions, lake/ridge realization, bounded slopes, storage, and feature-aware vertical residency/readiness.
- Shared spatial reservations retain planner ownership; independent non-Kentridge conflict fixture passed (`33441865025`).
- Exact source `b500683...` passed focused storage/module/player validation (`33464366092` attempt 3).
- Exact source `3e34713...` completed all seven 180-second evidence targets (`33469175243`), but visual settlement/geography quality was insufficient.
- Signed negative-Z storage streaming passed (`33474641146`); playable three-catalogue combine retained Fairy timber (`33476499718`).
- Exact source `fb4bc6d...` passed focused storage + 180-second player (`33480730488`), yet corrected authored wall/roof probes read `material=0` at every Moordell/Rossdam/Fairy/Orc capture. Full-resolution Fairy/Orc frames contain no four-building blockout. Camera/readiness/final-combine hypotheses are falsified.
- Shared selector contract run `33485694443` is green, confirming the seed catalogue requires and consumes the explicit semantic handoff; it is supporting evidence only, not the playable root-cause proof.
- Corrected real-caller discriminator run `33495686226` is green for the requested PlayMode test and repository-derived module validation on exact source `1e7bb5f8b704023b661132facd46f9c1c0835476`. This falsifies the playable compatibility Kentridge/Hightown authoring handoff as the missing macro owner.

## Current root-cause isolation
The same built-player symptom has survived multiple materially different remediation attempts, so no further production geometry/streaming fix is justified until a minimal runtime discriminator selects the failing boundary.

Run `33495686226` is overall red only because `.github/test-request.json` supplied the bare SceneIssue id. The focused real-caller discriminator passed, automatic module validation passed, and the player process was clean, but the malformed `scene_issue` value prevented the standalone SceneIssue replay/artifact from exercising the existing runtime trace. This is CI-request configuration, not product evidence.

Shared `FeatureGenerationTrace` already instruments the required boundary under the SceneIssue diagnostic flag: structure `candidate`, evaluation `accepted`/`rejected`, and `completed` with rasterization/voxel-write counts. Do not add duplicate instrumentation before collecting that existing signal from the real `KentridgePlayableSlice` runtime.

## Next gates
1. On the sole `ci-test/fixes/agent-6` transport, request the production-storage focused gate plus a 60-second SceneIssue replay using the repository-relative path `SceneIssues/open/20260829-020634-000-KentridgeMacroWorldPhysicalRealization/issue.json`; preserve the current feature source and existing trace instrumentation.
2. Classify Fairy/Orc runtime loss from `FEATUREGEN_TRACE`: no candidate => catalogue/region scheduling; rejected => evaluator/footprint; accepted+completed with zero writes => rasterization/mutation; completed with writes but final authoritative storage empty => later publication/overwrite/store replacement.
3. Only after that discriminator identifies the boundary, make the smallest demonstrated product fix and require exact built-player stored shells plus readable Moordell/Rossdam/Fairy/Orc settlements, lake/constrained route, ridge/pass, network, and CharacterMotor traversal.
4. Measure final route/water/feature/streaming/render/FPS/memory and vertical-residency blast radius.
5. Merge current master before final exact-SHA gates; close and non-force promote only after every checklist/acceptance item is green.
