# Plan

## Observed gap / acceptance
`captures` is empty, so the feature note is the repro contract. Closure requires the authoritative Kentridge macro graph to drive deterministic physical settlements, continuous terrain-aware hard routes, reusable geographic constraints, substantial lake/ridge geography, real CharacterMotor traversal, closure-quality built-player evidence, and measured cost. `SceneIssues/feature-readme.md` is absent; canonical `SceneIssues/README.md` governs. The `docs/repo-map.md` and `docs/CODE_STANDARDS.md` paths referenced by `AGENTS.md` are also absent from this revision, so no rules are inferred from missing files.

## Material results / hypotheses
1. **Scale up legacy markers/roads.** Rejected; it cannot satisfy reusable geography, settlement realization, or blocked-route semantics. The reusable physical-plan layer is implemented.
2. **Weaken geography to clear blocked routes.** Rejected. Rossdam/Bandit and Southern Ridge/Orc conflicts use explicit semantic route solutions with focused regressions.
3. **A green workflow/file count proves visual acceptance.** Rejected repeatedly. Runs `33259572439` through `33263409994` exposed incomplete evidence, harmful prewarm, occlusion, missing scheduling, and false-positive near-surface coverage.
4. **Remote blocking prewarm fixes evidence.** Rejected by run `33260866388`; same-camera prewarm harmed presentation and was removed.
5. **The planner failed to create generic settlements.** Rejected at plan level: each generic settlement receives deterministic >=4-building blockouts.
6. **Four stable `HasCompletePublishedNearSurfaceCoverage()` frames plus closer cameras are sufficient.** Rejected by run `33265086481` / artifact `9718439671`: focused PlayMode and built-player harness are green and all eight captures emit, but full-resolution images still do not prove four readable generic buildings or the intended lake/ridge geography.
7. **The production catalogue only contains settlement metadata/foundations.** Rejected by source trace. `TopDownWorldPhysicalVoxelCatalogue` emits grounded foundations, filled timber volumes and gable roof primitives for each generic building, and `KentridgeCombinedVoxelCatalogue` contains that catalogue.
8. **Camera-centered evidence streaming leaves the photographed focus nonresident.** Rejected after inspecting current production settings. `KentridgePlayableSlice` uses a radius of three 51.2 m regions (about 153.6 m), while every current camera-to-focus offset is under ~54 m. The target focus is therefore inside normal residency; no streaming-radius, motor, or residency change is justified.
9. **Renderer coverage proves the intended semantic target exists in final voxel storage.** Rejected as a sufficient discriminator. Exact merged candidate `ed72e82210a0135472e6d20b823f4be38f286d73` passed the new production-storage acceptance in run `33276625513`, proving sampled timber and gable-roof material reach final rasterized storage, while the unchanged built-player captures still fail visual acceptance. Generation exists at the sampled point, but the composed-world grounding/presentation remains wrong.
10. **The remaining defect is camera-only.** Rejected by full-resolution artifact `9721728843` plus source trace. Generic buildings use one centre terrain-height sample for the whole footprint, so terrain relief can bury most of a shell even though one sampled wall/roof voxel exists. The storage regression currently checks only one building and cannot prove four readable buildings at every generic settlement.
11. **The Rossdam visual is only a transparent-water rendering quirk.** Rejected. `TopDownWorldPhysicalVoxelCatalogue` authors a full rectangular WaterBody region while `TopDownWorldWaterBodyVoxelCatalogue` separately owns a bounded carve/fill basin for the same plan region. `SettlementCatalogueCombiner` only concatenates/rebases catalogues and preserves both independent feature sets; it performs no overlap/deduplication. The large flat/clipped Rossdam water sheet is therefore backed by duplicate production surface ownership and must be fixed in composition rather than hidden by evidence cameras.
12. **Suppress generic water globally.** Rejected because the reusable physical catalogue has a standalone contract. The chosen fix is a backward-compatible build policy/default that preserves generic WaterBody output for ordinary callers and suppresses it only when `KentridgeCombinedVoxelCatalogue` also composes `TopDownWorldWaterBodyVoxelCatalogue` as the dedicated owner.
13. **Raise a building to the highest terrain sample only.** Rejected because it can leave the low side floating. The chosen grounding model samples bounded terrain relief across the padded building footprint, places the feature origin at the sampled low point, extends the foundation through the sampled relief plus its normal 8 dm cap, and offsets timber/roof upward by the same relief. That preserves authored X/Z geometry while making the foundation absorb slope and keeping shell/roof above the sampled high point.

Resumed 2026-08-29 from `fixes/agent-6`. The first post-refresh final candidate merged current master at `ed72e82210a0135472e6d20b823f4be38f286d73`; CI run `33276625513` was green but is explicitly rejected as a closure gate because its full-resolution built-player evidence does not satisfy the feature note.

## Current remediation discriminator
1. **Verified:** production catalogue composition preserves both generic and dedicated water features; there is no combiner-level overlap resolution. Fix water ownership at the Kentridge composition call while keeping standalone behavior unchanged.
2. Give production Rossdam water a single owner: preserve the reusable standalone physical catalogue contract, but prevent the combined Kentridge production catalogue from also painting the full WaterBody rectangle when the dedicated carved-basin catalogue is present. Add a focused regression for the production composition.
3. Ground generic building programs against bounded sampled terrain relief across each padded footprint: foundation begins at sampled minimum and spans to normal foundation cap above sampled maximum; wall/roof start heights shift by the same relief. Keep authored X/Z building plan, deterministic material/shape choices, and bounded catalogue-build work.
4. Strengthen final-storage acceptance across every generic building in Moordell, Rossdam, Fairy Village, and Orc Village instead of sampling one building. Assert final above-ground timber/roof occupancy at deterministic probes and explicitly verify the placement/program grounding contract against sampled footprint relief.
5. Check every generic building footprint against WaterBody/separative-region exclusions. If Rossdam physical intent itself overlaps its settlement, repair the source semantic placement/region intent with a focused regression rather than masking it visually.
6. Re-inspect existing visual targets after physical composition changes. Adjust validation-only lake/ridge/network framing only where correct stored production geometry remains ambiguous.
7. Self-review blast radius/cost and re-read available repo workflow instructions. Only then issue another final exact-SHA request on the same designated `ci-test/fixes/agent-6` transport; no additional CI transport or CI-driven iteration.

## Blast radius / cost
- Macro realization remains opt-in through the existing one-shot `TopDownWorldLayoutSelection`; ordinary catalogue callers must retain prior behavior.
- Water ownership changes only in the Kentridge combined composition through a backward-compatible physical-catalogue policy defaulting to current standalone behavior; unrelated WorldBuilder callers retain WaterBody output.
- Terrain-relief sampling is catalogue-build work only. Use a fixed bounded footprint lattice including edges/corners/centre; no runtime GameObjects, streaming-radius change, renderer-budget change, CharacterMotor change, or resident-region semantic change.
- Foundation relief adds height only equal to sampled local terrain delta and leaves X/Z feature footprint unchanged, so streaming breadth and definition/placement counts do not increase.
- Strengthened storage regression remains test-only and rasterizes only regions needed for deterministic per-building probes, reusing/discarding bounded temporary storage rather than generating the whole macro world at once.
- Run `33276625513` cost baseline: player elapsed 73 s, peak RSS about 5557 MB, zero swap growth; steady rendered gameplay/evidence samples are generally within existing frame-time budgets after startup/validation transitions. Final numbers must be re-measured after remediation.
- Re-check branch diff, exact test telemetry, built-player CPU/GPU/frame/memory/streaming telemetry, and every screenshot at full resolution before closure.

## Remaining gate
Complete the production-composition/grounding remediation and strengthen the regression without touching other assignments or `.github/test-request.json` on the feature branch. Refresh current master immediately before the next final CI candidate, self-review the exact branch diff, then reuse only `ci-test/fixes/agent-6` for the exact-SHA storage/behavioral target plus built `KentridgePlayableSlice` evidence harness. Full-resolution evidence must visibly show four readable blockouts in each generic settlement, continuous road/motor traversal, a substantial clean Rossdam basin/shoreline with constrained route, readable ridge/pass response, and a connected route view without large holes. Only after every task/acceptance checkbox is proven should metadata be completed, the assignment move `open -> pending -> closed`, `status=fixed`/`resolvedUtc` be set, current `origin/master` be merged, and the exact feature head be non-force promoted.
