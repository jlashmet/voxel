# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. Keep the source-backed Mounting Force/Kentridge graph authoritative through shared WorldBuilder APIs; physically realize settlements, terrain-aware hard routes, reusable geography, Rossdam Lake, Southern Ridge/pass, real CharacterMotor traversal, readable built-player evidence, and bounded cost. `SceneIssues/README.md` is the workflow authority; `SceneIssues/feature-readme.md` is absent.

## Proven results
- Foundation: 20 hard routes, 6 settlements, 16 generic buildings, deterministic geography, bounded road slopes, authoritative storage, substantial lake/ridge, explicit constrained routes, and two-stage presentation readiness.
- Shared `FeatureRegionBuild` is tile-resumable; global scheduler preemption is rejected as excessive blast radius.
- Exact run `33311348299` was workflow/PlayMode green but closure-red. Rossdam had stable coverage at t=49; at t=50 the generic smoke harness set `AutoSurvey`, moving demand away and collapsing Rossdam residency (`visible=275 -> 0`, `residentGround=116.6m -> 14.2m`). This is validation-automation ownership, not proven production publication failure.
- The same artifact falsified Moordell visual acceptance: queued `macro-moordell.png` inherited the following macro-road camera and shows only tiny distant geometry.

## Selected fix / regression
Dormant `kentridge-macro-world` validation only:
1. Reassert `AutoSurvey=false` and `AutoRecede=false` in macro-driver `LateUpdate`, after ordinary `Update` writers.
2. Hold the Moordell survey camera through existing `TargetPostCaptureSeconds` so the queued screenshot renders the intended survey before macro-road continuation.
3. Log still-pending target content columns at most once/second with exact centre and X/Z presentation region; no new generation or world scan.

`KentridgeMacroWorldEvidenceSequenceTests` now proves accepted target order, macro-driver automation ownership, and post-capture survey hold. Production streaming scheduler, load radius, CharacterMotor, generation/render budgets, and 60 s cap are unchanged.

## Remaining discriminator / gates
Freeze the final source after this metadata/experiment update. Current master is already merged (`behind_by=0` before freeze); re-check immediately before CI. Use only `ci-test/fixes/agent-6` for the final PlayMode request with the assigned scene issue and 60 s replay.

Inspect exact NUnit result, player log, every full-resolution capture, and runtime telemetry. If Rossdam still stalls under uninterrupted macro demand, use the new per-column diagnostic to identify the exact production blocker before changing shared scheduling. Promote only if all required macro targets capture inside 60 s, four-building settlement surveys are visibly readable/grounded, roads/lake/ridge/constrained geography are visible, CharacterMotor traversal remains real, runtime exceptions/assertions are absent, and CPU/FPS/memory/streaming/render cost stays within existing budgets.
