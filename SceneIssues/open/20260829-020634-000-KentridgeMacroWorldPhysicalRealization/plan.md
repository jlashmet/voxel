# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. The source-backed Mounting Force/Kentridge graph remains topology authority while shared WorldBuilder APIs must physically realize every settlement, contiguous terrain-aware hard routes, reusable geography, a substantial lake/ridge with explicit route responses, real CharacterMotor traversal, closure-quality built-player visuals, and measured streaming/render cost. `SceneIssues/README.md` is the current repository workflow authority.

## Material results / hypotheses
- Proven foundation: 20 hard routes, 6 settlements, 16 generic buildings, 6 regions, deterministic geography, bounded road slopes, authoritative storage, and two-stage presentation readiness.
- Rejected evidence strategies: broad residency, serial building-centre pinning, an external camera helper, and replay >60 s. Normal production streaming demand must remain authoritative.
- Run `33293664047` is editor-infrastructure-red (native Burst crash), but its built player is independently closure-incomplete.
- Run `33294931953` is product-red. NUnit reaches the requested test and reports `road-kentridge-rossdam` is no longer geography-constrained; its player proves lake-first ordering is slower and ends after Rossdam at the 60 s cap.
- Exact geometry explains the route regression: resolved Rossdam Lake is `900 x 450 x 24 dm` at `(868,3325)dm`; the direct Rossdam route passes closest near `(860,2920)dm`, leaving ~180 dm between the route and the lake edge. Hypothesis A: a semantic southward Z offset restores a real obstruction without changing accepted size/depth.
- Current water program scans 16,591,536 primitive bounding cells because the non-solid water fill spans the full carved depth. Hypothesis B: preserve the 24 dm carve but fill only a 2 dm surface sheet, reducing real feature work to ~9.28M cells without reducing lake footprint or carved depth.

## Selected remediation
- Keep deterministic `900 x 450 x 24 dm` lake dimensions; move the Between-region south by 210 dm so the direct Moordell-corridor→Rossdam-approach route genuinely intersects it while the Rossdam-approach endpoint stays dry and still requires explicit `GoAround` solving.
- Keep the full-depth rounded basin carve. Move water fill to the basin top with 2 dm presentation thickness and tighten the scan regression from 17M to 10M cells.
- Restore the previously faster evidence sequence `Moordell -> Rossdam -> lake -> Fairy -> Orc -> Southern Ridge/pass -> network`; keep real content-settled + renderer-coverage gates, grounded streaming demand, near-overhead settlement framing, player-height road arrival, and the 60 s cap.

## Blast radius / remaining gate
Current master `e95324aeaef619cb49d84bf2b07f770184bead81` is already merged at `0cea93d647e5860795dd4938e3b8d8ab5b1bbc6c`; current diff is assignment-scoped. Production changes are limited to shared water-body realization plus Kentridge macro intent; no load radius, device budget, CharacterMotor, topology, or unrelated SceneIssue changes. After implementation, re-run the exact focused regression and built player, inspect every full-resolution target, measure route/water/feature/FPS/memory telemetry, refresh master again if it advances, and promote only when every task/acceptance checkbox is proven.