# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. The source-backed Mounting Force/Kentridge graph remains topology authority while shared WorldBuilder APIs must physically realize every settlement, contiguous terrain-aware hard routes, reusable geography, a substantial lake/ridge with explicit route responses, real CharacterMotor traversal, closure-quality built-player visuals, and measured streaming/render cost. `SceneIssues/README.md` is the repository workflow authority; this checkout does not contain `SceneIssues/feature-readme.md`.

## Material results
- Proven foundation: 20 hard routes, 6 settlements, 16 generic buildings, 6 regions, deterministic geography, bounded road slopes, authoritative storage, and two-stage presentation readiness.
- Rossdam Lake retains accepted `900 x 450 x 24 dm` dimensions, full carved basin, 2 dm water sheet, deterministic variation, dry route endpoints, and genuine geography-constrained routes.
- Exact source `cb4e1b0fa1464da214d799ba55750bea38159143` through wrapper `e5bdf88a387c486b72be5d8950fb6d1458a5aad9`, run `33304172039`, is focused PlayMode `1/1` green and standalone-runtime clean, but closure-red: the evidence remains at Moordell through 59 s and never reaches later targets.
- The driver waits on exactly four authored Moordell building centres. The fifth shared telemetry column is not an acceptance requirement, so readiness must not be weakened.
- `FeatureRegionBuild` is already resumable at block-tile granularity. The lake does not share a Moordell building column, but its regions are inside the normal residency disc and can consume feature work after the validation driver first exercises the macro road.

## Selected remediation after blast-radius review
Do **not** change the shared Showcase feature scheduler. A global paused/preemptive build queue would alter every Showcase scene to address a failure created by this validation driver's deliberate long-distance evidence sequencing. Keep production streaming semantics, readiness, load radius, feature budgets, water geometry, and rasterizer unchanged.

Instead, change only the dormant `kentridge-macro-world` evidence driver so Moordell is the first remote streaming demand after the local CharacterMotor proof. Capture Moordell only after all four real building columns and renderer coverage settle; then exercise/capture the required real macro-road CharacterMotor traversal and player-height Moordell road arrival while that nearby content is already ready; then continue the accepted macro target order `Rossdam -> Rossdam lake -> Fairy -> Orc -> Southern Ridge/pass -> network`.

This does not skip or pre-generate any target and does not add residency. It merely avoids making a validation-only road teleport start unrelated nearby lake feature work before the first settlement gate. The existing 60 s cap and all real content/renderer gates remain authoritative.

## Validation / cost
Maintain a focused behavioral test of the evidence sequencing so Moordell precedes macro-road capture and later target order remains unchanged. Then freeze the source SHA, compare with current master, use only `ci-test/fixes/agent-6` for one exact-SHA request, and inspect NUnit XML, every full-resolution capture, standalone log, streaming/render telemetry, feature cost, CPU/FPS, and memory. Run `33304172039` baseline: assertion failures 0, no swap growth, peak RSS `5,571,344 KB`, late FPS mostly >100, final visible coverage true. Do not promote until every visual and cost criterion is closure-green.