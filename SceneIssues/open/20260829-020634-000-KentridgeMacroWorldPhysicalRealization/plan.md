# Plan

## Acceptance authority
`captures` is empty, so `issue.json` is the acceptance contract. Keep the source-backed Mounting Force/Kentridge graph authoritative through shared WorldBuilder APIs; physically realize settlements, terrain-aware hard routes, reusable geography, Rossdam Lake, Southern Ridge/pass, real CharacterMotor traversal, readable built-player evidence, and bounded cost. `AGENTS.md`, `SceneIssues/README.md`, and `SceneIssues/feature-readme.md` govern this feature.

## Proven results
- Foundation: 20 hard routes, 6 settlements, 16 generic buildings, deterministic geography, bounded road slopes, authoritative storage, substantial lake/ridge, explicit constrained routes, and two-stage presentation readiness.
- `RefreshPending` is isolated in a residency partial, and already-accepted X/Z columns additionally queue only feature-bearing Y-regions from semantic catalogue bounds. No horizontal interest radius, device budget, scheduler, or scene-specific preload policy changes.
- Reuse audit is complete: shared physical intent/planner/physical catalogue/water catalogue contain semantic inputs and no Kentridge/Rossdam policy. Existing generic `a`/`b` blocked-water fixture independently proves planner rejection and semantic `GoAround` reuse without Kentridge adapters.
- Exact run `33346099006` proves ordinary production `ShowcaseWorld.StepStreaming` makes a real authored upper feature Y-region resident without moving the viewer into that layer.
- Experiment 018 found presentation readiness could become true before authored upper layers were feature-published. The shared readiness predicate now uses the same cached semantic feature-layer query as residency; exact source `9d51fb9a947af76d0b8005c35288a7007dd6d9e6`, run `33354287850`, proves the lower-ready/upper-missing regression green.
- Experiment 019 exact run `33359142139` proves every Rossdam semantic building maps to a production structure placement with roughly 1.03–1.20M authoritative structure voxels, grounded at production minimum Y with full authored wall-height occupancy. Generation/storage/grounding are rejected as the surviving visual owner.
- Experiment 020 readiness-aligned exact run `33370392969` proves all four Rossdam authored bounds intersect the real survey frustum and each intersects four ready visible production solid chunks after all target columns settle; generation, grounding, readiness, target ownership, frustum intersection, and coarse publication are rejected.
- Exact run `33372188117` is workflow-green but closure-red: vertically lowering the validation survey camera to 45 m visibly crops/mis-aims the settlement.
- Exact run `33373258471` is product-red in the focused focus-ray-dolly regression (`0.027976°` versus `<0.01°`) while its built-player step succeeds. The second materially different camera correction leaves the same cropped-settlement symptom, so experiment 021 isolated a minimal framing cause before another correction.
- Experiment 021 showed the 58° production lens cannot contain the generic flat settlement footprint on the diagonal survey axis; this justified a validation-only 72° lens while preserving the established 70 m semantic camera/focus pose and production streaming/LOD policy.
- Exact run `33376804313` is workflow-green but visual-closure-red. Its 72° scalar footprint regression passes, yet the full-resolution Moordell frame still clips the fourth blockout and Rossdam shows only one complete building plus a clipped structure. The scalar flat-envelope model is therefore falsified as a sufficient discriminator.
- Experiment 022 now models the exact built evidence geometry instead of another visual-only camera tweak: it rebuilds the same deterministic physical plan, reconstructs the 70 m survey pose at the required 1600x900 aspect, includes each blockout's foundation inset, sampled terrain relief, wall height, and roof height, and projects every authored 3D envelope corner into viewport space with a 4% containment margin. The validation-only settlement lens is widened to 90°; normal cameras are not narrowed and the normal lens is restored outside settlement surveys.

## Current gate
Run the focused projected-authored-bounds containment regression on the exact current feature SHA through the sole `ci-test/fixes/agent-6` transport. The prior request `33376804313` is completed, so replacing it is permitted. If the focused projection regression fails, use its reported viewport coordinate as the next discriminator rather than making another camera correction. If focused green, inspect full-resolution Moordell/Rossdam evidence before continuing.

## Remaining gates
1. Prove all four readable authored blockouts at Moordell, Rossdam, Fairy Village, and Orc Village with streets/open space and road arrival/exit in full-resolution built-player evidence.
2. Preserve player-height road and real CharacterMotor evidence; capture substantial Rossdam water plus constrained route, Southern Ridge/pass, differentiated countryside, and final macro-network overview.
3. The supported replay remains fixed at 60 s. Run `33376804313` captures Moordell around t40 and Rossdam around t56, then starts the lake target at t57; if framing closes but later evidence still misses the cap, quantify target-phase cost and make only an acceptance-required validation-sequence/performance correction. Do not weaken readiness, pre-generate targets, widen residency, or extend replay duration.
4. Quantify added feature Y-region residency and final world-build/route/feature/CPU/FPS/memory/streaming/render/far-field cost against budgets.
5. Re-fetch current master and re-check exact feature diff immediately before final targeted CI.
6. Only after every checkbox is green: move `open -> closed`, set `status=fixed` and `resolvedUtc`, merge current master, and non-force push that exact feature head to master; if master advances, fetch/merge/retry.
