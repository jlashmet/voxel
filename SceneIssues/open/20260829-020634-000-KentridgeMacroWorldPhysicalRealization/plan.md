# Plan

## Contract and ownership
Resume only `20260829-020634-000-KentridgeMacroWorldPhysicalRealization`. Keep the source-backed Mounting Force macro graph authoritative and deliver physical settlements, contiguous terrain-aware routes, reusable regional geography, Rossdam Lake, Southern Ridge/pass, real CharacterMotor traversal, durable built-player evidence, and bounded runtime cost. Affected ownership is WorldBuilder macro physical output, Showcase presentation readiness, and Kentridge Playable integration. Agent-6 has no Rendering production diff; do not weaken shared renderer acceptance or duplicate renderer ownership.

## Current exact evidence
Prior exact source `abff52bba0c04c3940d98d6c24ebab2b081258ec`, transport `522fe183329e6ad645664e4bf1f88515b51ae5dc`, run `34213075572`, confirms the module-validation compile-boundary correction worked. Repository module tests and the requested macro-world regression complete before player validation. The real `KentridgeMacroWorldValidation` player then runs 180 seconds and fails only because required `MACROEVIDENCE capture-ready target=macro-network-overview` is never reached. It advances through real Application New Game, CharacterMotor traversal, Moordell, Rossdam, and `rossdam-lake-detour`; the separate 180-second SceneIssue replay also builds/runs to completion. This is a product/acceptance failure, not infrastructure.

Current master `5c9a715a42735a0b573f436af1e3700dff2f591c` adds only the unrelated Mounting Force town-art-direction closure. It contains no production Rendering correction for the demonstrated cold-view GPU publication-liveness prerequisite. Master was cleanly reconciled through two-parent merge `0be3434b5111d9284c2b02a2586675e82ce35b66`; experiment 056 records the result.

## Remaining blocker and gates
All four required focused scene/scenario pairs now exist: WorldBuilder macro physical output, Showcase feature residency, Kentridge macro world, and Rendering GPU mirror relocation. Automatic validation reaches the Kentridge pair first and stops on its strict network-overview assertion, so downstream pair execution remains unproven.

Do not blind-retry the same product failure. Keep this SceneIssue open until a relevant shared product correction is present on current master/this feature branch. Do not raise budgets, widen residency, force generation, weaken `HasCompletePublishedNearSurfaceCoverage`, add a Kentridge-only renderer workaround, or start another SceneIssue.

After a relevant correction lands, merge current master if needed and run exact-SHA CI on the synchronized feature head. Require every repository-derived test/player plus the full 180-second replay to pass, including lake-detour, Fairy/Orc/ridge/network evidence, final `macro-network-overview`, and no runtime exceptions. Then inspect full-resolution captures as `production-quality`, record convergence/FPS/CPU/GPU/streaming/memory against existing budgets, complete every checklist item, move only this issue `open -> closed` with fixed metadata, open a fresh `fixes/agent-6 -> master` PR, enable auto-merge, and monitor required `affected` checks until merged. Do not reuse merged PR #311.
