# Plan — SceneIssue 20260825-192751-413 VoxelShowcase

## Acceptance surface
The capture has one marked region: the top-left FPS/surface telemetry while viewing the saved VoxelShowcase pose. Acceptance is the real production path, not a test-only toggle: GPU extraction must complete visible step-1/step-2 builds during a 210 m traversal at the scene's 18 m/s fly-speed cap, cross >=4 regions, preserve a visible voxel draw every moving frame, keep near/far fallback gaps <=5 cm, perform zero frame-path blocking completions, and stay below 18 ms p95 / 25 ms p99. After convergence, record stationary p50/p95 and effective FPS to quantify distance from the original ~1000 FPS headroom target.

## Evidence / competing hypotheses
- **Old GPU cutover unsafe as-is — supported historically, but the failing source is obsolete.** Exact source `2d4c0a090f37cefc6d60db1ccd1ca68c04190815` / run `33125988697` lost every visible solid draw at frame 8. That source predates the subsequent raw-mirror semantic fixes, so it does not prove today's GPU path fails.
- **CPU raw-voxel eligibility scan is a major streaming cost — supported.** The eight-worker profile attributed roughly 9.16/6.39/5.21 ms to scheduler/admission/worker preparation versus ~0.16 ms upload. Current exact-ring classification now marks step-1/step-2 builds as GPU candidates without walking mixed voxel payloads; `CSSampleDensity` classifies raw mirror bricks in compute instead.
- **Packed surface semantics caused GPU/CPU disagreement — fixed in current source.** `VoxelBrickDensity.hlsl` decodes Storage's packed `ushort` semantics before GPU style/coating decisions; the compute classifier rejects non-continuous/decorated surfaces.
- **GPU rejection can publish an empty hole — falsified in current state machine.** Zero/failed GPU counts release the staged mirror and explicitly schedule the CPU continuous-density chain; previous ready arena geometry remains live until a count/write-agreed GPU lease is atomically published.
- **Per-chunk draw submission is the main remaining steady-state cost — falsified for current renderer.** `VoxelRenderPass` already compacts visible metadata and emits bucketed instanced procedural submissions. This issue should not reopen that architecture without runtime evidence.

## Candidate / blast radius
Enable the validated near-ring GPU cutover by default while retaining `VOXEL_DISABLE_GPU_CUTOVER=1` as an emergency/A-B fallback. Keep GPU scope to source steps 1/2; step 4 feature-preserving fallback, step 8 HLOD, Storage, collision, gameplay/worldgen, profile geometry, and unsupported/decorated surface semantics remain on their existing CPU paths. The dense per-chunk GPU cache descriptor is only bounded metadata (about 10^3 entries at step 1 and 18^3 at step 2); the expensive mixed-voxel semantic scan has moved to the persistent GPU mirror/compute path.

## Behavioral regression / final gate
`ShowcaseGpuMigrationTests.MovingShowcaseCompletesGpuSurfaceBuildsAndPreservesCoverage` keeps the production-speed traversal and coverage/frame-time budgets, and additionally requires GPU cutover availability, a resident GPU backend, >=8 GPU-completed chunks, and >=5% GPU share of completed solid builds during movement. It logs GPU completions/fallbacks/readback-wait slices plus a 240-frame settled stationary p50/p95/effective-FPS benchmark. The final exact-SHA CI request must also run the required built-app saved-pose replay. Do not close on a merely green toggle: inspect the replay and telemetry, and iterate if GPU adoption, coverage, or measured performance remains materially deficient.
