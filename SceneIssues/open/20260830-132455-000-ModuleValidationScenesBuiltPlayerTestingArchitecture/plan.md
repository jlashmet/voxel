# Plan

Created with the SceneIssue so the assigned agent has a dedicated planning file from the start.

The agent must replace this stub before implementation with the observed validation defects, acceptance mapping, migration sequence, blast-radius/cost notes, and exact validation gates from `issue.json`.
