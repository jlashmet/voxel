# Tasks

The assigned agent must replace this stub before implementation with checkboxes covering every acceptance criterion in `issue.json`, discovered required work, blast radius/cost validation, and exact-SHA CI gates.
