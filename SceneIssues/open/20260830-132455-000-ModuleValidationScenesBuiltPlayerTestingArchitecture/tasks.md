# Tasks

- [x] Inventory current targeted CI, standalone-player capture, special-case scene/test mappings, and validation docs.
- [x] Define deterministic module ownership metadata: production paths, focused tests, optional validation scene, and separate scenario.
- [x] Implement diff-driven validation planning with conservative shared/core expansion and hard failure for unresolved required production ownership.
- [x] Make targeted CI automatically execute discovered focused tests, player-visible module validation targets, and `KentridgePlayableSlice` for production diffs.
- [x] Ensure required zero-match/skipped focused tests, module scenes, captures, or Kentridge integration fail validation.
- [x] Refactor the standalone-player harness so module validation, Kentridge, and SceneIssue replay share one generic mechanism without arbitrary test-name/scene-name feature inference.
- [x] Migrate Water to a module-local deterministic validation scene/scenario using production Water implementation.
- [x] Add an independent non-Water metadata/planner fixture to prove the discovery contract is reusable.
- [x] Keep existing useful focused regressions while removing PlayMode-only visual-acceptance semantics.
- [x] Update `AGENTS.md`, `SceneIssues/README.md`, `SceneIssues/feature-readme.md`, CI semantics, and validation documentation to describe the module-author workflow and hide harness internals.
- [x] Add focused automated regression coverage for module discovery, shared/core fallback, scene/scenario separation, and mandatory/fail-closed gate execution.
- [x] Require module-owned readiness/world-layout evidence so an early-aborted Water or Kentridge player can never satisfy the built-player gate with screenshots alone.
- [x] Isolate and fix the additive-height river-bank/pool-shelf overlap exposed by exact-SHA run `33338330606` without weakening the shoreline probe.
- [x] Isolate the production-rendering cause of the ring seams, detached strip, coarse banks, and smeared water in green exact-SHA run `33339810737`; the minimal repro established that the frozen far-field clipmap bypassed the production near-field Water owner.
- [ ] Replace the startup-only prototype Water validation fixture with a representative module-local production Water tableau covering still water, shallow shoreline, river flow, waterfall/cascade, and terrain contact; inspect exact built-player evidence as production-quality.
  - Earlier materially different presentation repairs fixed camera world scale, debug tint, and scene-owned lighting without satisfying visual acceptance; production-path isolation then found the actual Water draw-addressing defect.
  - Root cause: Water mesh indices are chunk-local while vertices live at nonzero shared-arena leases. Water now supplies `_SurfaceVertexBase = _liveLease.VertexStart`, and `WaterSurface.shader` adds it to local indices.
  - Green run `33362258628` was stale-source evidence and is excluded. Correct transport run `33362755513` used request `13ff0be529199b2555d3baca3446cfa4ff8cfe6c` directly on feature `66d031dd3fc34d9b0c26c57861a2c5d6da478bb3`; focused tests, Water, Kentridge, previews, and artifact upload passed.
  - Direct review of run `33362755513` proved the vertex-base correction made production Water visible, but still rejected visual acceptance as prototype/blockout quality: broad translucent water sheets floated/cut across terrain and river/cascade contacts read as overlapping planes.
  - First materially different fix for that visible-but-blockout symptom, `d48c5c2315c96b7a1d9cc33f662c267634c8b6d1`, added immediate sand/stone beds and stepped stone shoulders under/along production liquid without changing shared rendering policy.
  - Exact request `d2a93fc275a85699a0209ea5cddba3206435a37b` / run `33367217621` executed successfully for feature `a8cdbc45093e6aa252aa55f186271ced87e888c6`: focused Water regression, automatic module-plan derivation, Water + Kentridge built-player validation, previews, artifact upload, and final status all passed.
  - Direct review of run `33367217621` still rejected the same acceptance symptom. Grounding improved support/contact, but later frames remained dominated by oversized flat water slabs and terraced apron/bank geometry; frame 0 was blank before the large synchronous tableau converged.
  - Second materially different fix, `582f290a09dbb8b2b45d12cbb500f209ae079247`, reduced tableau footprint/relief and reframed all shots. Exact run `33368234376` passed end-to-end but direct standalone-player review still showed the same broad planar/slab blockout symptom, so the two-fix boundary was reached.
  - Minimal repro/root cause isolated before further fixes: `WaterBrickMeshBatchJob` greedily merged contiguous exposed top-water voxels into one large coplanar quad, leaving only corner vertices, while `WaterSurface.shader` animated fragment normals only. Therefore scene tuning could not create geometric waves or remove the large planar-slab read.
  - Behavioral regression `WaterBrickMeshBatchJobTests.FlatWaterTop_EmitsPerVoxelTopQuads_ForWaveDeformation` requires voxel-resolution top tessellation. Production fix `34ec82a0e82cf18d653c7fff5442605981fc123a` preserves that tessellation only for upward faces while keeping side/bottom greedy extraction; shader commit `1668500c6c9d9bb639158813e201de3737063f55` adds deterministic presentation-only vertex displacement for upward water surfaces. Authoritative voxels/collision are unchanged.
  - Water module metadata includes the topology regression so ordinary Water production diffs automatically run it instead of relying on explicit agent selection.
  - Request `40adb3aa86b9934bde2133a211a8b561af51ddf8` / run `33374658385` completed failure before module validation because the new EditMode regression did not compile: CS1654 on writes through `using var` NativeArray locals (`brickBases` and `materials`). Artifact `9751481232` confirmed the exact compiler diagnostics. This is a product/test defect, not infrastructure. Commit `63b720a387240a22505a6709d4f32d7df225543b` keeps the arrays mutable and disposes all native containers in `finally`.
  - Corrected request `8cb8e61560152dd10a4aa2d0a2e6b290a66979a0` / run `33375145205` passed end-to-end for exact feature `b04ddce047eb2dec6803173196126b9de7dc1877`. Automatic Water validation ran both focused tests (12.24s PlayMode + 9.30s EditMode), built-player Water (47.59s), and built-player Kentridge (110.69s), total 179.82s; artifact `9751836112` showed no gate skips.
  - Direct review of artifact `9751836112` still rejects production-quality visual acceptance. The topology/shader fix gives the liquid surface visible wave texture, but retained shots remain dominated by broad engineered rectangular channels and coarse terraced banks. Frame `frame_000_t002.1.png` is additionally an invalid startup-clear frame: player logs show Water reaches `liquid-ready`/`ready` only around 2.3-3.0s.
  - Evidence-window correctness fix: shared scenario metadata supports generic `capture.evidenceAfterSeconds`, propagated through the one player harness as `--evidence-after`. The harness removes pre-window captures before enforcing `minimumFrames`, so startup-clear frames cannot count as visual proof without adding Water/scene-specific policy. Water owns `evidenceAfterSeconds: 4`; independent parser regression `tools/tests/test_player_validation.py` proves the option is generic.
  - Request `0b9572c245fc551edd26361c9213d1836838a86f` / run `33376500985` completed successfully against evidence-window head `92e9c613cbc3c373a4f3823b64c7281a58aa7fe4`. Direct review proved startup-clear frames were correctly excluded, but that source still contained the older blockout tableau and therefore did not satisfy visual acceptance.
  - Exact-head request `05c4755733040d8e102735a949c53844ca8d7f7e` / run `33378452180` completed successfully for feature `f3d6c9dc84c2d7e051c4146d09fa3cfce75b0005`; focused regression, automatic module planning, Water + Kentridge player validation, previews, artifact upload, and final status all passed.
  - Direct review of every retained Water frame (`t=8.1`, `14.1`, `20.1`, `26.1`) still rejects production-quality acceptance on one specific scene-local relationship: the river/cascade reads as broad rectangular elevated slabs with hard vertical grade seams instead of water continuously grounded in the terrain. Pool shoreline/evidence timing are improved and are not the current defect.
  - Scene-local correction `2630225f948252e077ab54d76c7de1df41a737fe` narrows and increases the meander of the river, removes the three long elevated river grade plateaus by using one grounded upstream level, and narrows the cascade while retaining production Water/Cascade materials and the existing generic harness.
  - Next gate: issue a fresh exact-head request through `ci-test/fixes/agent-8`, inspect every retained Water standalone frame, and do not check this task until the visible tableau itself meets production-quality acceptance.
- [x] Validate an ordinary Water production diff automatically selects focused Water tests -> built-player Water scene -> built-player `KentridgePlayableSlice`; record runtime/cost. Corrected run `33359614839` selected exactly `water` + `kentridge-integration`; 11.4s focused + 46.5s Water + 105.11s Kentridge = 163.0s automatic path. Post-topology exact run `33375145205` remained practical at 179.82s total (+16.82s, about 10.3%).
- [ ] Review final diff against all 18 acceptance criteria and complete required SceneIssue metadata/closure only after green exact-SHA CI and production-quality Water visual acceptance.
