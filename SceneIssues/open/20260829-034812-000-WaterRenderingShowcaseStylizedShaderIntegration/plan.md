# Plan

## Goal / acceptance
Ship one reusable stylized water renderer for still, flowing, and waterfall profiles through canonical voxel storage/extraction and `Hidden/VoxelEngine/WaterSurface`. Built-player evidence must visibly show distinct motion and a production-quality waterfall with coherent downward flow, turbulence/aeration, irregular breakup, lip/edge/base foam, and free mist/spray. No scene-local renderer/material fork; `.github/test-request.json` stays off `fixes/agent-9`.

## Proven findings
- Metal arena addressing required explicit `_SurfaceVertexBase`; fixed and regression-covered.
- Generic waterfall topology/spray now travels through canonical extraction/cache/GPU arena. `WaterSprayFlag` survival and Cascade-only rasterization are proven.
- Two materially different fan treatments failed the same angular spray symptom; root cause was translucent spray writing depth in the body pass. Spray now renders in a selective second `ZWrite Off` pass only for entries containing spray.
- After that fix, broad same-span spray sheets and seven near-parallel showcase ribbons still looked synthetic. Minimal discrimination separated reusable topology from scene policy. Canonical spray is now three tapered sheets with distinct footprints; showcase composition is four overlapping connected Cascade bands. Independent regression requires taper/distinct footprints and the selective spray draw.
- Exact run `33375101254` passed `WaterArenaDrawRegressionTests`, automatic module validation, and the 60-second WaterRenderingShowcase replay for feature SHA `43079c6f44d0745e553b149fa6f2a6f36a3ff280`. Direct review still rejected the waterfall: the starburst/negative-space catastrophe is gone, but close/time-separated frames show repetitive bright parallel bands, weak irregular breakup, and weak free base spray.
- Comparing production to `WaterfallReference.shader` isolated the first curtain defect: production used fixed world-space sine carriers shared across overlapping bands, with noise only gating brightness. The shared renderer now warps that carrier with descending multi-scale turbulence.
- Exact run `33376859708` passed `WaterArenaDrawRegressionTests`, automatic module validation, and the 60-second built replay for feature SHA `ece306a6ab867701628a0db45dc9e230891353d7`. Direct review still rejects closure: parallel brightness is softer and the starburst remains gone, but the body is still a flat rectangular veil with weak breakup and thin impact wisps.
- The remaining flat-veil root cause is low-coverage Waterfall fragments writing depth despite low alpha. Vertical Waterfall fragments are now clipped before the depth-writing body pass while still/river depth behavior remains unchanged (`3b1729c9c8a98af4c8692b13b7450c196c524f8e`). `WaterfallBodyPunchesRealCoverageWhileStillWaterRemainsContinuous` renders the canonical shader to measure real coverage loss.
- The weak-impact composition cause is also fixed: all four authored Cascade feet now end one voxel above the receiving RiverWater surface, localizing canonical impact topology/spray at the pool instead of several voxels up the cliff (`da6445f9d4cfbee8b6763dbc77b3d8b6a380b703`). `ExactCascadeCurtainImpactsBesideReceivingWaterAndSurvivesProductionCache` locks the authored contact relationship through storage/cache (`fb4db36bd0346ce477e5b059f16a0248ac568ab4`).
- Exact run `33385090491` on feature SHA `c8d55b655806f85564600d9d42b832d2493038fe` completed as a product failure, not infrastructure: 3/4 `WaterArenaDrawRegressionTests` passed; the cutout discriminator measured 5,776 still-water pixels versus 5,630 Cascade pixels (~2.5% loss), below its 5% minimum. The same exact head built and replayed `WaterRenderingShowcase` for 60 seconds successfully. Direct artifact review confirmed the measured weakness, so the regression threshold was not weakened.
- The stronger waterfall-only body cutoff (`0.18` → `0.26`) passes exact run `33385919424` on feature SHA `a1a3594dc3c32332ff8bfbe1883552c6d8aa62b6`: `WaterArenaDrawRegressionTests`, automatic module validation, the 60-second standalone replay, artifact upload, and final status are green. Direct visual review still rejects production quality. The wide view remains a rectangular curtain; close frames at 32/42/52 seconds show moving internal holes but straight sheet sides and a pane-like outer silhouette, plus repeated hard triangular impact shapes rather than convincing free mist.
- Per the two-attempt rule, cutoff tuning is stopped. Comparing the exact output to `WaterfallReference.shader` isolates the surviving curtain cause: the reference perturbs the sheet's *outer width* with descending edge noise, while production already receives reusable `WaterEdgeFlag` topology but uses `edgeTopology` only in foam/color. Interior holes can therefore increase without producing the required irregular/wobbling outer edge. This is a shared renderer behavior defect, not scene placement policy.
- `WaterfallEdgeCoverageRegressionTests` now supplies the minimal discriminator: identical production-path quads with semantic side-edge topology must lose at least 2% Cascade coverage while still water remains identical. The first exact semantic-edge run `33386958512` on `95dd98a92e9496f1cbfd0b3d8ac1dfc6c343a5aa` is a product-strength failure, not infrastructure: the focused test measured 5,328 untagged Cascade pixels versus 5,267 edge-tagged pixels (~1.15% loss), while the same exact head still built and replayed the showcase for 60 seconds. Direct replay shows the mechanism is active and sides are more irregular, but the raster effect is below the locked discriminator.

## Selected fix / next gates
Keep the isolated semantic-edge design and regression threshold unchanged. Strengthen only the waterfall-local edge erosion amplitude from noise range `0.02..0.14` to `0.04..0.24`, retaining the existing `0.26` global body cutoff, `0.10` lip/impact boundary cutoff, `0.46` maximum cutoff, semantic `WaterEdgeFlag` input, and still/river guards. Re-run `WaterfallEdgeCoverageRegressionTests`; after it is green, run `WaterArenaDrawRegressionTests` plus the 60-second built showcase on the same exact feature head and directly inspect near/wide/time-separated frames. If the hard triangular base-spray silhouette remains, isolate its geometry/raster cause before another spray change.

A5 remains blocked: Kentridge is a real independent renderer consumer but current captures show no visible water. `VoxelShowcase` starts from the committed baked `ShowcaseWorld.bytes`; source registration and bake support do not prove that bake contains visible water. Prove `VoxelShowcase` plus another actual production scene with visible water through built evidence; never substitute `WorldbuildingGalleryShowcase`.

## Cost / blast radius
Water profile tables remain 3,072 bytes plus one semantic mask. Spray keeps the existing 32-byte vertex stride and unchanged three-sheet cardinality; only spray-containing entries pay one additional indirect draw. Edge erosion reuses already-uploaded topology and adds fragment arithmetic only for vertical Waterfall bodies; the amplitude correction changes no storage/extraction/API shape and no still/river branch. Final accepted-head CPU/GPU/memory/render cost remains to be measured; do not infer unavailable GPU timing.

## Merge state
Feature contains master `2ea5f5c95f89fbf0403dbefb50b782829583d304` via merge `87000073f2ca648922a18ae0788ed9008a55dd18`; master has advanced. Merge current master only before final exact-SHA closure/promotion as required.
