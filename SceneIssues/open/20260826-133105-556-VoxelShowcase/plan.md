# Plan

- **Observed defect / acceptance:** one captured VoxelShowcase pose, no circle annotations. The capture note records that the gate interaction succeeds but the front gate disappears; acceptance is an unobstructed doorway with two visibly retained, swung-open timber leaves after the same E interaction. The binary screenshot is preserved but is not renderable through this connector, so no pixel-only claim is used.
- **Competing hypotheses:** (1) E/input or proximity rejects the interaction; (2) renderer/collision invalidation loses otherwise-correct open geometry; (3) the mutation itself defines opening as deleting the gate. (1) is falsified by `HandleKeys -> TryInteract -> TryOpenCastleFrontGate` plus the captured successful interaction. (2) is falsified by the captured visible disappearance. (3) is confirmed: `TryOpenCastleFrontGate` enumerates the authored gate arch and calls `ClearVoxelsBulk`; the existing PlayMode regression requires that whole closed-leaf volume to become empty.
- **Selected fix:** keep the closed 4-voxel leaf and interaction clear unchanged, but author the two physical open-state leaves deeper inside the already-cleared gatehouse passage. They are occluded while closed; clearing the front leaf reveals two inward-swung, iron-banded timber leaves while the centre circulation lane stays empty.
- **Regression:** production PlayMode path opens from `CastleFrontGatePosition`, proves the original front plane clears, proves the centre passage remains traversable, and proves left/right wood/iron open leaves remain visible deeper in the gatehouse.
- **Blast radius / cost:** castle gatehouse authoring, one focused PlayMode regression, and this capture only. No input, renderer, storage, or other SceneIssue changes. Added geometry is a few thousand one-time castle-authoring voxel writes; interaction/frame-loop cost is unchanged.
- **Remaining gates:** targeted exact-SHA CI, native-resolution replay evidence, pending metadata/move, close bookkeeping, merge current master, non-force push to master.
