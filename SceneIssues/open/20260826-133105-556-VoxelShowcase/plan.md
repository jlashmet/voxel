# Plan

- **Captured defect / acceptance:** one VoxelShowcase capture, no marked circles. E succeeds but the castle gate disappears. Acceptance is the same nearby E interaction producing a visible time-based opening, two retained physical leaves, and an unobstructed centre passage.
- **Hypotheses:** (1) E/proximity rejects interaction — falsified by the successful production `TryInteract` path. (2) renderer invalidation loses correct open geometry — falsified because the world operation itself bulk-cleared the authored gate and the legacy test required emptiness. (3) opening is deletion — confirmed.
- **Discriminator / runtime evidence:** the static retained-leaf prototype compiled but its exact CI regression found the expected open leaf `Empty`, so it was removed. The animated implementation's exact run `33129439694` passed the behavioral regression and real-player replay. Its clean editor evidence was still fallback gray; the same saved camera in the real player was gray at 14.6 s and fully materialized by 24.6 s, isolating presentation convergence rather than gate state.
- **Selected fix:** snapshot the real authored closed gate; after E clears it, restore pose zero before render and rotate two material-preserving halves inward over 0.9 s / 9 bounded poses. A showcase-only frame driver supplies time; low-level interaction semantics stay unchanged. Verification now targets the saved `Showcase Camera` and waits 12 real seconds for presentation convergence before the clean 1928x836 render.
- **Regression:** production `TryInteract` proves closed, midpoint, and final voxel poses differ; final left/right timber+iron leaves remain, centre lane stays empty, and interaction remains one-shot.
- **Blast radius / cost:** gate-local ShowcaseWorld state, one showcase-only driver, one PlayMode regression, and this capture. Nine bounded voxel poses batched by block; no ongoing work after completion. No renderer/storage ownership or unrelated SceneIssue changes.
- **Remaining gates:** rerun exact-SHA targeted test+replay, inspect clean native artifact, commit evidence/pending metadata, move pending->closed, merge latest master, push exact feature head to master.
