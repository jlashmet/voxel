# Plan

- **Captured defect / acceptance:** one VoxelShowcase capture, no marked circles. E succeeds but the castle gate disappears. Acceptance is the same nearby E interaction producing a visible time-based opening, two retained physical leaves, and an unobstructed centre passage.
- **Hypotheses:** (1) E/proximity rejects interaction — falsified by successful production `TryInteract`. (2) renderer invalidation loses correct open geometry — falsified because the world operation explicitly bulk-cleared the gate and the legacy test required emptiness. (3) opening is deletion — confirmed.
- **Runtime evidence / discriminator:** the static retained-leaf prototype compiled but its regression found the expected leaf `Empty`, so it was removed. The animated regression now proves closed/mid/final poses, retained timber+iron leaves, and a clear lane. Exact run `33130356493` passed that test and replay; its pinned 1928x836 original pose shows inner masonry through the opened doorway, while the standalone replay at that same pose shows the closed timber/iron gate. Thus camera convergence is fixed, but the very close original pose cannot show the swung-in leaves.
- **Selected fix:** snapshot the authored closed gate; after E clears it, restore pose zero before render and rotate two material-preserving halves inward over 0.9 s / 9 bounded poses. A showcase-only frame driver supplies time; interaction semantics stay unchanged. Keep final evidence at the exact pose and add full-resolution diagonal left/right detail views for leaf inspection.
- **Regression:** production `TryInteract` proves closed, midpoint, and final voxel poses differ; final left/right timber+iron leaves remain, centre lane stays empty, interaction remains one-shot, and native evidence is emitted after view convergence.
- **Blast radius / cost:** gate-local ShowcaseWorld state, one showcase-only driver, one PlayMode regression, and this capture. Nine bounded voxel poses batched by block; no ongoing work after completion. No renderer/storage ownership or unrelated SceneIssue changes.
- **Remaining gates:** merge current master, run the one allowed warm-cache retry after the cold-bake timeout, inspect exact-pose + detail artifacts, commit evidence/pending metadata, move open->pending then pending->closed, merge latest master, push exact feature head to master.
