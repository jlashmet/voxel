# Plan

- **Captured defect / acceptance:** one VoxelShowcase capture, no marked circles. Nearby E must visibly open the castle gate over time without deleting it: two physical leaves remain and the centre passage is clear.
- **Hypotheses:** (1) E/proximity rejects interaction — falsified by successful production `TryInteract`. (2) renderer invalidation loses correct geometry — falsified because the old world operation explicitly cleared the authored gate and the legacy test required emptiness. (3) opening is deletion — confirmed.
- **Runtime evidence / discriminator:** the retained-leaf prototype was rejected when its expected open leaf read `Empty`. The animated regression now proves substantial pose zero, distinct midpoint/final poses, both timber+iron leaves, a clear centre lane, and one-shot interaction. Exact run `33131101780` was green, but canonical standalone evidence remained closed because the replay harness restored only camera pose and never replayed E; its offscreen test renders were fallback-gray and are noncanonical.
- **Selected fix:** snapshot the authored gate; restore pose zero on the interaction frame; rotate both material-preserving halves inward over 0.9 s / 9 bounded poses. For canonical evidence only, this capture opts into `replayAction: "interact"`; the standalone harness synchronizes the production motor with public `TeleportTo`, invokes the same public `TryInteract`, then holds the exact recorded camera while animation completes.
- **Regression:** `NearbyInteractionAnimatesBothGateLeavesAndKeepsCentrePassageClear` exercises production `TryInteract`; closed/mid/final voxel states differ, both leaves retain wood+iron, the lane is empty, and interaction remains one-shot.
- **Blast radius / cost:** production changes are gate-local ShowcaseWorld state plus one showcase driver. Replay action is opt-in and absent for every other capture. Nine bounded gate poses; no ongoing work after completion; no renderer/storage ownership changes.
- **Remaining gates:** exact-SHA targeted CI + standalone post-action visual inspection; commit final evidence/pending metadata; move open→pending→closed; merge latest master; non-force push exact feature head to master.
