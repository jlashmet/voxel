# Plan

- **Captured defect / acceptance:** one VoxelShowcase capture, no marked circles. The note says E succeeds but the castle gate disappears; acceptance is the same nearby E interaction producing a visible time-based opening, two retained physical leaves, and an unobstructed centre passage. The original screenshot is preserved; the failed-run real-player replay at the captured pose confirms the authored closed timber gate is present before interaction.
- **Hypotheses:** (1) E/proximity rejects interaction; falsified by `HandleKeys -> TryInteract -> TryOpenCastleFrontGate` and the captured successful interaction. (2) renderer/collision invalidation loses correct open geometry; falsified because the world method explicitly bulk-clears the authored gate and the legacy test required emptiness. (3) opening is implemented as deletion; confirmed.
- **Discriminating experiment:** the first static retained-leaf prototype compiled on exact request `e6df5ac9c99efa2daff5fb78973318616e3d1534`, but its regression found the expected post-interaction leaf voxel `Empty`; replay still showed a correct closed gate. It also could not meet the explicit animation requirement, so the prototype is removed.
- **Selected fix:** snapshot the real authored closed leaf while closed; after E clears it, restore pose zero before render and rotate the two material-preserving halves inward over 0.9 s / 9 bounded poses. Keep the low-level interaction semantics unchanged; a showcase-only frame driver supplies time.
- **Regression / evidence:** production `TryInteract` proves closed, midpoint, and final voxel poses differ; final left/right timber+iron leaves remain and the centre lane is empty. The test emits a native 1928x836 final render at the captured camera pose.
- **Blast radius / cost:** ShowcaseWorld gate state, one showcase-only frame driver, one PlayMode regression, and this capture. No other interaction, renderer, storage ownership, or SceneIssue changes. Mutations are gate-local, 9 bounded poses batched by block; no ongoing voxel work after completion.
- **Remaining gates:** merge current master, exact-SHA targeted test+replay, inspect native artifact, pending/closed metadata, final master fast-forward.
