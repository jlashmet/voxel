# CI operations

- `66905ed3812838a19c0f46ad207c671434198ba8` — final request for feature `6c4e149839a1205c6111929fd13c192a27ae7b04`; admitted as run `33110323331`, queued until runner availability, then failed during Unity compilation. Root cause: new regression mixed compatibility `VoxelEngine.Showcase.CastlePlan` with `Game.Structures.Api.CastleLayout`; replay build failed from the same compiler error. Product/test source corrected on `fixes/agent-6`; no queued request was replaced.
- `e6df5ac9c99efa2daff5fb78973318616e3d1534` — retry for feature `ee9a6a68ed3a6d3e3743ab5085243603c0640369`; run `33118103870` compiled and completed the assigned real-player replay, but the focused PlayMode assertion found the expected retained open leaf `Empty`. This is a source/test failure, not infrastructure. The static reveal is removed and replaced with a time-based gate animation; no queued request was replaced.
- `cb8dc99b7ffc83b6705244595c8d951e38988c92` — animated-gate request for merged source `596626dab552b39890b328f50fd76fc34d3fb43b`; run `33123445937` was admitted, queued behind another single-test run, then failed during Unity compilation before NUnit. `using System` made four regression `Object` calls ambiguous with `UnityEngine.Object`; the player build failed from the same compiler error. Production animation source had no reported compiler error. The regression now aliases `Object` to `UnityEngine.Object`; no queued request was replaced.
- `c50248d94d033c6916f598582ff99f31e089f662` — compile-fix request for source `b366c96f2fc6417b89842df716b1f8d3f89e3826`; run `33123891196` restored/baked the Showcase world, then the requested PlayMode test and real-player capture both completed successfully. The overall job exceeded its five-minute cap because the cache-miss bake consumed 3m25s, so GitHub concluded the run `cancelled` and published `ci/single-test=failure`. Treat this as infrastructure/timing evidence only; it does not satisfy the gate and does not justify a product change.
- `8288cba1487e446754f6608ded6789e8ed252d21` — exact request for source `674bb09ae97e769d7224cccd458e102ecf840314`; run `33129439694` finished `success` and the production-path gate regression plus real-player replay passed. Human evidence inspection rejected promotion because the clean 1928x836 editor frame was still fallback gray. The same fixed replay pose was gray at 14.6s and fully materialized by 24.6s, isolating presentation convergence. Only regression evidence handling changed; production stayed unchanged.
- `f94d89d1b8125400d06296470c84c6f9de9f144f` — exact request for source `a1189234c07cc40093c97c792b033d3fc401684e`; run `33129981847` finished `success` after a 12-second real-time settle, but human inspection again rejected the clean frame as fallback gray. Because that test teleported the camera only at `Render()`, this falsifies global wait alone and points to view-dependent convergence. The next attempt pins the exact saved Showcase camera throughout the settle; production remains unchanged.
