# Tasks

- [x] Trace current `VoxelShowcase` composition through WorldBuilder/shared authoring and identify the production owners for terrain/carving, subterranean traversal, formations, ruin/statue assembly, and local/underground lighting.
- [x] Map the issue acceptance criteria and discovered reusable constraints to existing capabilities or explicit reusable gaps.
- [x] Add a reusable destination-cavern/ancient-ruin composer in `Game.Structures`; reuse the existing natural-cave voxel formation planner and mine-cave lantern presentation rather than duplicating geology or light semantics.
- [x] Establish a bounded subterranean dark-stone host volume before carving so deep-cavern behavior does not depend on whether a sparse negative-y region begins solid or empty.
- [x] Use shared cave traversal results/placement requirements to select the cavern destination by meaningful entrance-to-destination traversal distance rather than a showcase-only coordinate assumption.
- [x] Integrate cavern generation with both offline `VoxelShowcase` baking and runtime bake restoration/presentation metadata so the shipping baked startup path contains the feature and its local lights.
- [x] Replace the production mouth's rectangular first impression with a reusable natural/asymmetric cave-mouth treatment while preserving normal walkable clearance.
- [x] Add deterministic direction-changing traversal through shared cavern authoring (not a straight corridor with only side alcoves); preserve deep-host protection and destination reachability.
- [x] Implement a much larger connected cavern with high irregular ceiling, broad navigable floor, recess/rock variation, and unblocked route.
- [x] Populate reusable geological formations including stalactites and multiple additional categories without blocking circulation.
- [x] Implement/place a reachable aged/damaged stone ruin with believable structural construction and readable entrance.
- [x] Place exactly two large aged dark-stone humanoid statues, grounded on credible bases and framing the ruin entrance.
- [x] Extend supported local torch/lantern navigation lighting from the descent into the primary cavern while keeping the emitted-light count bounded and preserving deep darkness.
- [x] Add focused behavioral regression through the authoritative `VoxelShowcase`/WorldBuilder production path, in addition to the detached authoring regression, for route execution, cavern/statues/geology/lights/determinism/write budget.
- [x] Review blast radius for every shared-system change: generic cave engine unchanged; new traversal/finish helpers are opt-in and only `VoxelShowcase` consumes them; no new renderer/light system or broader preload scheme introduced.
- [x] Make bend placement, route shape, and route-light spacing/count configurable or derived through reusable `UndergroundCavernTraversalProfile`; production no longer depends on fixed showcase dogleg/light segment indices or an exact cardinal-route count.
- [x] Move affected-region envelope/preload/publication and local cave-light registration onto reusable Game/engine contracts through `UndergroundCavernRuntimeSupport`, `IUndergroundCavernRegionRuntime`, and `LocalLightContributorRegistry`; cavern lighting does not use `CastlePresentationLights`.
- [x] Add a focused portability regression that authors a second cave at a different origin/facing/length/seed/profile and proves connected traversal plus reusable local-light and affected-region semantics without copying showcase implementation.
- [x] Diagnose historical request `70ee14d8` / run `33240610366`: the shared replay camera was being overridden; the generic replay-owner correction is verified by run `33254822417`.
- [x] Diagnose corrected-source CI request `808decb3`: product compile failure because `UndergroundCavernRuinAuthoring.cs` used `Coatings` without the `VoxelEngine.Storage.Api` namespace import.
- [x] Diagnose replacement CI request `0056637e`: product compile failure because `Game.Structures.Runtime.asmdef` did not reference `VoxelEngine.Storage.Api`; add the explicit API assembly reference rather than duplicating the coating ID.
- [x] Diagnose request `9b7b1d0b`: product PlayMode failure from `BrickPool` exhaustion while the showcase synchronously generated a coarse 3x3x3 terrain-region neighbourhood at every cave step. Bound terrain preparation to the actual tunnel/chamber/cavern voxel envelope instead of increasing the 65,536-brick device budget or weakening acceptance limits.
- [x] Diagnose request `ec947212` / run `33231311037`: forced dogleg geometry stayed at one segment-end Y while the generic cave core linearly descends across each segment. Make reusable dogleg carving and semantic waypoints sample the same piecewise-linear primary-route grade rather than weakening traversal limits.
- [x] Diagnose request `3deca755` / run `33233504808`: semantic traversal omitted a primary-route segment and shortcut through rock. Restore the omitted waypoints ahead of the dogleg; do not change motor/test/budget limits.
- [x] Diagnose request `1d095174` / run `33237069317`: the destination host shell sealed the already-authored terminal route. Reassert a protected destination corridor after destination authoring; do not weaken motor/test/budget limits.
- [x] Inspect exact request `867d1e63` / run `33254822417`: focused test and real-player build/capture were structurally green, but direct rendered review failed the AAA gate because the descent evidence clipped and the cavern/ruin/statues read as primitive placeholder composition.
- [x] Discriminate the visual failure against source: reject the camera-only hypothesis because the reusable source itself was dominated by one cylindrical destination carve, one box-first ruin shell, and simple box/cylinder statues.
- [x] Add reusable visual finish authoring: overlapping boundary lobes/recess silhouettes, layered damaged arched masonry, articulated upgrades for the same two semantic statues, large silhouette formations, deterministic weathering, and a final circulation reassertion.
- [x] Extend the focused production regression with hard visual-structure invariants and a separate visual-finish write ceiling while preserving the existing overall 55,000,000-write and eight-light ceilings.
- [x] Honor `quality-review.md`: return the assignment from premature `pending` to `open`; treat staged camera replay as diagnostic only, not final acceptance evidence.
- [x] Add an opt-in built-player route evidence driver that starts at the authored surface mouth and thereafter steers the existing `AutoWalk` input through the production semantic waypoints while normal `VoxelShowcase.MovePlayer -> CharacterMotor.Step -> streaming` remains authoritative.
- [x] Map the cavern acceptance test to a 95-second real-player capture profile that passes `-voxel-underground-cavern-traversal`, fails unless the built player logs route completion at the ruin, and requires at least six presented frames.
- [x] Merge current `origin/master` (`bc0593070e10497da89d651e1ab3c61772335f95`) into `fixes/agent-3` as a real two-parent merge before the fresh final CI request; no out-of-assignment conflict was found.
- [x] Diagnose exact request `7bdaac68` / run `33269000385` as a product compile failure: both focused Unity and built-player build stop on CS0246 because the two new reusable finish/circulation files omitted `using Game.Structures.Api;` for `DecorationBounds`. The existing cavern authoring proves the owning API namespace; add only the two missing imports and do not change assemblies, behavior, or budgets.
- [x] Diagnose exact request `2c0f86dc` / run `33269217520` attempt 1 as a product traversal failure: focused PlayMode stalls at waypoint 34/38 and the built player reaches only waypoint 30. The rear irregular-lobe shell overwrites the previously protected terminal approach while the post-finish circulation pass starts from ruin bounds too far forward; floor-grade mismatch is falsified by the matching terminal Y and earlier route carve.
- [x] Extend reusable `UndergroundCavernCirculationProtection` to protect from the rear cavern approach through the ruin using cavern/ruin bounds and facing, with bounded width-derived overlap and no showcase coordinates or weakened motor limits.
- [x] Add/strengthen a focused regression proving post-finish circulation includes the rear cavern approach and normal CharacterMotor traversal can reach the ruin.
- [x] Inspect exact request `77001787` / run `33272245282`: focused PlayMode and real-player route traversal are green through waypoint 38/38, but direct review of all eight 1600x900 frames still fails the AAA gate because the long descent reads as a rectangular beam-lined corridor and the final view does not clearly present a huge cavern, ruin facade, or both statues.
- [x] Naturalize the full reusable descent outside dogleg host windows with deterministic overlapping irregular voids that preserve the existing route grade/core while breaking the long rectangular cross-section silhouette.
- [x] Move the production ruin toward the far end of the cavern and use a reusable derived front-approach waypoint so the player finishes outside the facade with the two statues compositionally readable, not at the ruin centre.
- [x] Strengthen production regressions for full-route naturalization density, far-end ruin/approach semantics, normal CharacterMotor reachability, and the unchanged 55,000,000-write/eight-light budgets.
- [x] Review first visual-repair request `c0b16843` before execution: source inspection found naturalization ordered after route lantern geometry and a 132-voxel ruin offset that separated semantic statue bases from the articulated finish. Reorder naturalization before traversal fixtures and use the bounded 112-voxel far-wall composition; treat run `33274260594` as diagnostic only.
- [ ] Diagnose exact request/run `33274618946`: focused PlayMode and production traversal are green through waypoint 38/38, but the configured built-player screenshots end at `t=94.5s` while the same run reports a 177.35-second completed production path, so the required cavern/ruin/statue reveal is outside the evidence window. Extend only the cavern real-player evidence duration/capture coverage; do not change movement, acceptance criteria, or budgets to make the gate pass.
- [ ] Preserve and visually validate the final reveal sequence from the moving built player: daylight mouth -> prolonged varied descent -> huge dark irregular cavern/formations -> aged ruin/exactly two grounded humanoid statues.
- [ ] Quantify world-build/runtime cost from exact final CI metrics: voxel/chunk work, triangle/index/draw impact, local-light/shadow cost, and supported-device budget impact without weakening budgets.
- [ ] Run green exact-SHA focused targeted CI on `ci-test/fixes/agent-3` only after production/test work is final.
- [ ] Run green exact-SHA built-application `VoxelShowcase` traversal/capture and verify route completion, usable rendered frames, and no startup/runtime exceptions.
- [ ] Directly inspect every useful built-player frame for mouth, descent, cavern scale/geology, ruin, statues, localized lighting, circulation, intersections, and placeholder geometry.
- [ ] Validate every acceptance criterion from built-app evidence and record durable verification evidence beside the assignment.
- [ ] Move only this assignment `open` -> `pending` after both exact-SHA gates and direct visual review are green; complete pending metadata then.
- [ ] After all checkboxes/acceptance criteria are complete, set `status=fixed` and `resolvedUtc`, move only this assignment `pending` -> `closed`, re-check current master ancestry, and push the exact validated feature head to `origin/master` non-force.
