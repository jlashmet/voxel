# GPU renderer production restoration

## Objective and acceptance

Deliver production-quality `Assets/Scenes/VoxelShowcase.unity` through GPU rendering, delete retired CPU-only rendering, and pursue **1,000 FPS / 1.00 ms whole frame**, or the closest repeatable measured result under [tasks.md](tasks.md). Preserve CPU authority and GPU host orchestration. No hidden content, weaker budgets, reduced distance or permanent CPU fallback. User wants the full GPU path before additional optimization work.

## Retained evidence

Local harness/tests/screenshots are authorized. Worktree `/private/tmp/voxel-gpu-restoration`, branch `gpu-rendering-agent-1-resume`, HEAD `8e5c4ba95`. Last requested push reached `origin/fixes/agent-1` at `64b2921a3`.

Step8 has GPU summaries/meshing and module proof. Source directory reclamation now protects demanded/active keys, repairs collision chains on deletion and bounds lookup probes within the previous GPU-byte ceiling. Forty-eight targeted tests and near-module player passed. Last Showcase completed 180s/11 captures; reviewed 75.1s/150.1s remains **unacceptable**, with incomplete coarse coverage despite absent black obstruction. Three step8 publications and 1,041,989 directory refusals leave source-capacity/readiness work open. Full evidence is in [tasks.md](tasks.md).

## Current step4 migration and next experiment

Step4 now uses regular GPU extraction plus a per-descriptor GPU decision for false-empty output. Empty ordinary counts with no profile/error select existing feature-preserving HLOD count/write in the same arena transaction. No CPU geometry/count readback; empty sources emit no faces. All 47 targeted tests pass, including thin→ordinary→air lane reuse (20s harness, 4.4s tests). Thin-feature regression verifies actual prepared-cache count/prefix/arena/write output; suppression preserves ordinary geometry, profiles, errors and finer-ring behavior.

`gpu-step4-coarse-bands/` completed 60s/six captures/exit 0, three step4/two step8 publications. Reviewed 10s/50s is **prototype/blockout quality**: lumpy intermediate terrain, stepped coarse contours. First attempt hit native Burst SIGBUS; first retry correctly failed because its camera landed in step2. Corrected from logged distance bands.

`gpu-step4-showcase/` completed 180s/11 captures/exit 0. Final step4 publications 79, step8 three, missing-visible 598, directory refusals 4,376,579. Reviewed 75.2s/150.2s is **unacceptable**: distant houses lose roof/material separation versus prior source; terrain/frontier coverage remains incomplete. No black full-view obstruction in those captures. Timing is diagnostic only; no performance acceptance.

H1: step4 reconstruction loses exposed roof materials. H2: far-feature replacement switches to a simplified presentation. Next isolate a production-generated house at the step4 boundary and inspect actual material/source identity before changing sampling. Coarse demand also needs bounded source/summary streaming; water remains CPU-backed. Preserve these gates while continuing GPU completion, then optimization.

## Remaining gates

Finish mixed-LOD/frontier liveness, finish step4 visual fidelity and migrate water, delete CPU-only rendering/oracles, and complete G11 retirement/error policy. Validate edits, pressure, lifecycle, module/integration players and repeated frame/memory workloads. G01–G27 remain incomplete.
