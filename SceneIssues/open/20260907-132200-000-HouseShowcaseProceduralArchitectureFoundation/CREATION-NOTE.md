This feature SceneIssue was initially published with `issue.json` only. The required author-supplied `plan.md` and `tasks.md` were added immediately afterward to restore the intended creation contract before substantive implementation. The assignee may keep these files current as required by `SceneIssues/feature-readme.md`, but must not weaken the acceptance criteria in `issue.json`.
