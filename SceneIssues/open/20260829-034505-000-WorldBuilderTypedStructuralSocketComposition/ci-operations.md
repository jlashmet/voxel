# CI operations

- 2026-08-30T10:28Z — request transport `d33869e84f033b95b74f050e135343859453fb24` admitted as run `33306559976`, then failed in **Resolve test request** before Unity. Cause: `replay_seconds` was encoded as string `"60"`; workflow contract requires an integer from 20 to 60. Requested PlayMode test and built-player capture were skipped, so this run satisfies no validation gate.
- 2026-08-30T10:30Z — corrected transport `769a14c573d8466047da716943a062d72b37b501` (source `80a35682e21bfb96cd123254159c6227945ced50`) admitted as run `33306616999`. Both requested-test and real-player build reached Unity but failed script compilation. Exact in-scope defects included readonly-`using` catalogue mutations plus an unresolved `CharacterMotor` reference. No screenshots were emitted and no validation gate passed.
- 2026-08-30T10:36Z — transport `ec4eecbc2c8cd5b78e8a39bc883280d9561f20b0` (source `9b0b3afccc1bfda92bce10e3917bcd6912030b74`) admitted as run `33306897362`. Readonly catalogue fixes compiled far enough to isolate the remaining assembly error: lower-layer `ShowcaseWorld` could not own the SceneRuntime `CharacterMotor`. Both requested-test and player build stopped at compilation; no screenshots were emitted and no gate passed. Traversal execution was moved to SceneRuntime while route/preload data remained in `ShowcaseWorld`.
- 2026-08-30T10:43Z — transport `a2f2f9bf5e6a29a7b48c78054262bd79f03bc95f` (source `b906a31e8e253e2918d87981ff8c7c9a6f87fec8`) admitted as run `33307182322`. The traversal boundary compiled far enough to isolate one remaining SceneRuntime assembly reference defect: `WorldbuildingGalleryAuditHarness` uses planner-owned `StructuralCompositionResult` and `StructuralAttachmentRejectReason` from `VoxelEngine.Structures.Runtime`, but `VoxelEngine.Showcase.asmdef` referenced only `VoxelEngine.Structures.Api`. The structural runtime has no dependency back to Showcase, so the minimal acyclic fix is to add that existing runtime assembly reference. Requested test and player build both stopped at this compile error; zero screenshots were emitted and no validation gate passed.
- 2026-08-30T11:03Z — transport `8ddccbc8ceaaba3c1aff3f4a184215c821fc92f1` (source `5a27b32a46c7bb6a42a793143f291d4b4d0028d3`) admitted as run `33307954838`. Compilation succeeded and both requested PlayMode and standalone-player stages executed. Both exposed the same production catalogue defect: the monumental bridge failed composition with `ClearanceBlocked`, so the player audit later reported `STRUCTURAL_AUDIT result=FAIL reason=structural-content-missing`. Reproducing the production seed/hash stream showed the required 3-pier slot had `LocalMin.x=80`, `LocalMax.x=240`, `Spacing=80`; because `FeatureHash.Range` is inclusive and attachment positions are floor-quantized by spacing, the final 240 lattice bin had only one draw value while the 80/160 bins each had eighty. This exact seed accepted 160 and 80, then missed 240 for all 16 bounded retries. Correct the authored inclusive maximum to 319 so the three legal lattice positions have equal 80-value bins; do not raise the global placement-attempt budget.
- 2026-08-30T11:18Z — transport `1ce237b1f43bc70a93e6e128b50a035d208ac383` (source `1f31fb56e900dbf1f710f7a4d5240a3119c4e1b7`) admitted as run `33308789329` after the support-lattice correction. Assembly admission passed and the standalone player authored the structural gallery successfully: `STRUCTURAL_GALLERY authored=True`; bridge metrics were `children=5`, `primitives=12`, `voxelBudget=6073200`, `regions=3`, `instances=8`, `voxelsWritten=1094400`, graph `0xA45A3A2127E35988`. The focused PlayMode test failed later on `FixedString32Bytes` truncation while copying `upper-terrain-supported-platform`. The player audit independently proved bridge and castle traversal, then route 2 stopped with `startDistance=46m`, `endDistance=30.919m`, final feet `(-340.92, 21.80, 104.00)`. Reproducing the cliff proof (`site=(-3600,1000)`, `low=213`, `rise=14`) against `BoxEmitter.RampContains` shows the 260-long/18-high ramp has zero occupied voxels in its initial shallow columns, so the motor walks off the lower platform, drops to terrain, and then meets the later wedge face. The in-scope correction is proof-catalogue-only: shorten the socket name to the fixed-string contract and add one authoritative `260 x 1 x 80` base slab beneath the ramp, raising that piece's local primitive ceiling from 1 to 2. Conservative incremental cost is 20,800 voxels and one primitive before overlap; global solver/ramp semantics and budgets remain unchanged.
- 2026-08-30T12:36Z — transport `7ebae779b6926b62d5f890acfa3d395a74b52501` (exact source parent `250cc16515451e57a5543d5ae1104e3c5f79b2eb`) admitted as run `33311927310`. The requested focused PlayMode regression passed and the standalone player built successfully. Built-player structural metrics were valid for all four proofs: bridge `children=5 primitives=12 voxelBudget=6073200 regions=3 instances=8 voxelsWritten=1094400`; castle `children=4 primitives=11 voxelBudget=5072000 regions=3 instances=7 voxelsWritten=4881600`; cliff `children=3 primitives=6 voxelBudget=2537600 regions=6 instances=12 voxelsWritten=1466144`; facade/roof aggregate `children=8 primitives=22 voxelBudget=10923840 regions=3 instances=13 voxelsWritten=9121984`. Bridge and castle production `CharacterMotor` traversals passed. Cliff route 2 improved from 30.919 m remaining to 6.435 m, ending at `(-316.44, 24.60, 104.00)`, proving the ramp base slab fixed the initial no-voxel gap. Geometry review shows the upper platform walking surface is still 8 voxels (0.8 m) above the ramp's top surface, exceeding the motor's 0.35 m step height. The structural audit exits on this traversal failure before its capture loop, so the artifact contains no `StructuralCompositionAudit` frames; unrelated town frames do not satisfy this feature's visual gate. Next correction remains proof-catalogue-only: lower the stable upper-platform attachment by 8 voxels so the walking surfaces meet; no global ramp/solver/motor budget or behavior change.
- 2026-08-30T13:06Z — transport `dec7bed4987d80b8bebb9815bf9e542c39813b38` (exact source parent `78f3b3ff7a907d8c9b2146c8a1c6adcadd796a01`) admitted as run `33313094692`. The focused PlayMode regression passed. Built-player authoring again produced valid metrics for all four proofs; bridge and castle production `CharacterMotor` traversals passed. Cliff route 2 now crossed the corrected ramp/upper-platform seam and reduced its horizontal distance from `46m` to `2.318m`, ending at `(-312.32, 24.70, 104.00)`, but did not reach the `1.35m` audit radius. Source/geometry inspection shows the generic route target is `site.X + 500`, while the upper house attached on that platform begins at `site.X + 480`; the target is inside the house and the motor correctly stops at its facade. The structural audit therefore failed before its eight structural capture frames. The remaining fix is validation-route-only: target the clear upper-platform landing before the house without changing `CharacterMotor`, solver/catalogue topology, stable IDs, or global budgets.
