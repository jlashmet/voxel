# CI operations

- 2026-08-30T10:28Z — request transport `d33869e84f033b95b74f050e135343859453fb24` admitted as run `33306559976`, then failed in **Resolve test request** before Unity. Cause: `replay_seconds` was encoded as string `"60"`; workflow contract requires an integer from 20 to 60. Requested PlayMode test and built-player capture were skipped, so this run satisfies no validation gate.
- 2026-08-30T10:30Z — corrected transport `769a14c573d8466047da716943a062d72b37b501` (source `80a35682e21bfb96cd123254159c6227945ced50`) admitted as run `33306616999`. Both requested-test and real-player build reached Unity but failed script compilation. Exact in-scope defects included readonly-`using` catalogue mutations plus an unresolved `CharacterMotor` reference. No screenshots were emitted and no validation gate passed.
- 2026-08-30T10:36Z — transport `ec4eecbc2c8cd5b78e8a39bc883280d9561f20b0` (source `9b0b3afccc1bfda92bce10e3917bcd6912030b74`) admitted as run `33306897362`. Readonly catalogue fixes compiled far enough to isolate the remaining assembly error: lower-layer `ShowcaseWorld` could not own the SceneRuntime `CharacterMotor`. Both requested-test and player build stopped at compilation; no screenshots were emitted and no gate passed. Traversal execution was moved to SceneRuntime while route/preload data remained in `ShowcaseWorld`.
- 2026-08-30T10:43Z — transport `a2f2f9bf5e6a29a7b48c78054262bd79f03bc95f` (source `b906a31e8e253e2918d87981ff8c7c9a6f87fec8`) admitted as run `33307182322`. The traversal boundary compiled far enough to isolate one remaining SceneRuntime assembly reference defect: `WorldbuildingGalleryAuditHarness` uses planner-owned `StructuralCompositionResult` and `StructuralAttachmentRejectReason` from `VoxelEngine.Structures.Runtime`, but `VoxelEngine.Showcase.asmdef` referenced only `VoxelEngine.Structures.Api`. The structural runtime has no dependency back to Showcase, so the minimal acyclic fix is to add that existing runtime assembly reference. Requested test and player build both stopped at this compile error; zero screenshots were emitted and no validation gate passed.
