# Plan

1. Audit the current structural-slot, typed-socket, decoration-socket, structure-authoring, and streaming APIs; identify the canonical path and explicitly retire or complete dormant alternatives.
2. Define the shared typed structural-socket contract and deterministic bounded composition solver, including compatibility, orientation, clearance, support, recursion, budget, inspection, and destruction/support-loss semantics.
3. Prove the production path with four concrete built-app cases: monumental gorge bridge, castle wall/tower/gatehouse, multi-level cliff settlement/platform chain, and meso-scale facade/roof attachment.
4. Add focused production-path regressions and negative cases for incompatible sockets, unsupported attachments, overlap/clearance failures, recursion/cycles, and budget violations.
5. Validate exact built application with CharacterMotor traversal, visual inspection of connection seams and monumental composition, plus blast-radius/performance/streaming measurements.