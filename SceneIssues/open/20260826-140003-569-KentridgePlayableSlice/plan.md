# Plan

- Capture/acceptance: there are no circles, so the full saved pub view is in scope. The request is readable seating at the tables/bar plus wall decoration; the previously submitted agent-9 frame was rejected because count/signature coverage did not prove physically credible furniture or layout.
- Runtime/source evidence: `KentridgeRole.Pub` already owns the production furnishing pass, but the current chair assembly was only a seat slab plus solid back, and the Pub path emitted no bar stools or paintings. The prior count-only PlayMode regression could therefore pass while the reported visual defects remained.
- Hypotheses: (H1) missing/incomplete reusable Pub assemblies are the primary defect — confirmed by production source. (H2) props already exist but are hidden by the saved camera — rejected for stools/paintings because no such program instructions existed. (H3) role/window selection is wrong — rejected by the earlier ownership trace; Pub and warm-window treatment resolve correctly.
- Fix: keep placement in `KentridgeHouseInteriorPropCatalogue`; give the existing six chairs floor-reaching supports/open backs, add three customer-side bar stools with a service gap, and mount two framed paintings on opposing side walls. Do not patch scene-specific coordinates or change unrelated roles.
- Regression: `ProductionPubFurnitureIsSupportedDecoratedAndClearOfBar` evaluates the real production catalogue and asserts chair supports, three supported stools, two separated wall-art frames/canvases, table-to-bar clearance, stool-to-bar clearance/alignment, retained bartender/shelves/windows, and no Pub prop leakage into the Inn. Existing production evaluation continues to enforce `MaxPrimitives = 256` for every role.
- Blast radius/cost: changes are Pub-only baked `EmitBox` instructions under the existing structure lifetime/streaming owner; no `Update`, new system, material, scene override, or per-frame allocation is introduced. Final acceptance requires exact-SHA PlayMode CI plus the saved-pose real-player replay and full-frame visual inspection before closure.
