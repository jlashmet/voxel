# CI operations — Kentridge meadow density

- Prior product-failure runs `33244533044`, `33246401704`, and `33246992214` were workflow-green but each failed mandatory built-player grass-motion inspection.
- Exposed-face request `4cb67bf942b5b4f9ad8834bb8c4ac92780ac84f2` / run `33247434464` failed during Unity compilation before tests or player capture. This was a product branch regression; it produced no gate evidence.
- Corrected request head `68a043abf55d453a3aa2d435aee191a6e7e6de6b` points directly at feature source `6163f48f232025efe5cf19c10c10b3762ac7fbd2`. Run `33247764440` completed successfully for `ci/single-test`, the focused Kentridge meadow acceptance test, player build, 60-second Kentridge replay, capture previews, and artifact upload. Do not replace or rewrite this completed request; it remains historical evidence.
- Run `33247764440` diagnostics: 11,469 semantic grass instances / 115,119 renderer-equivalent blades total; 5,760 / 57,724 in the primary connected meadow; 8 grass mesh chunks; zero excluded-surface grass. The isolated production-shader minimal framebuffer repro reports visible deformation, proving shader/mesh motion under explicit rendering.
- Mandatory human visual inspection still fails on the actual player replay. Late stationary foreground/ground crops are byte-identical while the sky changes. Comparing those same foreground pixels against pre-grounding run `33246992214` is also byte-identical, so translating every generated root by one voxel did not contribute a single changed foreground pixel. Experiment 004 is therefore noncausal for the visible defect.
- Because the completed workflow is green but visual acceptance is not, do **not** mark pending/fixed, do not promote to master, and do not count `33247764440` as the final feature gate.
- Next source work must first discriminate exact replay-camera coverage versus normal packed-render submission versus geometry/depth. Only after the feature SHA changes and the existing CI mailbox is idle may one fresh final exact-SHA request be created on `ci-test/fixes/agent-5`; no extra CI transport and no feature-branch `.github/test-request.json` edit.
