# CI Operations — WorldBuilder Secret Discovery Clue Generation

- `33416403719`: focused reusable interaction regression failed because the `BreakableWall` fixture inherited the production preset's `Hidden` initial state. Traced `WorldObjectSceneRuntime` behavior; corrected the fixture to explicitly author an exposed breakable route. Product runtime was unchanged.
- `33419056074`: SUCCESS on exact source `86138410045da72ecd2b23f4eca7167f4cb034e3`. Reusable mechanism + natural routes share one canonical discovery identity across interaction, snapshot/restore, revisit, and repeated activation.
- `33420376990`: SUCCESS on exact source `5c8fda4957b6d1b69c8f115e164f3228c437b822`. A real generated `CaveSecretPocket` feeds canonical WorldBuilder identity and verified voxel bypass evidence.
- `33433119715`: request/setup failure before Unity because the transport JSON used `test_name` instead of required `test`; no product test result. Corrected request schema on the same transport.
- `33433401020`: overall FAILURE on exact source `147c5661bc22b14c12409c97914f95536ecf69a0`, but the requested generated-cave test passed and automatic Kentridge built-player module validation passed. SceneIssue replay did not start because `--scene-issue` was given a bare issue id; `showcase-player-capture.sh` requires `SceneIssues/open/.../issue.json` (or another supported issue.json path). Classified as request invocation failure, not a product regression.

Next exact-head request must use `SceneIssues/open/20260830-164351-000-WorldBuilderSecretDiscoveryClueGeneration/issue.json` and validate the new reusable `CaveSecretPocketComposition` plus automatic module tests. Do not replace a queued/running request.
