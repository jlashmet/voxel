# CI Operations — WorldBuilder Secret Discovery Clue Generation

- `33416403719`: focused reusable interaction regression failed because the `BreakableWall` fixture inherited the production preset's `Hidden` initial state. Traced `WorldObjectSceneRuntime` behavior; corrected the fixture to explicitly author an exposed breakable route. Product runtime was unchanged.
- `33419056074`: SUCCESS on exact source `86138410045da72ecd2b23f4eca7167f4cb034e3`. Reusable mechanism + natural routes share one canonical discovery identity across interaction, snapshot/restore, revisit, and repeated activation.
- `33420376990`: SUCCESS on exact source `5c8fda4957b6d1b69c8f115e164f3228c437b822`. A real generated `CaveSecretPocket` feeds canonical WorldBuilder identity and verified voxel bypass evidence.
- `33433119715`: request/setup failure before Unity because the transport JSON used `test_name` instead of required `test`; no product test result. Corrected request schema on the same transport.
- `33433401020`: overall FAILURE on exact source `147c5661bc22b14c12409c97914f95536ecf69a0`, but the requested generated-cave test passed and automatic Kentridge built-player module validation passed. SceneIssue replay did not start because `--scene-issue` was given a bare issue id; classified as request invocation failure, not a product regression.
- `33434058376`: FAILURE compiling `CaveSecretPocketComposition` because `Game.Composition.CaveWorldBuilder` exposed/used `FixedList4096Bytes<>` through `CaveTraversalCandidateSet` without referencing `Unity.Collections`; asmdef corrected.
- `33439307700`: SUCCESS on exact source `b34313a163d4d0ce8b9b4a4fba2a0466d48f7fb8` for deterministic physical-conflict fallback in reusable cave secret composition; automatic module validation and exact SceneIssue replay also passed.
- `33440180807`: SUCCESS on exact source `9d95382c1ba4ca53df8f52f09d53607a66c1e07f` for reusable cave clue anchors; automatic module validation and SceneIssue replay passed. The gallery screenshot was inspected and remained unsuitable as visual clue proof.
- `33445911882`: FAILURE before any dedicated-scene screenshot. Focused test and SceneIssue build both stopped on compiler error `CaveSecretPocketCluePresentation.cs(20,36): CS0103 The name 'Coatings' does not exist in the current context`. Root cause is local to the new clue-presentation validation guard. The coating contract is a byte and zero is the no-coating sentinel, so the code now rejects `boundaryCoating == 0` without adding an unnecessary presentation-module dependency.

Next exact-head request uses only `ci-test/fixes/agent-5`, the focused `CaveSecretPocketCluePresentationTests.BoundaryEvidenceIsDeterministicAndPreservesVerifiedSeal`, and the full SceneIssue `issue.json` path. Do not replace a queued/running request.
