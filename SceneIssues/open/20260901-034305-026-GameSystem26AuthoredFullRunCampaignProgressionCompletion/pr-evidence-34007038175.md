# PR 312: green workflow is not full-run acceptance

## Exact provenance

- Feature: `dd81b895811a168b9fbfadb8a5c993acb988d2fa`; base: `356b2e0e4d2818901c73bbc6b1788f8d6850356d`.
- Checked-out synthetic PR merge: `2933925e86d83e324ec7f9ae42be78e35cb42440` (checkout log), not a newly validated feature commit.
- Workflow `34007038175`, job `101416067096` (`affected`), terminal success. Both affected EditMode and canonical standalone application steps succeeded mechanically.
- Artifact `pr-test-results`, ID `9981558493`; ZIP SHA-256 `9cac10ddab792f3da34125a78d354834364723f12e641154fb626bf244e8cf96` verified after download.

## Inspected behavioral results and coverage gap

`Artifacts/PrTests/persistent-editmode.txt`: `result_state=Passed`, `passed=652`, `failed=0`, `quarantined_failed=0`, `skipped=0`, `inconclusive=0`, `duration_seconds=24.344`. `persistent-summary.txt` reports `status=passed`; the failures file is empty.

`persistent.log` explicitly records passes for both `CanonicalCampaignRouteTests` cases and both `CampaignOutcomeIntegrationTests` cases. It contains neither `KentridgeSessionPersistenceTests` nor `StoryRuleEngineSystem26Tests`. `selection.txt`, the job's `VOXEL_CI_EDITMODE_ASSEMBLIES` and the result assembly list omit `Game.Composition.Kentridge.Tests` and `Game.Story.Tests`. Therefore this run cannot revalidate those omitted suites. Their earlier targeted-module evidence is separate, not disproved merely by omission here.

Unchanged production `tools/select-tests.py` discovers tests from `UNITY_INCLUDE_TESTS`, `optionalUnityReferences: TestAssemblies`, or NUnit/TestRunner assembly references. Both System26-owned editor-only test asmdefs lack all recognized markers. The observed selection omission is an actual metadata/discovery defect, not a guess based only on absent log names.

## Inspected standalone-player result

The build succeeded and the real player ran for 30 seconds. Both full-resolution files under `Artifacts/PrTests/FullApp/Screenshots/` are byte-identical:

- `showcase-000-t013.7s-stationary.png`
- `showcase-001-t023.7s-stationary.png`

Each PNG SHA-256 is `5fbc3dd23780422e6bd5b874abdab59506d17f194577b135ba2ed6b9c9aa6e1f`. Direct image inspection shows a black loading overlay reading **Loading Kentridge...**, not a playable world. Classification for System26 player-visible acceptance: **unacceptable**.

`FullApp/player-run.log` ends with `HARNESS done after 30.0s, assertion failures 0`; its final diagnostics retain `coverage=False`, `missingVisible=241`, `jobs=12`. It records no playable campaign or authored terminal milestone. `FRAMEPIPE` sample counts are zero and are not CPU/GPU performance proof. These observations reject this capture as readiness/full-run evidence; they do not isolate the full renderer root cause or justify changing its budgets.

T26-044/045/053 must require real production readiness, post-readiness gameplay captures and semantic milestones. Successful build/exit, zero assertion failures and two PNGs alone did not establish those conditions. Do not mark physical, terminal/frontend, multiplayer, visual or performance acceptance complete from this workflow.

## Narrow correction and local discriminator

T26-057 adds one standard `optionalUnityReferences: ["TestAssemblies"]` entry to each existing test asmdef. Production selector, gameplay code, platforms, reference lists and budgets remain unchanged. `tools/tests/test_system26_test_selection.py` invokes the real selector CLI for all-EditMode discovery and owning-runtime changes; it checks selected assemblies, run mode and graph-derived selection reasons.

Local Git fetch failed DNS. The local discriminator used a bounded snapshot of the fetched selector and four real asmdefs, not a fabricated graph or full checkout. All original Git blob hashes were verified before executing:

| Input | Original Git blob |
|---|---|
| `tools/select-tests.py` | `20662517268863caf721a35316475d8cd8812a56` |
| Story test asmdef | `22b393283b6c5d7d702acdb4e1bab8a1bb275c41` |
| Kentridge test asmdef | `cce40692a6b15d5ec6f18144bbc6cfe536796635` |
| Story runtime asmdef | `7353ca7bf96878d3328241383193375fc32f0e23` |
| Kentridge runtime asmdef | `fa7e5eddc300f921588fb23d92f104910a218240` |

Command: `python -m unittest discover -s tools/tests -p test_system26_test_selection.py -v`.

Before correction: four executed tests, four missing-selection failures, exit 1. After correction: the same four executed tests pass, exit 0; each runtime-change case selects its owned suite with a `depends on` reason rather than global fallback. Selector blob remains identical. This proves metadata selection, **not Unity/NUnit execution**. Require later exact-SHA module results for both suites before checking T26-057. The Python regression is not claimed to be automatically invoked by existing Unity workflows.
