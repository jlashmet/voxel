# 26 Authored full-run campaign progression & completion — implementation plan

## Acceptance / ownership

Complete the authored Kentridge -> Rorik/Moordell/Rossdam/Logan route, exactly-once System15 terminal/frontend aftermath, mid-run restore, shared multiplayer progression, and milestone-driven built-player full-run evidence. Story consumes semantic facts; System11 owns objectives, System15 outcomes, Systems16/14 persistence/restore. No fake regions, parallel chapter authority, alternate transport, or privileged progression shortcuts.

## Selected production path

`KentridgePlayableSlice` enters through `KentridgePlayableFullRunBootstrap`: exact rich opening geometry is overlaid by semantic identity onto hierarchy-aware `AuthoredFullRunKentridgeComposition`, while continuation coordinates come only from the recovered `TopDownWorldPhysicalPlan`. Both the shipped player and dedicated full-run validation obtain Kentridge through `Game.Kentridge.PlayableSlice.KentridgeDefinition`, which enters `WorldBuilderTownAuthoring` and preserves the exact authored town/settlement pair required by `KentridgePlayableWorldBuilderBridge`.

WorldBuilder is also affected player-visible runtime. T26-059 owns `Assets/Game/WorldBuilder/Validation/TopDownPhysicalWorld/`: production macro planning/reservations/voxel+water catalogues feed `ShowcaseWorld`, production streaming and `RenderingComposition`, surveying Moordell, Rossdam and authored water. Readiness uses only existing production signals: the target region must be `ShowcaseWorld.IsGenerated(...)` and `RenderingComposition.HasCompletePublishedNearSurfaceCoverage()` for four stable frames.

## Validation state / hypotheses

Request `3796acf10f04fe266413f0956ebbd59e954336ad` exposed stale `AuthoredTownPlan.BackendPlan` test usage; fixed. Request `7d5ee6f751e0d4d72d3061b9a4474703921a5d58` exposed two T26-059 compile-boundary defects; both were fixed without changing production semantics.

Request `cdba76adecef1ea1104cbd8c7b8a3b1f2ea4da08`, source `360cad203f30f7b771ff2c93798cfa29d4a7591c`, run `34178608752`, job `101912883262`, artifact `10040106171` (`sha256:b5291fbbdffce3eca44940d19ca76f29097f9e1ead25441a761014666656ccd1`) compiled and passed every selected persistent test assembly plus CaveWorldBuilder and Kentridge encounter players. The full-run player then failed before `physical-world-ready`: validation had called legacy `MountingForce...KentridgeDefinition.Build`, so `KentridgePlayableWorldBuilderBridge.Resolve` correctly rejected a settlement not authored through the scene-session WorldBuilder seam. Commit `c36dca64a08aa2af50ee16b97fba5a9e640d2a48` routes validation settlement/id selection through the shipped compatibility wrapper.

Request `6dfcd630443dcd8b28bcd05465cad95e0986d0d3`, source `95ac32bb3c5bd4658f0ddb5947e51ba4e88d324e`, run `34186591032`, job `101936066103`, artifact `10041593201` (`sha256:21fc664bd6ae337ee38b7ddf20a4130bd05f5542d72c9fb0cfb76ff599afb3cc`) passed the selected persistent Unity batches plus the CaveWorldBuilder and Kentridge encounter players, then the full-run player stopped before `physical-world-ready`. The exact exception was `Full-run production realization omitted a continuation cutscene stage.` Triage showed the continuation identity-only cutscenes intentionally author zero semantic stage requirements, while `BlueprintCompiler` and `CampaignBlueprint` deliberately create/derive physical cutscene stages only when choreography requires stage points. The player proof was therefore asserting a stronger, false invariant than the production contract. The validation now requires every authored stage-requiring cutscene to have a realization, and `AuthoredFullRunKentridgeCompositionTests` carries the same contract as a regression without inventing continuation choreography or fake geometry.

Request `ea8df281d99daa58f695ce5a90267ed89136ffea`, source `eb35eec2fc0bc8a426dfb9118ac3292b444bb5f4`, run `34192079529`, job `101952025472`, artifact `10043168364` proved the full System26 player route end-to-end. Its standalone log reached `physical-world-ready`, all opening/Rorik/Moordell/Rossdam/Logan milestones, `KENTRIDGE_FULL_RUN_OUTCOME disposition=Success outcome=main-campaign-complete revision=1`, `KENTRIDGE_FULL_RUN_APPLICATION_OUTCOME PASS`, and `KENTRIDGE_FULL_RUN_VALIDATION PASS`. The same automatic run then failed independently in T26-059: its WorldBuilder player passed Moordell and Rossdam publication coverage, but the water survey's visible missing-solid set stayed roughly 595-629 until the 42-second harness limit, so the required water PASS marker never appeared.

T26-059 root cause is validation residency geometry, not a campaign or water-authoring semantic failure. `ShowcaseWorld` regions span 51.2 m. The scene streamed two regions and also exposed a full two-region (102.4 m) voxel ring while its camera sits about 34 m diagonally off each survey point. Near a region boundary the visible circle can therefore cross into a third region that streaming intentionally never makes resident; `HasCompletePublishedNearSurfaceCoverage()` correctly refuses to report complete coverage while those visible solid chunks are missing. The validation now keeps one whole region as residency safety margin: with the default radius it streams 102.4 m, renders 51.2 m, and still contains the 33.9 m survey offset. It retains the same semantic four-frame production coverage gate and does not add a correctness sleep.

Next discriminator: one fresh direct-child exact-SHA automatic run. Require the already-proven full-run player to remain green, T26-059 to reach Moordell/Rossdam/water plus final PASS, and the normal shipped Kentridge integration target to pass before final independent validation is considered complete.

## External prerequisite / closure

T26-043 remains owned by System25. Its authoritative separate-process checklist still has authority/client topology, convergence, gameplay contention, reconnect/rehost, release, and final evidence incomplete. Do not duplicate that harness in System26. Close only after every task is complete, then merge current master into the feature and promote by fresh PR + auto-merge with required `affected` gate.
