# Plan

## Captured evidence / acceptance
- Pending inspection: review every capture frame/annotation and saved pose directly before editing.

## Competing hypotheses / discriminator
1. Pending after capture inspection.
2. Pending after capture inspection.

## Fix / regression
- No production change yet. First discriminate the runtime owner, then add the smallest behavioral regression through production computation.

## Blast radius / cost
- Pending owner identification; must stay scoped to the affected Kentridge playable-slice behavior.

## Remaining gates
- Inspect capture and runtime owner; discriminate hypotheses; implement/test smallest fix; replay all original poses; green exact-SHA targeted CI; commit canonical verification evidence; pending -> closed bookkeeping; merge current master; non-force push exact feature head to master.
