# CI operations

- Persistent transport: `ci-test/fixes/agent-1`.
- No final request issued yet.
- Do not modify `.github/test-request.json` on `fixes/agent-1`.
- Do not replace queued/running CI or create another transport.
- Earlier branch refresh merged `origin/master` `5865c6e04f93c7d2ba0f10258909f38115424607` at merge commit `040bc7a146672c01e9c7f9f1d165d65b3c5854e6`. Master has advanced since then; the required final current-master refresh remains pending until implementation/evidence is otherwise ready.
- The original Microsoft `Mesh008.ply` transfer path was blocked by binary transport limits. That blocker was superseded by the user-provided `mountain_dragon_supported.zip`, from which the support-free dragon and deterministic conventional OBJ source were derived. Exact third-party attribution/license metadata remains an external prerequisite because no source URL, author, or named license text was supplied.
- Live `VoxelShowcase.cs` call-site wiring is also blocked in this execution environment: the connector only replaces whole UTF-8 files, while reads of the 58 KB scene owner truncate. The reusable selection/input-consumption seam and focused regressions are committed, but the live call sites remain unchecked rather than risking unrelated source loss or adding a competing controller. Final visual validation must follow the newer module-owned validation-scene architecture instead of using the Worldbuilding Gallery/VoxelShowcase as the feature proof scene.
- 2026-08-30 run `33344325912`, CI SHA `1edc366a1faac93310a6aee3ddef58ab2f2c88d0`: `VoxelEngine.Tests.EditMode.MountainDragonSourceArchiveTests` executed one test and failed with `FormatException` while decoding the aggregate Base64 source archive. This was a product/data failure, not infrastructure.
- 2026-08-30 run `33354245312`, CI SHA `0367285e44df6245c69ee5b1025dcfd8c6254d2a`, exact feature parent `aede630b37f140050297619ce567f6ed2c701829`: after segmented-part repairs the same reconstruction test again executed one test and failed with the same aggregate Base64 `FormatException`. Per the repeated-gate rule, no further speculative payload replacement is allowed before isolating the minimal corrupt piece/boundary.
- Feature commit `85cf53b3fe5ee719afa459a481f57b4c78d7158c` added fail-closed per-piece Base64 character/padding diagnostics and aggregate length validation.
- Diagnostic run `33354408710`, CI SHA `e1e3c261bf134bd5e69e96df4a6f0b4373bf1260`, exact feature parent `85cf53b3fe5ee719afa459a481f57b4c78d7158c`: one test executed and isolated the failure to aggregate Base64 length `319999`, which is not divisible by four. The segmented `part13.00`/`part13.01` boundary was then compared against the historical unsplit `part13` blob and found lossless; the one-character loss predates that split.
- The original user upload is now available through the persistent file library and was materialized locally. Its ZIP/STL sizes and STL SHA-256 match `verification-uploaded-source.txt`, so source regeneration can use the authoritative upload instead of damaged transfer chunks.
- Feature commit `61e3ad6f4377e88af2b7ce9c488ef53862970f6f` adds a temporary focused discriminator that partially decompresses only the known-good first transfer chunk and prints the historical OBJ prefix. This is intended to reveal the exact serializer/header/vertex-order formatting so the deterministic derivation can be reproduced from the authoritative upload and the missing Base64 character recovered without guessing.
- An accidental empty-file write briefly advanced the CI transport to `ceee28201965d9808a8c6ffae0453dbee83664c9`; GitHub reported zero workflow runs for that SHA, so no queued/running CI was replaced. The CI transport is being rebuilt from the exact feature SHA for the intended prefix discriminator.
- 2026-08-30 source-tree audit before the next reconstruction gate found stale wildcard-matching repair/transfer artifacts (`part13.01fix`, `part15`, whole and segmented `part16`, and `part17`). `MountainDragonSourceArchive` deliberately consumes `mountain_dragon_clean.obj.gz.b64.part*` and fails closed on invalid suffixes, mixed whole/segmented logical parts, or non-contiguous logical indices, so those historical artifacts would deterministically prevent the intended archive from reaching its pinned hash checks. Removed only those obsolete matching artifacts. The active archive is now logical `part00`-`part14`, with logical `part13` represented by `part13.00` and `part13.01`; nonmatching historical notes/probes remain untouched.
- Exact-SHA source reconstruction, bake, dragon-anatomy regression, module-local built-player visual validation, destruction validation, and final current-master gates remain pending until source reconstruction is green and the remaining implementation tasks are complete.
