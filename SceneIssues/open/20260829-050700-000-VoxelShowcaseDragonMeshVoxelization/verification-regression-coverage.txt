Focused feature regressions present on fixes/agent-1:

MeshVoxelizationFeatureTests
- closed curved UV sphere: conservative surface extrema + solid interior
- same source/settings: deterministic repeat output and stable cell ordering
- off-origin rotation + non-uniform/mirrored transform
- deterministic source material regions + interior fallback material
- thin triangle sheet retention without global solidification
- invalid index + oversized working-set preflight
- sparse bake codec exact round-trip + malformed/out-of-bounds rejection
- canonical IStructureAuthoringSession replay at arbitrary origin
- selection scroll/Space commit state cannot duplicate across repeated idle frames

MeshVoxelizationTopologyTests
- open source explicitly falls back to surface-only and reports boundary edges
- strict open-source policy rejects with topology diagnostic
- closed source reports closed topology and filled interior

MeshVoxelizationPreflightTests
- non-finite source vertex rejected before voxelization
- non-finite transform rejected before transforming vertices

MeshVoxelizationReuseTests
- independent non-dragon closed-box fixture traverses the public MeshVoxelizer -> BakedVoxelStructureCodec -> IStructureAuthoringSession replay path without dragon-specific setup or branches

Runtime-authority review (2026-08-30)
- `ShowcaseWorld.PlaceBakedMeshStructure` accepts only `BakedVoxelStructure`; it prepares canonical regions, creates the normal structure-authoring session, replays sparse cells, and publishes canonical storage. No triangle mesh enters this runtime placement API.
- Unity mesh hierarchy/skinned-mesh extraction is isolated in `Assets/Editor/MeshVoxelization/UnityMeshVoxelizationAdapter.cs`.
- `VoxelEngine.MeshVoxelization.Editor.asmdef` has `includePlatforms: ["Editor"]` and is not an ordinary player assembly.
- Repository code search for `MeshCollider` returned no matches on current master, and this feature diff adds no `MeshCollider` use. Source mesh presentation therefore has no collider/gameplay fallback in the implemented path.
- The generic `MeshVoxelizer` implementation remains a runtime-library type so deterministic conversion logic is engine-independent/testable, but no ordinary showcase/runtime composition path invokes voxelization; authoring adaptation is editor-only and player placement consumes baked sparse cells.

These are supplemental regression/contracts and architecture evidence; they are not a substitute for the mandatory dragon-specific regression, exact-SHA CI, built-player validation, fidelity metrics, destruction truth check, or production-quality visual review.
