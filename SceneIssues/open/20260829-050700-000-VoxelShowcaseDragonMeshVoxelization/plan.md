# Plan — VoxelShowcase Dragon Mesh Voxelization

## Acceptance / ownership
Deliver a reusable conventional triangle-mesh → canonical sparse-voxel authoring path, proved on the recovered detailed curved Dragon. Runtime world truth must be only discrete authored voxels; source triangles are Editor/presentation input only. Shared importer/codec/replay/metrics remain mesh-agnostic under Structures; Dragon/source/palette/comparison policy remains Showcase composition. Independent non-dragon reuse is covered.

Built visual proof uses the module-owned standalone-player fixture with the matched `Mesh -> Voxels` exhibit and semantic ten-view capture sequence. Top-level VoxelShowcase remains integration evidence, not the feature fixture.

## Proven state / blockers
Recovered source: zip SHA `f48cab5ab5b7edf6a84cc7bf14797c73d0ac61bf597ef76a587589a4522aeb0f`; contained STL 2,144,152 triangles; deterministic support-free OBJ 29,734 triangles / SHA `f1f44d59f7d9c775b600ac0b9ad066a15a3c652bf685a12b2344b8c383ff12b1`. Exact reconstruction passed CI `33451165954`. Commercial-use permission is recorded, but original upstream URL, author, and named license text remain unavailable external acceptance blockers; do not invent them.

Exact bake generation passed CI `33451568424`: 98,100 cells, 99×107×107, 0.30 source units/voxel, 594 sparse bricks, canonical SHA `83370421048606be2dc658315ec9acc2cae39d2a7a20011151d7d561267bec41`. Runtime transport SHA `758612c8b63316e3757a7695bfdb07f99ee5709f3706c504688d657017ecc961` preserves that canonical identity. Anatomy/fail-closed regressions exist.

Exact-parent run `33476063393` proved the corrected reusable fidelity metric and required module EditMode gates on feature SHA `645b7f37979103f4a1d8b57278f786469a6d2356`: requested pinned-source fidelity passed; automatic `MountainDragonBakedArtifactTests`, pinned-source fidelity, and independent box reuse all passed. The metric now bounds query samples while measuring against the full opposite surface / continuous source triangles, avoiding the prior sparse-sample-spacing defect.

Exact-parent run `33481100955` proved the production-tier runtime path: focused Dragon tests, top-level replay, module EditMode tests, and Kentridge player completed; the Dragon player authored all 98,100 voxels with production tier-byte storage sizing and no BrickPool/runtime exception. It exposed only the scenario timing/camera evidence defect.

Two materially different visual fixes still produced the same blank/tiny authoritative viewport. Per protocol, a minimal renderer repro/root cause was isolated before a third fix: URP enqueues `VoxelRenderPass` for each non-preview camera, but one shared `VoxelSurfaceScheduler` advances clipmap/discovery/build work only for the first camera in a frame; later cameras only recollect visibility from already-known/ready chunks. The source and voxel exhibits are ~110 m apart, so source-first rendering starved the authoritative voxel viewport. `MainCamera` tagging was irrelevant.

Commit `78ac1bcc0963bf3c5e6ac80cda2614073f42be76` implements the root-cause-driven validation-only repair: create/render the voxel camera first and the presentation source camera second. Exact-parent CI run `33485072972` is fully green: requested Dragon tests, automatic module validation, Dragon/Kentridge players, top-level SceneIssue replay, previews, and final exact-SHA status all passed. Direct human inspection of all ten built Dragon frames also passes the visual hard gate: both halves are readable in matched semantic views and the voxel result preserves the same sculpture's overall proportions, head/horns, wings/negative spaces, limbs/feet, curled tail, scenic base, and required close-ups without gross missing/fused/bloated/broken anatomy.

## Current work
The next nonblocked acceptance item is built destruction/world truth plus canonical runtime storage evidence. The module fixture waits until the tenth pristine `tail-closeup` view has been held for a durable capture, then destroys a deterministic occupied torso cell through `ShowcaseWorld.Explode`, the public Showcase composition mutation seam that the SceneRuntime `ShowcaseWorldExplosionAdapter` forwards to unchanged. It asserts the exact cell is blocking before and empty after through `ShowcaseWorld.SurfaceQuery`, which is the same `IVoxelSurfaceQuery` used by `CharacterMotor.IsBlocked`. Source-mesh colliders are disabled immediately and destroyed, so no triangle collider can survive as a second collision truth. The supplemental post-edit view keeps the source intact only on the left and reframes the authoritative voxel side on the edited torso. `StoragePressure` used/capacity/critical bytes and pressure state are logged alongside placement/destruction metrics.

Exact-parent run `33488495875` did not execute destruction behavior: Unity stopped at compilation because the fixture referenced SceneRuntime-only `ShowcaseWorldExplosionAdapter`, `ShowcaseExplosionRouter`, and `ShowcaseExplosionRouteResult` from the lower-level `Game.Composition.Showcase` assembly. Both the requested EditMode step and top-level player build failed with CS0246/CS0103 before automatic module validation. This is a validation-code assembly-boundary defect, not a gameplay/runtime assertion failure. Commit `51586ddd50d0c8af4e0089242a103290ecb5bc4d` removes that illegal reverse dependency and calls the same public `ShowcaseWorld.Explode` implementation directly; no shared API or SceneRuntime dependency was added.

A separate composition defect remains: `VoxelShowcase.TryPlaceSelectedStructure` still requires optional inspector `m_MountainDragonVoxelBake` and generic-codec decoding, while `MountainDragonBakedArtifact.Load()` already owns the pinned runtime transport. Repair this in Showcase composition only; do not teach the shared codec Dragon/MDVP policy.

## Next gates
1. Re-run the corrected destruction/storage-pressure fixture on the exact current feature SHA through `ci-test/fixes/agent-1`; require the destruction log, no exceptions, and directly inspect the post-edit built frame while retaining all ten pristine comparison frames.
2. Repair VoxelShowcase placement mode to consume `MountainDragonBakedArtifact.Load()` through Showcase composition, then validate one-shot placement through the built/top-level path.
3. Continue exact-source provenance search as an external blocker without weakening acceptance.
4. Reconcile `tasks.md`, cost/blast-radius evidence, and final feature diff/reuse ownership; keep provenance and any unproven gate unchecked.
5. Only after every acceptance item is complete: merge current master, run final exact-SHA focused/module-player gates, inspect captures, move open→closed with fixed metadata, and non-force promote that exact feature head to master.
