# Plan — VoxelShowcase Dragon Mesh Voxelization

## Acceptance / ownership
`VoxelShowcase` needs a reusable **authoring-time** conventional triangle-mesh → canonical voxel path, proved on a legitimately redistributable detailed curved winged dragon. Runtime must load/replay only baked sparse cells through normal voxel storage so rendering, collision, edits, and destruction share world truth. Matched source/voxel built-player evidence must be judged `production-quality`.

## Architecture / discriminator
Hypothesis A: `IStructureAuthoringSession` and normal structure storage remain authoritative; the missing capability is deterministic offline conversion plus composition wiring. Hypothesis B: an existing arbitrary mesh/SDF importer should be generalized. Result: **A supported; B rejected.** Generic implementation remains under `Structures/Runtime/MeshImport`: transformed triangles → conservative surface coverage → bounded closed-interior fill → deterministic material ownership → ordered sparse bake/codec → canonical authoring replay. Unity hierarchy/skinned adaptation is editor-only. Shared importer/metrics APIs contain no dragon/source/showcase policy, and independent non-dragon reuse is covered.

Input ownership is also separated: `StructurePlacementSelection` owns one-shot selection/commit state and `StructurePlacementInputRouter` owns control-consumption policy. Live `VoxelShowcase` call-site wiring remains incomplete because the connected whole-file writer cannot safely edit the large canonical scene owner from truncated reads; do not bypass this with reflection or a competing controller. Built-player feature proof must use the module-owned validation-scene architecture recorded for this issue rather than the Worldbuilding Gallery/top-level showcase as a test fixture; production integration still remains required by acceptance.

## Source / blockers
The authoritative user upload has been re-located in the ChatGPT file Library as `mountain_dragon_supported.zip` (about 50.5 MB, uploaded 2026-08-30). The archive contains `mountain_dragon_supported.stl`. This resolves the prior external **source-byte availability** blocker: the agent must materialize that uploaded archive directly, verify its identity/content, and use those bytes as the source for vendoring/bake work rather than reconstructing the historical segmented repository transfer or waiting for `stanford_dragon_model.fbx` to appear on `master`.

Earlier work on the same user-provided archive identified a 2,144,152-triangle CHITUBOX STL. Connectivity cleanup retained a 1,763,914-triangle dragon/scenic-base component. Deterministic 0.5-unit vertex clustering produced a conventional OBJ with 13,431 vertices / 29,734 triangles, 860,349 bytes, SHA-256 `f1f44d59f7d9c775b600ac0b9ad066a15a3c652bf685a12b2344b8c383ff12b1`, gzip SHA-256 `fd2f8253fcf5bc32b275640448511f59d20dcc7d01c307f99124b224431892d4`. These derived identities remain evidence to verify against the newly recovered authoritative upload before reuse; do not assume a historical reconstructed transfer is authoritative merely because a derived hash was previously recorded.

Repeated reconstruction failures were isolated to repository source-transfer packaging rather than the decoder. Run `33362323833` proved the historical segmented logical `part13` reconstructed to the wrong gzip identity. Run `33364246307` proved mixed whole+segmented transport fails closed as intended. After stale segments were removed, run `33364871767` proved the remaining whole `part13` is still malformed: the archive totals 319,999 Base64 characters rather than 320,000.

Because exact-source acceptance failed after materially different reconstruction fixes, the transfer path was reduced to a minimal repro rather than mutated again. Historical/current repository variants are contradictory: the current 4,999-character `_probe13first5k` is an exact prefix of current `part13` but has Git-blob identity `ee2a84d752398b31be7d2c1f8c7884adee87dacd`, while historical `part13.00` is the different blob `d87618d052f83b18b0510951b2fef33b760f217b`. Independently staged `historical-part13.02` is 5,001 characters and the corresponding current window is that tail with one demonstrated deletion; repairing that deletion produces a 20,000-character `part13`, but the resulting complete gzip still does not match the pinned gzip/OBJ identities.

Exact-SHA CI run `33377081586`, job `99440882707`, passed `MountainDragonSourceArchiveTests.DiagnosePart13TransferLoss_HistoricalTransfersRemainNonAuthoritative`. That discriminator still stands for repository-history reconstruction, but it is no longer a blocker now that the original uploaded ZIP is available. Do not spend more work speculatively repairing segmented repository transfer; recover from the uploaded archive and validate exact bytes instead.

Exact provenance is still independently blocked: the user states the model was free and usable, but source URL, author, and named license/permission text are unavailable. Do not close licensing acceptance without them.

The STL/derived OBJ has no standard source material regions. Composition now deterministically maps unmaterialed source/interior cells to canonical `DarkStone` via `MountainDragonPalettePolicy`/`MountainDragonAuthoringPolicy`; this is explicit showcase palette mapping, not preservation of absent source color. Existing/future real source material IDs pass through unchanged.

## Current state / next work / gates
The shared importer now has explicit `VoxelShellFill` semantics independently covered by non-dragon topology regressions: topologically open input may fill only when conservative rasterization itself encloses voxel-space cells, while genuinely open rasters remain unfilled and `Reject`/`SurfaceOnly` semantics remain unchanged. The dragon composition explicitly opts into that policy at `0.30` source units/voxel with X/Z <=127, Y <=511 and no global thin-feature dilation.

Dragon source-byte acquisition is now **unblocked** by the recovered Library upload. The immediate source task is to materialize `mountain_dragon_supported.zip`, verify the contained `mountain_dragon_supported.stl` and any expected derived identities, then vendor/generate the production bake and run dragon-anatomy assertions from those authoritative bytes. Exact provenance remains a separate blocker and acceptance is unchanged.

Independent production integration also remains required: wire the canonical `VoxelShowcase` selection mode by reusing `StructurePlacementSelection` and `StructurePlacementInputRouter`, keep ordinary movement/brush controls unchanged outside selection mode, and keep scene-specific policy in the canonical composition owner. Do not create a parallel controller or reflection authority. Comparison staging, module-owned validation scene, capture integration, metrics, and destruction validation follow with the recovered dragon artifact.

Branch was last merged with `origin/master` `ebdc2e4f63ef73153cd4e0ff5c62efe604f35470`; final promotion must refresh master again. Final gates remain: exact provenance, exact baked artifact, production selection/comparison integration, quantitative metrics/cost, built-player destruction/world truth, all required matched captures, and direct `production-quality` human visual review. Only after all gates pass should master be refreshed, the final exact-SHA requests be issued through `ci-test/fixes/agent-1`, the issue move `open` → `closed`, and the exact feature head be promoted non-force.
