# CI operations

- Request `c11d015e…`: focused PlayMode test passed and the 45-second built-player replay reached `missingVisible=0`, but direct screenshot inspection still showed the upper rectangular grass tongue. Rejected as visual failure.
- Request `423ee5d9…`, workflow `33176475634`, attempt 1: infrastructure failure because an interactive Unity editor was open; requested regression was skipped. Diagnostic player replay completed. Used the one allowed infrastructure retry on the same request.
- Same request, attempt 2: requested regression executed and failed because it assumed an inactive civic-west court placement. The built-player replay again reached full residency; marked-ground pixels were unchanged from the prior replay, falsifying the civic terrace/court hypothesis.
- Request `c63244e4…`, workflow `33205371873`: failed before Unity in `Resolve test request` because scene-issue requests require `platform=PlayMode`. No product code or harness ran.
- Request `084f9401…`, workflow `33205557129`: request resolution succeeded, but the moved PlayMode regression failed compilation because it referenced `StructureArchetype` outside the test assembly. The test dependency was repaired without changing production geometry.
- Request `d0dbaeeb…`, workflow `33206033751`, source `636f6120…`: startup bake passed, focused PlayMode regression passed, and the 45-second real-player replay completed, but the workflow hit its hard five-minute timeout and was cancelled, so it is not a valid gate. Artifact comparison showed the rendered ground plane pixel-identical to the earlier rejected replay, falsifying organic plot grading as the visual owner.
- Composition/source follow-up: exact seed `1592594996` selects the organic Kentridge backend. District terrace/civic correction stages are absent from the rendered catalogue. The live `KentridgeDirectedTownSurfaceCatalogue` rasterizes routes as overlapping axis-aligned square carve/fill stamps; live route bounds overlap the corrected upper marked envelope. Final candidate replaces only those square stamps with equal-radius vertical cylinders.
