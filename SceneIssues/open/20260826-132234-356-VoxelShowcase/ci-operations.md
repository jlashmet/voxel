# CI operations

- Request `c11d015e…`: focused PlayMode test passed and the 45-second built-player replay reached `missingVisible=0`, but direct screenshot inspection still showed the upper rectangular grass tongue. Rejected as visual failure.
- Request `423ee5d9…`, workflow `33176475634`, attempt 1: infrastructure failure because an interactive Unity editor was open; requested regression was skipped. Diagnostic player replay completed. Used the one allowed infrastructure retry on the same request.
- Same request, attempt 2: requested regression executed and failed because it assumed an inactive civic-west court placement. The built-player replay again reached full residency; marked-ground pixels were unchanged from the prior replay, falsifying the civic terrace/court hypothesis.
- Request `c63244e4…`, workflow `33205371873`: failed before Unity because scene-issue requests require `platform=PlayMode`.
- Request `084f9401…`, workflow `33205557129`: request resolution succeeded, but the moved PlayMode regression failed compilation; repaired without changing production geometry.
- Request `d0dbaeeb…`, workflow `33206033751`, source `636f6120…`: bake/test/player succeeded but the workflow hit its hard timeout, so it was not a valid gate.
- Request `agent8-132234-final-57006268`, workflow `33214166946`, source `57006268…`: route-overlap regression failed; zero live route placements intersect the corrected upper envelope. Lower mark improved; upper tongue remained.
- Workflow `33215984995`, source `a09f2897…`: exact plot regression and built player were green, but the upper rectangle remained, rejecting the archetype-pad candidate visually.
- Request `agent8-132234-precedence-final-36eb66c5`, workflow `33225240544`, source `36eb66c5…`: forced bake/player capture succeeded; focused test failed because no route intersects the marked WideHouse-pad region. Screenshot remained visually unchanged. Product failure; precedence hypothesis rejected. Next correction derives organic generated-house grading from the exact resolved foundation rather than an archetype-wide pad.
- Request `agent8-132234-foundation-final-c4f4ec8b`, workflow `33225920872`, source `c4f4ec8b…`: cache miss forced a fresh Showcase bake, but both bake and player build stopped on CS0246 in the new regression (`SettlementPlan`, `BuildingPlot`, `Int3`). Product test-harness failure; production geometry was not executed. Added the missing `MountingForce.WorldGen` import before the next exact-source request.
- Workflow `33226248513`, source `ecec9b71119ff2dfbe250c5d8b6d7d994193e11b`, CI transport `30d6d3090f0a0820f02c42de3f90aa8af2530348`: focused regression passed, a WorldBuilder-aware cache miss forced a fresh `ShowcaseWorld.bytes` bake, the real `VoxelShowcase` player built, and the exact 1928×836 saved-pose replay reached stable residency. Direct inspection rejected the result: the upper marked region still contained the same hard 90-degree grass-topped grading corner, and pixels inside both original circles were byte-for-byte unchanged from workflow `33225240544`. Green CI did not satisfy the visual gate; the unrotated generated-pad regression was subsequently found to be a false model of MayorHouse's production orientation.
- Request on CI transport `ab2786e812d71e580fccabd9ab9b1f5ceee9b0d2`, workflow `33230675179`, exact feature source `6557e9c90b771c02a21424c926d158732fdf0450`: product compile failure before the geometry/regression could execute. The new rounded-surface helper referenced undefined `SurfaceStyles` and `Coatings`; the shape VM contract uses raw style/coating operands, and the existing box helper already supplies default `0, 0`. Repaired only those bytecode defaults; no geometry or regression semantics changed.
- Request `agent8-132234-rounded-final-ed933466`, workflow `33231174953`, exact feature source `ed9334667bee230900a1012770ee9749b31d7e0f`: forced bake, focused PlayMode regression, real-player build, and exact saved-pose replay all completed successfully and reached stable residency, but the aggregate job was cancelled at its hard timeout and therefore was not a green gate. Direct artifact inspection rejected the product result anyway: the lower circle was substantially smoother, while the upper circle still showed a metre-scale axis-aligned grass edge. Camera projection proved the old southwest pad corner had changed to Dirt, but the fixed 1.2m rounded-cap tangent landed at about `Z=29.8m`, inside the original upper marked envelope. The cap was functioning but too small; replace only its visible plan footprint with a larger bounded curve before the final request.
