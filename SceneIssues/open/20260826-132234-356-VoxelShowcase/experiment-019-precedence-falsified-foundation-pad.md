# Experiment 019 — precedence falsified; generated foundation isolated

## Runtime evidence
Final request `agent8-132234-precedence-final-36eb66c5`, workflow `33225240544`, tested source `36eb66c54017756431da5552b2a9c7344f45cd1b`. Forced bake and real-player capture succeeded. The focused regression failed because **no organic route placement intersects the WideHouse pad inside the corrected upper marked envelope**. The replay screenshot is visually unchanged from the prior rejected source, so lowering plot precedence had no causal effect.

## Discriminator
The upper mark remains inside MayorHouse's semantic parcel, but the current supposedly bounded WideHouse pad is still an archetype-wide rectangle `(x=6,z=4,w=116,d=100)` dm. The production architecture compiler resolves MayorHouse itself to a generated `98×86` dm foundation centered at local `x=17` dm with the shared-house front inset `z=10` dm. Thus the pad begins 1.1 m before the real foundation and visibly grades ground that the building does not occupy. Representative marked point `(92.0m,29.5m)` is inside the old pad but outside the generated foundation.

## Verdict
Revert the precedence candidate. For organic Kentridge generated houses only, make each plot pad role-specific and derive its rectangle from the same resolved `StructureForm` consumed by the shared-house compiler. Bespoke landmarks and legacy/non-Kentridge archetype pads remain unchanged. Regression must prove the exact MayorHouse pad equals its generated foundation, `(92.0m,29.5m)` stays natural, and organic route stamps remain round.
