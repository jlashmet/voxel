# Tasks

- [x] Trace the existing road profile/corridor, road-influence, SmoothSurface material/reconstruction, vegetation, junction, streaming/LOD, and persistence paths; record the demonstrated presentation root cause and affected consumers. Root cause: semantic influence and voxel lowering were unions of straight segment-local corridor fields; the generic rasterizer lacked continuity/topology/cross-section/wear semantics, while SmoothSurface only reconstructs/shades the resulting packed generic voxel payload and cannot recover them. Existing Kentridge and TopDown network consumers use the shared path.
- [x] Implement the narrowest reusable semantic/config-driven road-presentation extension for coherent curve/edge geometry and bounded terrain-aware cross-section/shoulders/cut-fill without changing route authority. `WorldRoadPresentationPath` refines only presentation geometry; resolved route points remain authoritative, and the generic corridor now carries bounded crown/shoulder shaping.
- [x] Add deterministic shared terrain-surface wear/variation and topology-aware junction presentation through existing production material/surface contracts; keep non-road consumers unchanged. Wear remains in existing packed surface-detail semantics; exact resolved network junctions are now passed through semantic influence, local-frame sampling, clearance and physical lowering instead of inferred from proximity.
- [x] Prove reuse with an independent non-Kentridge road/trail/profile consumer or fixture where practical; prefer existing TopDown road construction. Generic `Trail`/custom-profile regression fixtures exercise the same presentation path and terrain-corridor contract used by TopDown without Kentridge policy.
- [x] Fix the demonstrated built-player catalogue overflow without weakening `FeatureBudget.MaxDefinitions`. Run `33340511339` failed `TooManyDefinitions`; turn refinement was bounded and later run `33345875621` built/replayed without overflow.
- [x] After two materially different generic fixes left the same built-player trench symptom, isolate a minimal production repro/root cause before another fix. Run `33362012059` proved combined organic Kentridge wrote road grading before plot surfaces, allowing later plot carve/fill to overwrite a grading-only point.
- [x] Correct the Kentridge writer-order regression and exact-SHA revalidate it. Organic Kentridge finishes plot surfaces before the authoritative road pass; run `33369999858` passed focused regression, automatic module validation, and standalone player replay.
- [x] Isolate the remaining built-player trench/channel after the writer-order fix before another production geometry change. Human inspection rejected run `33369999858`; diagnostic run `33370815109` measured an 8 dm raised plot feather on the actual public approach for role 14, isolating Kentridge parcel frontage authoring.
- [x] Fix and exact-SHA revalidate only the demonstrated remaining road/plot presentation cause. `KentridgePlotSurfaceCatalogue` finishes the bounded canonical frontage strip at core elevation, preserving side/rear terrace feather. Post-master run `33375836636` exposed a compatibility regression: master’s reservation-aware composition restored the solved organic road write before plots and failed `OverlappingPlotLandformsMustRunBeforeRoadGrading` (road index 6, expected after plot index >257). Production commit `cdc0a5755edbf72a65f5cd0b2e336606df81661a` preserves master’s shared solved road/reservation snapshot but defers only physical `BuildResolvedRoadNetwork(...)` until after plot surfaces. Exact feature SHA `8e6c10bcb704402ce7e7bbf41dac37c1f97eb8ef` run `33378705492` passed both `OrganicPublicApproachMustNotCrossRaisedPlotLip` and `OverlappingPlotLandformsMustRunBeforeRoadGrading`, automatic `spatial-reservations` tests, and standalone `kentridge-integration` + reservation player replays. The generic SceneIssue replay remains an opening-interior view and is not counted as final road visual acceptance.
- [x] Resolve the road-specific evidence-harness timing blocker. Exact run `33416931897` completed successfully with six full-resolution Kentridge captures through `t=73.0s`; late `t=63.0s` and `t=73.0s` frames are genuine player-height CharacterMotor traversal evidence.
- [x] Diagnose and document the newly exposed visual production defect before another fix. Human review of run `33416931897` shows repeated flat bands, endpoint/capsule transitions, doubled crown/shoulder ridges and terraced edges at internal curve pieces and a real junction. Code inspection shows semantic `WorldRoadInfluence` already selects the best closest-point sample over the full presentation polyline, while physical `BuildPieces` rasterizes independently clamped full cross-sections whose overlap result depends on decomposition/write composition. Whole-decimetre cross-section quantization is recorded as a secondary hypothesis only.
- [x] Add a minimal generic failing regression proving physical partition/order invariance for one presentation road plus a real shared-vertex junction fixture. Exact run `33421466465` compiled and executed both fixtures on the real storage/mutation/raster path; the internal turn and three-piece junction each changed final top surface from 16 to 15 solely by reversing incident piece order, proving the intended decomposition defect.
- [x] Implement only the reusable continuous-corridor correction: select the physical closest-point winner over the whole presentation polyline/network field and evaluate one grade/cross-section/coverage sample; keep bounded pieces as storage/execution partitions only. No endpoint fade heuristic or road-specific rasterizer vocabulary was added. `FeatureRegionBuild` now arbitrates contiguous terrain-corridor primitives as one bounded physical stage so write order cannot select a different endpoint-clamped cross-section. Exact pre-master run `33422342809` passed the two new continuous-field regressions and automatic required module validation.
- [ ] Exact-SHA validate focused production-path regressions covering deterministic semantics, representative profiles/sides/junctions, material wear, exposed-top/slope behavior, vegetation, chunk/LOD continuity, persistence/vertex budgets, non-road behavior, and Kentridge road/plot composition. Current exact feature head after merging master is `79fbed25b9dbbe9dc54dd8bf2ab8a49fcbb32636`; pre-merge run `33422342809` is supporting evidence only and must not substitute for post-merge exact-SHA validation.
- [ ] Measure relevant world-build/voxel/vertex/material/primitive/streaming cost against existing budgets; do not weaken budgets. Current bounded delta: frontage fix adds at most three landform primitives per affected plot definition (`MaxPrimitives` 39 -> 42); the post-master writer-order compatibility fix adds no catalogue content. Continuous-field arbitration adds no persisted vertex/program fields or corridor primitives; exact run must confirm runtime/module budgets remain green.
- [x] Merge current `origin/master` when compatibility requires it and before final exact-SHA validation. Latest merge `79fbed25b9dbbe9dc54dd8bf2ab8a49fcbb32636` incorporates master `b64e9456b150b374dc950ab11f02a2427412cc66` as a true second parent. Refresh master again before final promotion and merge/revalidate if it advances.
- [ ] Validate exact built `KentridgePlayableSlice` player evidence: curved/diagonal approach, both uneven/sloped shoulders, real junction/approach, non-flat cross-section, medium/far continuity, vegetation recovery, and CharacterMotor traversal; explicitly verify that the in-town plot overlap no longer reintroduces the trench. Run `33416931897` now supplies valid player-height evidence but visually fails this gate because curve/junction piece seams and terraced cross-section bands remain; a fresh exact built-player result is required after the continuous-field correction.
- [ ] Review final diff and complete pre-gate metadata fields that are supported by the implementation/validation result; keep this assignment in `open/`.
- [ ] Request targeted CI for the exact feature SHA using only `ci-test/fixes/agent-3`; leave queued/running CI untouched and resolve any completed failure before retrying. Post-merge request is being prepared from exact head `79fbed25b9dbbe9dc54dd8bf2ab8a49fcbb32636` only.
- [ ] After all exact-SHA gates are green, set `status=fixed`, `resolvedUtc`, `resolutionSummary`, `regressionTest`, and `fixCommit`, move only this assignment directly from `open/` to `closed/`, merge current `origin/master`, revalidate affected work if needed, and push the exact feature head to `origin/master` non-force.
