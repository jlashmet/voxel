# Tasks

- [x] Trace the existing road profile/corridor, road-influence, SmoothSurface material/reconstruction, vegetation, junction, streaming/LOD, and persistence paths; record the demonstrated presentation root cause and affected consumers. Root cause: semantic influence and voxel lowering were unions of straight segment-local corridor fields; the generic rasterizer lacked continuity/topology/cross-section/wear semantics, while SmoothSurface only reconstructs/shades the resulting packed generic voxel payload and cannot recover them. Existing Kentridge and TopDown network consumers use the shared path.
- [x] Implement the narrowest reusable semantic/config-driven road-presentation extension for coherent curve/edge geometry and bounded terrain-aware cross-section/shoulders/cut-fill without changing route authority. `WorldRoadPresentationPath` refines only presentation geometry; resolved route points remain authoritative, and the generic corridor now carries bounded crown/shoulder shaping.
- [x] Add deterministic shared terrain-surface wear/variation and topology-aware junction presentation through existing production material/surface contracts; keep non-road consumers unchanged. Wear remains in existing packed surface-detail semantics; exact resolved network junctions are now passed through semantic influence, local-frame sampling, clearance and physical lowering instead of inferred from proximity.
- [x] Prove reuse with an independent non-Kentridge road/trail/profile consumer or fixture where practical; prefer existing TopDown road construction. Generic `Trail`/custom-profile regression fixtures exercise the same presentation path and terrain-corridor contract used by TopDown without Kentridge policy.
- [x] Fix the demonstrated built-player catalogue overflow without weakening `FeatureBudget.MaxDefinitions`. PlayMode/player run `33340511339` at feature SHA `f30c398c17b925fd331f57c8c8c70d44f383779e` failed `Combined settlement catalogue failed validation: TooManyDefinitions`; presentation refinement was multiplying each resolved turn into multiple bounded corridor definitions. The implementation now bounds each non-junction turn to one entry/exit chamfer, and later exact-head player run `33345875621` successfully built/replayed Kentridge without catalogue overflow.
- [x] After two materially different fixes left the same built-player trench symptom, isolate a minimal production repro/root cause before another fix. `KentridgeRoadCompositionRegressionTests` finds a real fixed-seed plot landform overlapping only a road grading envelope; focused run `33362012059` at feature SHA `a0d1905ec1ff1a61efccb06a7f1c6dd3ad785f7d` failed because combined organic Kentridge writes road grading before plot surfaces, allowing the later plot carve/fill to overwrite the correctly sampled road grade.
- [ ] Fix and exact-SHA revalidate the road-shaping defect demonstrated by the built-player evidence. The generic dual-envelope corridor is frozen; the root-cause fix is scene-specific composition only: organic Kentridge now finishes plot-surface landforms before the authoritative `KentridgeDirectedTownSurfaceCatalogue` road pass, while legacy authored ordering is unchanged. Exact run `33366463644` could not test the fix because the new regression omitted `using Game.WorldBuilder.Api;`, producing CS0246 for `WorldRoadNetwork`; that test-only compile defect is now fixed. Focused, module-derived, budget, and visual validation remain required before this task is complete.
- [ ] Exact-SHA validate focused production-path regressions covering deterministic semantics, representative profiles/sides/junctions, material wear, exposed-top/slope behavior, vegetation, chunk/LOD continuity, persistence/vertex budgets, non-road behavior, and the Kentridge plot/road writer-order regression.
- [ ] Measure relevant world-build/voxel/vertex/material/primitive/streaming cost against existing budgets; do not weaken budgets.
- [x] Merge current `origin/master` when compatibility requires it and before final exact-SHA validation; `b3cd098a1910ff23ebee96bf76270c24cd2823f4` merged observed master `2ea5f5c95f89fbf0403dbefb50b782829583d304`. Refresh master again before final promotion and merge/revalidate if it advances.
- [ ] Validate exact built `KentridgePlayableSlice` player evidence: curved/diagonal approach, both uneven/sloped shoulders, real junction/approach, non-flat cross-section, medium/far continuity, vegetation recovery, and CharacterMotor traversal; explicitly verify that the in-town plot overlap no longer reintroduces the trench; classify visual quality at the AAA bar and fix demonstrated defects before proceeding.
- [ ] Review final diff and complete pre-gate metadata fields that are supported by the implementation/validation result; keep this assignment in `open/`.
- [ ] Request targeted CI for the exact feature SHA using only `ci-test/fixes/agent-3`; leave queued/running CI untouched and resolve any completed failure before retrying.
- [ ] After all exact-SHA gates are green, set `status=fixed`, `resolvedUtc`, `resolutionSummary`, `regressionTest`, and `fixCommit`, move only this assignment directly from `open/` to `closed/`, merge current `origin/master`, revalidate affected work if needed, and push the exact feature head to `origin/master` non-force.
