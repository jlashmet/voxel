# Tasks

- [x] Trace the existing road profile/corridor, road-influence, SmoothSurface material/reconstruction, vegetation, junction, streaming/LOD, and persistence paths; record the demonstrated presentation root cause and affected consumers. Root cause: semantic influence and voxel lowering are unions of straight segment-local corridor fields; the generic rasterizer has no continuity/topology/cross-section/wear inputs, while SmoothSurface only reconstructs/shades the resulting packed generic voxel payload and cannot recover those missing semantics. Existing Kentridge and TopDown network consumers are affected by the shared path.
- [ ] Implement the narrowest reusable semantic/config-driven road-presentation extension for coherent curve/edge geometry and bounded terrain-aware cross-section/shoulders/cut-fill without changing route authority.
- [ ] Add deterministic shared terrain-surface wear/variation and topology-aware junction presentation through existing production material/surface contracts; keep non-road consumers unchanged.
- [ ] Prove reuse with an independent non-Kentridge road/trail/profile consumer or fixture where practical; prefer existing TopDown road construction.
- [ ] Add focused production-path regressions covering deterministic semantics, representative profiles/sides/junctions, material wear, exposed-top/slope behavior, vegetation, chunk/LOD continuity, persistence/vertex budgets, and non-road behavior.
- [ ] Measure relevant world-build/voxel/vertex/material/primitive/streaming cost against existing budgets; do not weaken budgets.
- [ ] Merge current `origin/master` when compatibility requires it and before final exact-SHA validation; revalidate affected work after merge.
- [ ] Validate exact built `KentridgePlayableSlice` player evidence: curved/diagonal approach, both uneven/sloped shoulders, real junction/approach, non-flat cross-section, medium/far continuity, vegetation recovery, and CharacterMotor traversal; classify visual quality at the AAA bar and fix demonstrated defects before proceeding.
- [ ] Review final diff and complete pre-gate metadata fields that are supported by the implementation/validation result; keep this assignment in `open/`.
- [ ] Request targeted CI for the exact feature SHA using only `ci-test/fixes/agent-3`; leave queued/running CI untouched and resolve any completed failure before retrying.
- [ ] After all exact-SHA gates are green, set `status=fixed`, `resolvedUtc`, `resolutionSummary`, `regressionTest`, and `fixCommit`, move only this assignment directly from `open/` to `closed/`, merge current `origin/master`, revalidate affected work if needed, and push the exact feature head to `origin/master` non-force.
