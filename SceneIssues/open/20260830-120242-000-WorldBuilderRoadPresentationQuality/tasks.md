# Tasks

- [x] Trace shared road profile/influence/presentation/voxel lowering, vegetation, junction, chunk/LOD, persistence, and rendering paths; keep resolved route/topology authoritative.
- [x] Add reusable presentation refinement, formed crown/shoulders/cut-fill, deterministic wear, exact-topology junction handling, and independent Trail/custom-profile reuse coverage without Kentridge policy in shared APIs.
- [x] Fix the demonstrated catalogue overflow without weakening `FeatureBudget.MaxDefinitions`; bounded refinement later built/replayed successfully.
- [x] After two generic fixes left the same trench symptom, isolate Kentridge composition causes before more geometry changes. Runs `33362012059` and `33370815109` proved plot writer order and an 8 dm frontage feather were separate production causes; both received narrow composition fixes and exact-SHA regression coverage.
- [x] Resolve road-specific built-player evidence timing. Run `33416931897` produced genuine player-height CharacterMotor captures through `t=73s`.
- [x] Diagnose the exposed internal curve/junction seam defect. Generic run `33421466465` proved final top surface changed 16 -> 15 solely by reversing incident corridor piece order.
- [x] Implement continuous-corridor physical arbitration. `FeatureRegionBuild` batches contiguous corridor-only stages and chooses one closest physical candidate per x/z column before mutation; bounded pieces remain storage/execution partitions. No endpoint fade, route rewrite, Kentridge coordinates, persisted vertex growth, program-width growth, or extra corridor primitives.
- [x] Merge current master for compatibility. Merge `79fbed25b9dbbe9dc54dd8bf2ab8a49fcbb32636` incorporates master `b64e9456b150b374dc950ab11f02a2427412cc66`; refresh again before final promotion.
- [x] Exact-SHA validate the continuous-field correction after the merge. Feature SHA `0f40e94119c9222dd4242d16734319c6ad2a6c40`, run `33430943763`: attempt 1 passed the requested 2-test regression then hit a Unity SIGSEGV 139 starting an automatic module test at low RSS; the proven infrastructure failure was retried in place. Attempt 2 succeeded: requested continuous-field tests, all repository-derived `spatial-reservations` tests, and both `kentridge-integration`/`spatial-reservations` standalone players passed.
- [x] Inspect fresh exact built-player evidence after continuous-field arbitration. `t=64s`/`t=74s` show the repeated piece-end/capsule bands materially reduced, but visual acceptance still fails on long crown/shoulder ridges and abrupt lateral terraces through the foreground curve/junction.
- [x] Add a minimal generic failing voxel-precision regression for the residual stair-step before another production fix. Exact run `33431993454` executed three continuous-field tests; the two partition/junction invariants stayed green while `PhysicalCrossSectionMustNotTurnOneDecimetreCrownIntoMultiVoxelTerrace` failed on the intended discriminator with adjacent physical targets differing by 4 voxels where the invariant allows at most 1. This proves whole-decimetre cross-section rounding is a separate residual physical defect.
- [x] Move only physical cross-section interpolation to voxel precision while preserving rounded semantic dm contracts, coverage/wear/vegetation, route authority, grading bounds, and deterministic behavior. `TerrainCorridorRasteriser` now keeps centreline/coverage sampling in the authored dm grid but evaluates crown/shoulder vertical offsets in voxel units before the final target is formed; no persisted/program representation change was introduced. Exact validation is pending on feature SHA `4670c82dc2b2f8e4e036d6c7dc13f70c864df364`.
- [ ] Re-run focused road regressions plus repository-derived automatic module/player validation on the exact new head. Confirm representative profiles/sides/junctions, wear, exposed-top/slope behavior, vegetation, chunk/LOD continuity, Kentridge road/plot composition, and non-road behavior. Current request commit `163e8082edcfcea9765af9b50ddf5c4342f0aebf` targets exact feature SHA `4670c82dc2b2f8e4e036d6c7dc13f70c864df364`; run `33434127106` is queued and must not be replaced.
- [ ] Measure relevant world-build/voxel/vertex/material/primitive/streaming cost against existing budgets; do not weaken budgets. Current continuous-field implementation adds no persisted vertex/program fields or corridor primitives; frontend correction remains bounded at `MaxPrimitives` 39 -> 42 for affected plot definitions.
- [ ] Validate final built `KentridgePlayableSlice` evidence: curved/diagonal approach, both uneven/sloped shoulders, real junction, non-flat but smooth cross-section, medium/far continuity, vegetation recovery, CharacterMotor traversal, and no reintroduced in-town frontage trench.
- [ ] Review final diff and complete supported pre-gate metadata fields; keep assignment in `open/` until every acceptance criterion is green.
- [ ] Before closure, refresh/merge current `origin/master`; any merge invalidates exact-SHA gates and requires revalidation. Use only `ci-test/fixes/agent-3` for targeted CI and never replace queued/running work.
- [ ] After all exact-SHA gates are green, set `status=fixed`, `resolvedUtc`, `resolutionSummary`, `regressionTest`, and `fixCommit`, move only this assignment directly `open/` -> `closed/`, merge current master, revalidate if needed, and push the exact feature head to `origin/master` non-force.
