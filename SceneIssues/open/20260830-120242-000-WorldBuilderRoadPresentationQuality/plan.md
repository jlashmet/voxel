# Plan

## Acceptance
Improve the authoritative `WorldRoadNetwork` presentation without changing route/topology authority: coherent curved/diagonal edges; bounded formed cross-sections/shoulders/cut-fill; deterministic shared terrain-surface wear; topology-aware junctions; stable chunk/LOD continuity; vegetation/material/collision/destruction semantics preserved; costs within existing budgets; and exact built-player AAA validation in `KentridgePlayableSlice`.

## Current architecture / ownership
`WorldRoadProfile` + `WorldRoadResolver` own semantic route/profile intent. `WorldRoadPresentationPath` is a deterministic presentation-only refinement; `WorldRoadInfluence` and `WorldRoadNetworkVoxelCatalogue` consume it while leaving resolved route points unchanged. Each bounded piece still emits one generic `EmitTerrainCorridor`; `TerrainCorridorRasteriser` evaluates the shared continuous influence, bounded crown/shoulder profile and deterministic surface-detail wear into voxel-authoritative terrain. `SmoothSurfaceVertex` remains 32 bytes and `SmoothSurface.shader` consumes the existing generic packed material/style/coverage channels. Kentridge and TopDown remain independent network consumers.

## Root cause / demonstrated defects
The original geometry defect originated before SmoothSurface: semantic influence and physical lowering were unions of segment-local straight corridor fields, so turns and true junctions could not receive continuous tangent or topology-aware shaping. The rasterizer also lacked crown/shoulder/wear response. SmoothSurface only reconstructs and shades the resulting generic voxel/material payload, so shader-only work could not recover those missing semantics.

A follow-up architecture pass found one correctness gap in the first implementation: `WorldRoadPresentationPath` supported explicit junction preservation, but production network sampling and voxel lowering called its route-only overload, so an interior point shared by another route could still be rounded away. The production path now passes the network's exact resolved-vertex junction set into semantic influence, local-frame sampling, clearance sampling, and physical piece lowering. Nearby overlap still cannot invent topology.

Exact built-player run `33340511339` at feature SHA `f30c398c17b925fd331f57c8c8c70d44f383779e` demonstrated a catalogue-budget regression (`TooManyDefinitions`): four quadratic presentation subdivisions multiplied every resolved turn into too many corridor definitions. The implementation now bounds each ordinary turn to one entry/exit chamfer without raising `FeatureBudget.MaxDefinitions`. Revalidation run `33340845028` at feature SHA `9de01e72d44d1a66dcf07852dd6101da105e2806` built and rendered the real Kentridge world with nonzero surface/streaming telemetry, proving that overflow is removed. Its editor-side PlayMode assertion later failed on an unrelated story-NPC interaction-range check; that adjacent system is out of scope and will not be changed for this feature.

The same built-player artifact exposes remaining road-presentation defects rather than satisfying visual acceptance: the survey view still shows broad puddle-like junction/shoulder unions and visibly faceted road silhouettes. The current 60-second replay also reaches only interior dialogue at the player-height captures before switching to survey view, so close road/traversal evidence remains incomplete. A concrete semantic inconsistency is under investigation: route composition carries an explicit `ShoulderWidthDm`, while semantic influence and physical lowering currently use the broader `WorldRoadProfile.TransitionWidthDm`; Kentridge config makes those materially different, which can widen recovery/junction unions beyond the authored road surface intent.

## Selected approach
Keep the existing semantic/profile -> presentation path -> bounded corridor -> packed-surface production path. Curve refinement and cross-section shaping are reusable deterministic semantics, not Kentridge geometry. Junction behavior comes only from resolved network connectivity. Generic corridor wear stays in the existing persisted surface-detail channel; no road-only shader/material-instance island or vertex/storage growth is introduced.

First make semantic influence and physical lowering consume one explicit route presentation shoulder width consistently, using existing route configuration rather than scene IDs. Add a focused fixture where profile transition and presentation shoulder differ, so the boundary is regression-proven independently of Kentridge. Then address the demonstrated faceting within the fixed definition budget, preferring bounded presentation-only simplification of resolver micro-turns over additional per-turn subdivisions. Exact junction vertices and authoritative resolved points must remain untouched.

## Blast radius / cost expectations
WorldBuilder road API/lowering, generic terrain-corridor sampling/rasterization, packed terrain surface response, and focused regressions are in scope. Storage format and `SmoothSurfaceVertex` remain unchanged. Do not raise catalogue/storage/vertex budgets. Keep one bounded corridor primitive per physical piece where practical; reducing resolver micro-turns is preferable to multiplying definitions. Measure definitions, voxel writes, vertex/material, world-build and streaming/LOD costs against existing budgets.

## Current validation state
An earlier exact-SHA EditMode run `33338976847` was green for nine focused `WorldRoadPresentationRegressionTests`, including topology, deterministic wear, sloped/chunk-independent shoulders, vegetation recovery, generic trail reuse, non-road isolation, and storage/vertex budgets. The feature changed after that run, so final exact-SHA EditMode validation is still required. Player run `33340845028` proves real-world generation after the catalogue fix but does not meet close visual acceptance; its unrelated NPC assertion is recorded as a gate-selection blocker rather than feature work.

## Current commit
Feature head is `9de01e72d44d1a66dcf07852dd6101da105e2806`; last observed `origin/master` was `ebdc2e4f63ef73153cd4e0ff5c62efe604f35470`. Refresh master before compatibility merge/final gates.

## Remaining gates
Fix the demonstrated presentation-width inconsistency and faceting without weakening budgets; extend focused regression coverage for those production boundaries; rerun exact-SHA structural/cost gates; obtain built-player player-height curve/shoulder/junction/non-flat/far-field/vegetation/traversal evidence through the allowed transport; perform the human `production-quality` review; merge current master and revalidate affected work; then direct open -> closed metadata/bookkeeping and non-force promotion to master.
