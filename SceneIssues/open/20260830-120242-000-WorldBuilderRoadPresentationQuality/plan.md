# Plan

## Acceptance
Improve authoritative `WorldRoadNetwork` presentation without changing route/topology authority: coherent curved/diagonal edges; formed carriageway/shoulders with bounded cut/fill recovery; deterministic shared wear; topology-aware junctions; stable chunk/LOD continuity; preserved vegetation/material/collision/destruction semantics; existing budgets; and exact built-player AAA validation in `KentridgePlayableSlice`.

## Root cause and evidence
The original road geometry was a union of straight segment-local influence fields. That prevented continuous turn/junction shaping before `SmoothSurface`; shader work could not reconstruct missing physical semantics. Presentation refinement and explicit resolved-vertex junction preservation address that reusable geometry defect without rewriting resolved route authority.

Run `33340511339` at `f30c398c17b925fd331f57c8c8c70d44f383779e` exposed `TooManyDefinitions` after over-subdividing turns. Ordinary turns were bounded to one entry/exit chamfer. Run `33345875621` at `4cb2625f1696ebfe4bbb26d044eed08b0e25e21e` subsequently built/replayed Kentridge successfully with no catalogue overflow, proving that symptom removed, but human inspection still found narrow trench-like roads with steep/stepped banks.

A materially different second fix restored the already-authored `WorldRoadNetworkRoute.GradeRadiusDm` to physical lowering while keeping visible material/detail at `SurfaceRadiusDm`. Exact structural validation passed, but the same in-town built-player trench symptom remained. Per the two-fix rule, generic corridor geometry was frozen until a minimal production repro isolated scene composition: organic Kentridge wrote road grading before plot surfaces, allowing later plot carve/fill to overwrite grading-only shoulders. Focused run `33362012059` at feature SHA `a0d1905ec1ff1a61efccb06a7f1c6dd3ad785f7d` failed that writer-order invariant.

Organic Kentridge now finishes plot surfaces before `KentridgeDirectedTownSurfaceCatalogue`; legacy authored ordering remains unchanged. Run `33367106975` exposed a test-harness false positive because the old `kentridge-plot-` matcher also included later plot-dressing definitions. After matching the exact standalone plot-surface stage, exact run `33369999858` on feature SHA `d242f2ede1cab1d7888cd184358b446a3a382296` passed the focused regression, automatically derived module validation, and standalone player replay.

## Human visual rejection and renewed isolation
Run `33369999858` is mechanically green but does not satisfy visual acceptance. Direct inspection of its durable built-player captures still shows an in-town curved road/junction as a dark-sided incised channel with a bulbous merged approach; the survey view still reads as thin, flat/uniform brown ribbons. The evidence also does not provide the required player-height close views of both shoulders, non-flat cross-section, real junction, vegetation recovery, and CharacterMotor traversal, so visual acceptance remains open.

Because the trench-like symptom survived the writer-order fix, do not make another generic corridor change. The next minimal repro targets a more specific production interaction: `KentridgePlotSurfaceCatalogue` documents frontage-height alignment but its canonical pads raise twelve 1 dm terraces away from the flat core. Several archetypes leave the public frontage outside that core (`PadFor.Z` = 4–10 dm), while `KentridgeWorldRoadNetwork` prepends the realized public entrance and the road later cuts through this feather. `OrganicPublicApproachMustNotCrossRaisedPlotLip` samples the fixed production seed along each realized entrance-to-public-approach line and compares authored plot fill top against the authoritative shared road target. Exact diagnostic request `33370815109`, rooted at feature SHA `e0037f7e5d5bc8f5f9ab59f84b015085de430af6`, is queued on the sole self-hosted runner; do not replace it.

## Architecture / intended correction boundary
Keep the shared profile -> presentation path -> bounded generic `EmitTerrainCorridor` -> voxel surface path unchanged unless the new minimal repro disproves the plot-frontage hypothesis. If the repro confirms a raised public frontage lip, correct only Kentridge plot/frontage composition so the authored yard actually meets its road/access elevation; preserve side/back parcel feathering and the semantic road authority. Do not add scene-local voxel patches, parallel road geometry, cover meshes, or road-only materials.

Independent `TerrainCorridorGradingEnvelopeTests` continue to cover packed dual-envelope sampling, production road-catalogue encoding/bounds, legacy compatibility, and packing round-trip. Existing `WorldRoadPresentationRegressionTests` cover route authority, topology, curves, slopes, vegetation, wear, non-road isolation, and storage/vertex budgets. `KentridgeRoadCompositionRegressionTests` guards the production composition interactions generic fixtures cannot expose.

## Current state / remaining gates
`fixes/agent-3` includes observed `origin/master` via merge `b3cd098a1910ff23ebee96bf76270c24cd2823f4`; refresh current master again before final promotion. Structural exact-SHA validation of the writer-order fix is green at `d242f2ede1cab1d7888cd184358b446a3a382296`, but human visual acceptance is rejected and a new minimal repro is queued. Resolve that repro before another production fix, then rerun focused/module/budget/player gates on the resulting exact feature SHA. Inspect full-resolution player evidence against every required visual relationship and close only after all gates and the repository AAA bar are actually satisfied.
