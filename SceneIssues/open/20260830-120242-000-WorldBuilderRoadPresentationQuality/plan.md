# Plan

## Acceptance
Improve the authoritative `WorldRoadNetwork` presentation without changing route/topology authority: coherent curved/diagonal edges; formed carriageway/shoulders and bounded cut/fill; deterministic shared wear; topology-aware junctions; stable chunk/LOD continuity; preserved terrain/vegetation/collision/destruction semantics and budgets; and production-quality exact built-player validation in `KentridgePlayableSlice`.

## Evidence and discriminating results
The original road was a union of segment-local fields; presentation refinement plus exact resolved-vertex junction preservation addresses that reusable geometry defect without rewriting route authority. Generic `Trail`/custom-profile fixtures prove the shared path independently of Kentridge.

Two materially different generic fixes still left the same built-player trench. A production repro then isolated Kentridge composition: run `33362012059` proved plot surfaces could overwrite the wider road grading envelope. Deferring organic road rasterization until after plots passed focused/module/player validation in run `33369999858`. Human inspection still rejected the trench, so a second minimal repro was added instead of changing generic geometry again. Run `33370815109` measured an 8 dm raised plot feather on the real public approach for role 14, isolating the remaining defect to Kentridge parcel frontage authoring.

The narrow frontage correction keeps twelve 1 dm side/rear terraces but finishes only canonical `z=0..PadFor(...).Z` at core elevation. It rotates with semantic `FrontageDirection`; no role coordinates, cover meshes, parallel road geometry, shader island, schema change, or route change are introduced.

Merge `4b7675202b9b164b8f9a7a33e8d394443b3725aa` incorporated master `8d8fccd1198e36d164c92fc80760580de12efe51`. Exact post-merge run `33375836636` failed `OverlappingPlotLandformsMustRunBeforeRoadGrading`: master’s reservation-aware composition had moved the solved organic road write back before plots (road index 6, plot index >257). The standalone player still built/replayed, so this was a product regression, not infrastructure. Production commit `cdc0a5755edbf72a65f5cd0b2e336606df81661a` preserves master’s single solved `organicRoadNetwork` and reservation snapshot while deferring only `BuildResolvedRoadNetwork(...)` until after plot surfaces.

Exact feature SHA `8e6c10bcb704402ce7e7bbf41dac37c1f97eb8ef` run `33378705492` closes that compatibility validation: `KentridgeRoadCompositionRegressionTests` executed two production regressions and both passed, automatic `spatial-reservations` validation passed, and both derived standalone players (`kentridge-integration`, `spatial-reservations`) built/replayed successfully. The derived Kentridge survey shows the road network, while the explicit SceneIssue replay remains pinned to no capture and opens inside the legacy scene; that generic replay is smoke evidence only and is not being substituted for the mandatory road-specific visual checklist.

Exact post-master feature SHA `9e0f5471473916d5adf5e71b4f7657608daaec68` run `33414828060` passed all 10 `WorldRoadPresentationRegressionTests`, automatic `kentridge-integration` and `spatial-reservations` validation, and a 60-second built `KentridgePlayableSlice` replay inside the five-minute targeted-CI budget. Its final survey frame confirms medium/far road continuity and junction layout, but the earlier captures remain dominated by the opening interior sequence, so this run is not being substituted for the mandatory player-height road visual gate.

Validation-evidence run `33415787999` on exact feature SHA `43feca9ddea9aeb29dba73672605237a55f2af56` also passed all focused and automatic module/player gates, but human review rejected its four Kentridge captures as insufficient acceptance evidence. The player log isolates the harness timing cause: world loading delayed the first presented frame and `HARNESS autowalk on` until `t=23.6s`, despite the declarative 12-second threshold; the 60-second replay therefore left too little post-opening CharacterMotor traversal. This is an evidence-harness failure rather than a new road-production symptom.

The second materially different evidence attempt extends only the declarative Kentridge validation replay from 60 to 80 seconds, keeps normal autowalk, and moves survey mode to 78 seconds. It requires at least six 1600x900 captures, adding two late walking frames after the observed 23.6-second world-ready point rather than merely retuning the pre-load trigger. The previous exact job completed in about 4m32s, so the additional 20-second player window is expected to remain within the repository’s five-minute targeted-CI envelope. No production geometry, scene composition, runtime content, or road semantics change.

Exact validation-evidence run `33416931897` completed successfully and produced six full-resolution Kentridge captures through `t=73.0s`, including genuine player-height walking views at `t=63.0s` and `t=73.0s`. The evidence harness problem is therefore resolved. Human review of those late frames rejects visual acceptance for a production road defect: the road is readable and traversable, but internal curve/junction pieces show repeated flat bands, abrupt rectangular/capsule transitions, doubled shoulder/crown ridges, and terraced edges. The foreground junction at `t=73.0s` is the clearest discriminator because the artifacts align with internal road-piece boundaries rather than the former plot frontage; the frontage trench itself is no longer the dominant symptom.

Code inspection isolates the dominant reusable cause. `WorldRoadInfluence` already evaluates the presentation polyline as one semantic field by testing every segment and selecting the best closest-point candidate, but physical lowering decomposes the same polyline in `WorldRoadNetworkVoxelCatalogue.BuildPieces` into independently rasterized `TerrainCorridor` primitives. Each primitive evaluates a complete endpoint-clamped crown/shoulder field. Where pieces overlap, physical output is therefore determined by primitive decomposition/write composition rather than by the same global closest-point winner used by semantic influence. This makes internal subdivision visible and explains repeated bands/ridges around curve pieces and multi-route junctions. Whole-decimetre cross-section quantization remains a secondary hypothesis only.

## Planned fix before implementation
1. Add a minimal generic failing regression independent of Kentridge that proves the required invariant directly: the same presentation road must produce the same physical sample/output regardless of whether it is represented as one physical span or partitioned into multiple bounded pieces, and reversing the order of overlapping internal pieces must not change the resulting road surface. Add a real shared-vertex junction fixture as a second discriminator. Existing resolved route vertices remain authoritative.
2. Reuse the standard continuous-corridor model already present in `WorldRoadInfluence`: closest point is selected over the whole presentation polyline/network field first, then one target grade/cross-section/coverage is evaluated for that winning road sample. Physical pieces remain only bounded storage/execution partitions and must not own independent visual end caps or decide overlap precedence.
3. Implement the narrowest generic physical-lowering change that makes partitioning invisible. Prefer reusing/shared extraction of the existing closest-point candidate selection rather than adding endpoint fade heuristics. Junction handling must be topology-aware and deterministic. Do not change route authority, road profiles, Kentridge policy, vegetation semantics, deterministic wear, or grading bounds. Do not grow persisted vertex format or `EmitTerrainCorridor` instruction width unless the failing repro proves the existing contract cannot represent the continuous field; any required budget change must be measured and justified rather than assumed.
4. Re-run the partition/order/junction regression plus the existing 10 road-presentation regressions, then inspect exact built-player `t=63s`/`t=73s` equivalents specifically for removal of piece-boundary bands/ridges.
5. Only if continuous-field composition removes the joins but a residual staircase remains, isolate whole-decimetre cross-section quantization separately and move the shared cross-section calculation to voxel-scale precision while preserving rounded semantic `TargetHeightDm` compatibility. Do not combine that secondary change without evidence.

## Blast radius / cost
The frontage correction adds at most three bounded landform primitives per affected plot definition; worst-case `MaxPrimitives` is 42 vs 39. The compatibility fix only reorders the existing solved road catalogue and adds no primitives, voxels, materials, vertices, allocations, or persisted data. Existing storage/vertex/instruction budgets remain unchanged and must still be measured/validated. The validation-only evidence change affects CI/player-validation duration only; it adds no production content or runtime cost. The continuous-field correction should preserve bounded piece footprints and primitive budgets where practical; storage or instruction changes are not pre-authorized and require a demonstrated representation need plus explicit budget measurement.

## Remaining gates
Do not implement another visual tweak until the generic partition/order/junction regression reproduces the `33416931897` artifact class. Implement the continuous shared-field correction rather than endpoint seam hiding, then run broader road production regressions on the new exact head through `ci-test/fixes/agent-3`, preserving automatic module/player validation. Inspect durable full-resolution exact player evidence for curve/diagonal edge, both uneven shoulders, a real junction, non-flat cross-section, medium/far continuity, vegetation recovery, CharacterMotor traversal, and the former in-town frontage overlap. If a residual staircase survives after the continuous-field regression is green, isolate the decimetre-quantization cause before changing it. Re-fetch current master before final promotion and merge/revalidate if master advances again.
