# Plan

## Acceptance
Improve authoritative `WorldRoadNetwork` presentation without changing route/topology authority: coherent curved/diagonal edges; formed carriageway/shoulders with bounded cut/fill recovery; deterministic shared wear; topology-aware junctions; stable chunk/LOD continuity; preserved vegetation/material/collision/destruction semantics; existing budgets; and exact built-player AAA validation in `KentridgePlayableSlice`.

## Root cause and evidence
The original road geometry was a union of straight segment-local influence fields. That prevented continuous turn/junction shaping before `SmoothSurface`; shader work could not reconstruct missing physical semantics. Presentation refinement and explicit resolved-vertex junction preservation address that reusable geometry defect without rewriting resolved route authority.

Run `33340511339` at `f30c398c17b925fd331f57c8c8c70d44f383779e` exposed `TooManyDefinitions` after over-subdividing turns. Ordinary turns were bounded to one entry/exit chamfer. Run `33345875621` at `4cb2625f1696ebfe4bbb26d044eed08b0e25e21e` subsequently built/replayed Kentridge successfully with no catalogue overflow, proving that symptom removed, but human inspection still found narrow trench-like roads with steep/stepped banks.

A materially different second fix restored the already-authored `WorldRoadNetworkRoute.GradeRadiusDm` to physical lowering while keeping visible material/detail at `SurfaceRadiusDm`. Exact structural validation passed, but the same in-town built-player trench symptom remained. Per the two-fix rule, generic corridor geometry is now frozen until root cause is isolated.

`KentridgeRoadCompositionRegressionTests` is the minimal production repro. It uses the fixed playable Kentridge seed, finds a real `PlotSurface` carve/fill overlapping only the wider road grading envelope, and checks combined-catalogue writer order. Focused run `33362012059`, rooted on feature SHA `a0d1905ec1ff1a61efccb06a7f1c6dd3ad785f7d`, failed that assertion: organic Kentridge emits the shared `WorldRoadNetwork` road stage before plot-surface landforms, so later plot pads can overwrite road grading after the road rasterizer has correctly formed it.

## Selected fix
Keep the shared profile -> presentation path -> bounded generic `EmitTerrainCorridor` -> voxel surface path unchanged. Change only Kentridge composition policy: for organic-route Kentridge, finish plot landforms first and append `KentridgeDirectedTownSurfaceCatalogue` immediately afterward so the authoritative shared road grading pass wins plot-pad overlaps. Preserve the established legacy/authored Kentridge ordering unchanged. This adds no shared API, primitive, vertex, instruction, or definition cost; it only changes scene-specific writer order.

Independent `TerrainCorridorGradingEnvelopeTests` continue to cover packed dual-envelope sampling, production road-catalogue encoding/bounds, legacy compatibility, and packing round-trip. Existing `WorldRoadPresentationRegressionTests` cover route authority, topology, curves, slopes, vegetation, wear, non-road isolation, and storage/vertex budgets. `KentridgeRoadCompositionRegressionTests` now guards the production composition interaction that the generic fixtures could not expose.

## Current state / remaining gates
`fixes/agent-3` includes observed `origin/master` via merge `b3cd098a1910ff23ebee96bf76270c24cd2823f4`. Root-cause isolation is complete and the narrow Kentridge-only ordering fix is selected. Exact request run `33366463644` against feature SHA `b5be7fe73d070bd48fa7df00be61ff45748eb5b3` did not execute the regression because the newly added test omitted the `Game.WorldBuilder.Api` namespace import; both focused and player steps stopped on CS0246 for `WorldRoadNetwork`. The regression compile defect is fixed narrowly on the feature branch; it does not change production behavior. Re-run the focused composition regression on the exact new feature SHA, then exact-SHA module/structural and standalone-player validation. Inspect the player evidence for the original in-town trench symptom plus curve/shoulder/junction/non-flat/far-field/vegetation/traversal acceptance. Measure relevant budgets, refresh/merge current master before promotion, and close only after all exact-SHA gates and visual acceptance are green.
