# New House WorldBuilder Implementation Plan

## Binding objective / reference
Recreate `Assets/Textures/Stylized/experiment1/house/10dddef5-de0a-4153-9c09-b1e8016830db.png` as closely as possible through production WorldBuilder, Structures authoring, voxel storage/meshing/rendering, and normal material/texture systems. Pinned Git blob: `6d87b08d4c7c9bddc1705c0f34343aa79bc18423`. Green CI is not visual acceptance.

## Observed reference / acceptance
Target: tall ornate three-register house; pale stone lower storey; deep round portal; narrow lower arches; plaster/timber middle register with compact blue-shuttered arch; broad swept blue portrait gable with smaller upper arch/flowers; transverse shoulders; compact crest; tall left chimney; natural planting; blue-gold banner and bracketed sign. Exact built-player evidence must be **production-quality** and very close in silhouette, proportions, opening hierarchy, detail, materials, and presentation with no holes, unsupported pieces, overlap, or flicker.

## Material results / root causes
Iteration 18 exact feature `88db9de4ec92e7e15beccd9622910bf29e791fed`, request `aa0b3dba491063b3aacacc4caad0b7d88659e9d3`, run `34218064179`, artifact `single-test-34218064179` / ID `10052999848` passed. Focused FQN `AuthorHouse_CompactMiddleFacadeRebuildOccursAfterPortraitPass_AndRestoresReferenceScaleOpening` executed non-zero (`8.43s`); all 11 repository-derived EditMode assemblies, every required player, WorldBuilder module-local player, SceneIssue 32s replay, and canonical `KentridgePlayableSlice` passed. Exact standalone audit stayed complete (`missingVisible=0`, `completeCoverage=True`) with resident geometry up to `407850784` bytes and arena use `10721536/34408080` vertices, `16185344/60214140` indices, `1266/16384` draws. Direct pinned-reference/target/front-left/rear-right inspection confirms the oversized middle timber cage is gone and roof/portal/ivy/chimney/shell survive. Closure remains rejected as **prototype/blockout quality**.

## Competing hypotheses / discriminating source inspection
1. **Favored:** the largest remaining opening-hierarchy defect is an authored high-gable overlap. `NewHouseReferenceFinishPass.RefinePortraitGable` creates the compact `11x17` arched panel at `eave + 12`, then emits a separate `29x2` full-width timber belt at `portraitEave + 27`; with default datums that belt crosses the arched panel and visibly extends through surrounding plaster. The reference keeps the arch/mullions readable and structural belts outside the glazed opening.
2. **Rejected:** the arched panel's own mullion is the primary cause. Its horizontal frame is intentionally narrow and contained inside the glazing; the exact frontal render instead shows a much wider bar extending into the plaster field, matching the separate 29-voxel belt.

## Selected fix / remaining gates
Iteration 19 changes only the conflicting high-gable full-width belt: preserve the compact upper arched panel and its internal mullions, flower box, lower structural belt, braces/posts outside the opening, roof silhouette, middle facade, portal, ivy, site, camera/light, chimney, banner/sign, and rear shell. Remove or relocate only the belt that geometrically crosses the high arch so the opening reads cleanly. Add a focused production-path regression proving the wide crossing belt is absent while the compact arched panel, contained mullion, flower box, and surrounding finish ordering remain. Then exact-SHA CI + module/player replay + direct reference/target/front-left/rear-right inspection. If still below production-quality, record only the next largest demonstrated mismatch. Finish every `tasks.md` item before open→closed, master reconciliation, PR + auto-merge; never use `pending/` or direct-push master.
