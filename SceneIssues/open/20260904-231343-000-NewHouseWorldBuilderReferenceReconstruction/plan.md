# New House WorldBuilder Implementation Plan

## Binding objective / reference
Recreate `Assets/Textures/Stylized/experiment1/house/10dddef5-de0a-4153-9c09-b1e8016830db.png` as closely as possible through production WorldBuilder, Structures authoring, voxel storage/meshing/rendering, and the normal material/texture systems. Pinned Git blob: `6d87b08d4c7c9bddc1705c0f34343aa79bc18423`. Similar style or green CI is not completion.

## Observed reference / acceptance
The reference is a tall ornate three-register house: pale stone ground storey; central round-arched timber portal and narrow arched side windows; timber/plaster middle storey with one blue-shuttered arched window; steep narrow blue portrait gable with a smaller arched window; lower transverse blue roof shoulders; distinctly swept/concave eaves; compact warm crest; left chimney; flower boxes/ivy; blue-gold banner and bracketed sign. Garage, driveway, dormer, porch roof, and materially visible gutters are absent. Final target/audits must be production-quality, very close in silhouette/proportion/details/material relationships, and free of holes, floating pieces, missing faces, obvious overlaps, and z-fighting.

## Iteration history / proven root causes
Iterations 1–5 established the roof replacement/order hazards and then exposed side/rear shell voids. Iteration 6 (`a679777d`, request `e9c81478`, run `34136916256`) repaired those voids but remained prototype/blockout quality.

Iteration 7: feature `cd9ae8c2ed0f8789e5f2ddf858365ef78e0ba9a1`, exact request `02d3bf547696212b0530e42cb797884c687b7bae`, run `34145422534`, artifact `single-test-34145422534` completed successfully. Exact reference plus target/front-left/rear-right frames at 10/20/30 seconds were inspected directly. Rear shell remained closed and module/player validation was green. Visual quality remained **prototype/blockout quality**: the intended smaller high gable window was visibly occluded, the crest remained accumulated/blocky, the eave hook was too short, and the facade remained much coarser than the reference. Source/evidence isolated the high-window cause: `FillArch` wrote opaque plaster forward to `frontZ-4`, while the replacement `ArchedPanel` carve began at `frontZ-1`. The crest clear similarly began too high and retained duplicate layers.

Iteration 8: feature `ef132fadc51c93ec520e611254d65949a4691b48`, exact request/transport `6d825d7de506287a253fd229687f29d6eb6f53d9`, run `34148998318`, artifact `single-test-34148998318` completed successfully. The exact pinned reference and target/front-left/rear-right captures were inspected directly. The finish-priority correction works mechanically: both upper arched openings survive the final repair pass, the crest no longer accumulates the previous duplicate mass, and the rear/right shell remains closed. Direct visual inspection still rejects the result as **prototype/blockout quality**. The dominant remaining structural/proportion mismatch is now the portrait-gable outer roof profile: it reads as a very large straight triangular plane, while the binding reference has a substantially narrower, concave/swept front gable/eave. The front-left audit proves this is authored geometry rather than a frontal-camera illusion.

## Selected next fix / iteration 9 discriminating experiment
Hypothesis A: the dominant silhouette mismatch is caused by the straight portrait roof profile itself. Hypothesis B: the authored profile is already sufficiently swept and only camera/material presentation makes it look straight.

Hold camera, lighting, materials, site, lower transverse shoulder roof, side/rear shell, and opening layout fixed. Replace only the shallow portrait-gable outer roof/shell profile with a row-by-row concave swept production profile. The rebuild must keep the attic shell opaque/closed, author roof edge/bargboard support with a nonlinear narrowing curve, and flare the first eave rows outward/downward to match the reference's swept hooks. Final embedded openings and compact ornaments remain authored after that destructive/rebuild pass so they cannot be erased. Add a focused authoring regression proving the shallow straight profile is cleared, the rebuilt profile narrows nonlinearly, the final upper opening carve occurs after the rebuild, and the destructive clear does not reach the rear audit shell.

Expected target/front-left result: visibly narrower swept outer profile under unchanged camera/materials. Expected rear-right result: the already-closed audit shell remains structurally intact. If the target/front-left silhouette stays straight despite this isolated geometry change, reject Hypothesis A before any broad camera/material pass.

## Ownership / remaining gates
`Assets/Game/WorldBuilder` owns reusable authoring/refinement and module-local validation; site/camera/light remain validation composition. `Assets/Game/Materials` owns presentation; Rendering remains semantic-free. Continue exact-SHA targeted CI and direct target/front-left/rear-right inspection until production-quality and very close to the reference. Then satisfy every remaining `tasks.md` item, reconcile current `origin/master`, close `open/`→`closed/`, use PR + auto-merge, and pass the required exact merged-master `affected` gate. Never use `pending/` or push the exact feature head directly to master.
