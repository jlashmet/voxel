# New House WorldBuilder Implementation Plan

## Binding objective / reference
Recreate `Assets/Textures/Stylized/experiment1/house/10dddef5-de0a-4153-9c09-b1e8016830db.png` as closely as possible through production WorldBuilder, Structures authoring, voxel storage/meshing/rendering, and normal material/texture systems. Pinned Git blob: `6d87b08d4c7c9bddc1705c0f34343aa79bc18423`. Similar style or green CI is not completion.

## Observed reference / acceptance
The reference is a tall ornate three-register house: pale stone ground storey; central round-arched timber portal and narrow arched side windows; timber/plaster middle storey with one blue-shuttered arched window; broad but steep blue portrait gable with a smaller arched window and dense flower box; lower transverse blue roof shoulders; distinctly swept/concave eaves; compact warm crest; left chimney; flower boxes/ivy; blue-gold banner and bracketed sign. Final target/audits must be production-quality, very close in silhouette/proportion/details/material relationships, and free of holes, floating pieces, missing faces, unintended overlaps, and z-fighting.

## Material results / root causes
Iterations 1–8 repaired roof ordering, shell holes, the high-window depth defect, duplicate crest mass, and the straight portrait roof. Iteration 9 (`34ed16f6`, request `22ed95b3`, run `34153598913`) proved the concave direction but failed module validation on an older duplicate-coordinate regression and showed one-voxel roof/plaster seams. Iteration 9b (`3574429e`, request `1dabd0e9`, run `34155498206`) fixed the seams and passed full validation, but its final destructive pass erased the upper-gable flower box.

Iteration 10 feature `6e5501cf0804eb37f97dffafd30b178fe102f3ad`, exact request `ec5b0ec2926811202d4db092d8720e70320dbb33`, run `34162021441`, artifact `single-test-34162021441` completed successfully. Exact target/front-left/rear-right inspection confirms the restored upper flower box survives and surface diagnostics remain complete (`missingVisible=0`; rear shell closed). Visual closure is still rejected as **prototype/blockout quality**: compared with the pinned image, the portrait roof reads as a narrow needle/spire. Its spring-line envelope is materially too narrow relative to the facade even though ridge height, camera, materials, shoulder roof, planting, and rear shell are now stable.

## Selected fix / next discriminating experiment
Hold vertical datums, camera, lighting, materials, site, openings, flower boxes, shoulder roof, crest, and rear shell fixed. Widen only the final concave swept portrait profile from half-width 24 to 29 voxels while preserving the same nonlinear curve and seam-closing plaster interface. This tests the observed width/proportion mismatch without conflating it with camera or roof-height changes. Feature work starts at `8ec0d6f18b5b4c700751b00342957377a802d4d3`; run the existing behavioral swept-profile regression plus exact standalone-player target/front-left/rear-right proof before any further visual change.

## Ownership / remaining gates
`Assets/Game/WorldBuilder` owns reusable authoring/refinement and module-local validation; site/camera/light remain validation composition. `Assets/Game/Materials` owns presentation; Rendering remains semantic-free. Continue exact-SHA compare/correct cycles until production-quality, then complete every `tasks.md` item, reconcile current `origin/master`, close `open/`→`closed/`, use PR + auto-merge, and pass the required merged-master `affected` gate. Never use `pending/` or push the exact feature head directly to master.
