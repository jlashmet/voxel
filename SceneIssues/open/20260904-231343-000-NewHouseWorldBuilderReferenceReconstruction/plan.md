# New House WorldBuilder Implementation Plan

## Binding objective / reference
Recreate `Assets/Textures/Stylized/experiment1/house/10dddef5-de0a-4153-9c09-b1e8016830db.png` as closely as possible through production WorldBuilder, Structures authoring, voxel storage/meshing/rendering, and normal material/texture systems. Pinned Git blob: `6d87b08d4c7c9bddc1705c0f34343aa79bc18423`. Green CI is not visual acceptance.

## Observed reference / acceptance
Target: tall ornate three-register house; pale stone lower storey; deep round portal; narrow lower arches; plaster/timber middle register with compact blue-shuttered arch; broad swept blue portrait gable with smaller upper arch/flowers; transverse shoulders; compact crest; tall left chimney; natural planting; blue-gold banner and bracketed sign. Exact built-player evidence must be **production-quality** and very close in silhouette, proportions, opening hierarchy, detail, materials, and presentation with no holes, unsupported pieces, overlap, or flicker.

## Material results / root cause
Iteration 19 exact feature `e7748b3a2eb368fbc48cb33798b2343f2f2eda74`, request `ab5287a67bb7df3e2c19c26d8e95a3608c0c5376`, run `34219985590`, artifact `single-test-34219985590` / ID `10053840705` passed. Focused FQN `AuthorHouse_HighGableArchKeepsMullionContained_WithoutCrossingStructuralBelt` executed non-zero (`passed=1`, `8.48s` phase); all 11 repository-derived EditMode assemblies, every required player, WorldBuilder module-local player, SceneIssue 32s replay, and canonical `KentridgePlayableSlice` passed. Exact standalone audit stayed complete (`missingVisible=0`, `completeCoverage=True`) with final resident geometry `407850784` bytes and arena use `10721536/34408080` vertices, `16185344/60214140` indices, `1266/16384` draws. Direct pinned-reference/target/front-left/rear-right inspection confirms the crossing high-gable belt is gone and shell/roof/portal/ivy/chimney survive. Closure remains rejected as **prototype/blockout quality**.

## Competing hypotheses / discriminating inspection
1. **Favored:** the next largest mismatch is layered opening construction. The late `ArchedPanel` fills every carved row to the full arch width with glass while placing surround only outside that contour, so the high and middle windows read as broad flat gray slabs with a cross rather than recessed panes held by substantial timber joinery. The pinned reference has visibly smaller dark panes nested inside thick arched timber frames.
2. **Rejected:** another facade-scale belt is causing the slab appearance. Iteration 19 removed the only wide crossing belt; the exact target still shows the flat pane shape inside the now-clear opening.

## Selected fix / remaining gates
Iteration 20 changes only the reusable late arched-panel layering: keep the same carve, opening dimensions, contained mullions, surrounds, shutters, flower boxes, roof/massing, portal, ivy, site, camera/light, chimney, banner/sign, and rear shell; inset each glass row one voxel from the arch contour and restore a continuous one-voxel inner timber frame along that contour. Extend the same focused FQN to reject the former full-width pane and require the inset pane/frame while retaining the iteration-19 no-crossing-belt invariant. Then exact-SHA CI + module/player replay + direct reference/target/front-left/rear-right inspection. If still below production-quality, record only the next largest demonstrated mismatch. Finish every `tasks.md` item before open→closed, master reconciliation, PR + auto-merge; never use `pending/` or direct-push master.
