# New House WorldBuilder Implementation Plan

## Binding objective / reference
Recreate `Assets/Textures/Stylized/experiment1/house/10dddef5-de0a-4153-9c09-b1e8016830db.png` as closely as possible through production WorldBuilder, Structures authoring, voxel storage/meshing/rendering, and normal material/texture systems. Pinned Git blob: `6d87b08d4c7c9bddc1705c0f34343aa79bc18423`. Similar style or green CI is not completion.

## Observed reference / acceptance
The reference is a tall ornate three-register house: pale stone ground storey; central round-arched timber portal and narrow arched side windows; timber/plaster middle storey with one blue-shuttered arched window; broad but steep blue portrait gable with a smaller arched window and dense flower box; lower transverse blue roof shoulders; distinctly swept/concave eaves; compact warm crest; a tall left masonry chimney rising close to the gable height; flower boxes/ivy; blue-gold banner and bracketed sign. Final target/audits must be production-quality, very close in silhouette/proportion/details/material relationships, and free of holes, floating pieces, missing faces, unintended overlaps, and z-fighting.

## Material results / root causes
Iterations 1–8 repaired roof ordering, shell holes, the high-window depth defect, duplicate crest mass, and the straight portrait roof. Iteration 9 (`34ed16f6`, request `22ed95b3`, run `34153598913`) proved the concave direction but failed module validation on an older duplicate-coordinate regression and showed one-voxel roof/plaster seams. Iteration 9b (`3574429e`, request `1dabd0e9`, run `34155498206`) fixed the seams and passed full validation, but its final destructive pass erased the upper-gable flower box.

Iteration 10 feature `6e5501cf0804eb37f97dffafd30b178fe102f3ad`, exact request `ec5b0ec2926811202d4db092d8720e70320dbb33`, run `34162021441`, artifact `single-test-34162021441` completed successfully. Exact target/front-left/rear-right inspection confirmed the restored upper flower box survives and surface diagnostics remain complete (`missingVisible=0`; rear shell closed), but the portrait roof still read as a narrow needle/spire.

Iteration 11 feature `470005099b19ae20f9ec422ff128513e5db5308e`, exact request `f015843332d5c793ebcc50d5ec60948b2b4a63c2`, run `34165061316`, artifact `single-test-34165061316` completed successfully. Exact target/front-left/rear-right inspection shows the widened half-width-29 swept profile materially improves the spring-line proportion while preserving seam closure, upper flower-box survival, and a closed rear shell. Visual closure is still rejected as **prototype/blockout quality**. The largest remaining silhouette mismatch is now the left chimney: the render shows only a short cross-like cap barely above the shoulder roof, while the pinned reference has a tall rectangular masonry stack that rises close to the portrait-gable height and materially anchors the left side of the composition.

## Selected fix / next discriminating experiment
Hold the iteration-11 gable, camera, lighting, materials, site, openings, flower boxes, shoulder roof, crest, hanging details, and rear shell fixed. Extend only the late production left chimney using the existing stone material and existing footprint, carrying its masonry shaft from inside the shoulder-roof penetration to roughly `ridge - 10` with restrained stepped cap courses. Do not clear surrounding roof geometry or change the camera. This tests the demonstrated chimney-height/silhouette mismatch without conflating it with facade detail or material changes. Add a focused finish-pass ordering/source regression proving the late chimney extension is invoked and uses the ridge-relative top datum, then run exact-SHA standalone-player target/front-left/rear-right proof.

## Ownership / remaining gates
`Assets/Game/WorldBuilder` owns reusable authoring/refinement and module-local validation; site/camera/light remain validation composition. `Assets/Game/Materials` owns presentation; Rendering remains semantic-free. Continue exact-SHA compare/correct cycles until production-quality, then complete every `tasks.md` item, reconcile current `origin/master`, close `open/`→`closed/`, use PR + auto-merge, and pass the required merged-master `affected` gate. Never use `pending/` or push the exact feature head directly to master.
