# New House WorldBuilder Implementation Plan

## Binding objective / reference
Recreate `Assets/Textures/Stylized/experiment1/house/10dddef5-de0a-4153-9c09-b1e8016830db.png` as closely as possible through production WorldBuilder, Structures authoring, voxel storage/meshing/rendering, and normal material/texture systems. Pinned Git blob: `6d87b08d4c7c9bddc1705c0f34343aa79bc18423`. Similar style or green CI is not completion.

## Observed reference / acceptance
The reference is a tall ornate three-register house: pale stone ground storey; central round-arched timber portal and narrow arched side windows; timber/plaster middle storey with one blue-shuttered arched window; steep narrow blue portrait gable with a smaller arched window and dense flower box; lower transverse blue roof shoulders; distinctly swept/concave eaves; compact warm crest; left chimney; flower boxes/ivy; blue-gold banner and bracketed sign. Final target/audits must be production-quality, very close in silhouette/proportion/details/material relationships, and free of holes, floating pieces, missing faces, unintended overlaps, and z-fighting.

## Material results / root causes
Iterations 1–6 established roof-order hazards and repaired the side/rear shell. Iteration 7 (`cd9ae8c`, request `02d3bf54`, run `34145422534`) isolated an occluded high-window repair-depth defect. Iteration 8 (`ef132fad`, request `6d825d7d`, run `34148998318`) fixed opening/crest order and kept the rear shell closed, but exact target/front-left inspection still rejected the oversized straight portrait gable as prototype/blockout quality.

Iteration 9 feature `34ed16f6c845e7a7436ed02fd7ecb1f7ba8a61d0`, exact request `22ed95b36c074f65ab3d41329866cc38a728b352`, run `34153598913`, completed **failed**. The requested concave-profile regression was not the failure; full WorldBuilder module validation found an older late-eave regression binding to a pre-audit duplicate operation. Diagnostic built-player evidence supported the narrow/swept geometry direction but showed one-voxel roof/plaster seams.

Iteration 9b feature `3574429e2374cb3d1d311839edd397cbf15602c5`, exact request `1dabd0e974e70c13e1e93b6375f8aeb5ab7c1559`, run `34155498206`, artifact `single-test-34155498206` completed **successfully**. Exact pinned-reference plus t=10/t=20/t=30 inspection confirms the roof/plaster seams are closed and the multi-angle shell remains closed with no obvious sky holes. Green CI is still not closure: the target/front view is missing the reference-defining dense flower box directly beneath the upper portrait window. Production-order inspection identifies the cause: `AddIvyAndFlowers` authors that box before `NewHouseReferenceFinishPass` destructively rebuilds the portrait shell, and the finish pass restores the high arch but not the flower box.

## Selected fix / next discriminating experiment
Hold camera, lighting, materials, site, narrow swept profile, shoulder roof, crest, and rear shell fixed. Restore only the upper portrait flower box after the final destructive portrait-shell rebuild and high-window carve, using the same timber/foliage/blossom palette and dimensions already authored by the production refinement. Add a focused regression proving the post-finish ordering. Run that regression through exact-SHA targeted CI and directly inspect target/front-left/rear-right evidence. If the planting survives and the gable still fails the reference, select only the next largest demonstrated visual relationship rather than broad simultaneous changes.

## Ownership / remaining gates
`Assets/Game/WorldBuilder` owns reusable authoring/refinement and module-local validation; site/camera/light remain validation composition. `Assets/Game/Materials` owns presentation; Rendering remains semantic-free. Continue exact-SHA compare/correct cycles until production-quality, then complete every `tasks.md` item, reconcile current `origin/master`, close `open/`→`closed/`, use PR + auto-merge, and pass the required merged-master `affected` gate. Never use `pending/` or push the exact feature head directly to master.
