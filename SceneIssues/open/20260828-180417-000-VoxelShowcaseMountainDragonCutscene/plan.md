# Plan

## Acceptance and ownership
Built `VoxelShowcase` must show a substantial grounded natural mountain, a readable continuous shared-road ascent from accessible terrain, a stably supported allowed cube dragon, and normal proximity dialogue exactly `Hello, I'm Mr. Dragon.` Every issue criterion/checklist item remains required. WorldBuilder owns landform/road authoring; Showcase composes it; Cutscenes owns dialogue; Rendering owns presentation; Composition owns bake provenance. Preserve canonical input and unchanged 240 s / 14 GiB bake guards.

## Current evidence
Run `34001756898` on source `a4a3df0d...` passed automation and traversed 92/92 waypoints, but all seven route captures are **unacceptable**: error-magenta masses and flat gray slabs obscure the mountain/road. Its build retained the dedicated `Voxel/ProceduralFarFeature` Metal variants, so shader packaging alone is falsified as the fix.

Exact diagnostic request `019f5562d8b9d2575de0024d71ccbdb55dca028f`, run `34006671692`, source `affc45d5...` passed repository-derived module validation but failed the standalone replay. It did not attribute the draw owner: the temporary observer missed the live `DontDestroyOnLoad` replay harness before producing isolation frames. Experiment 040 records that discriminator. The observer now uses `Resources.FindObjectsOfTypeAll` plus loaded-scene filtering and logs each prerequisite separately. Later branch work also preserves distinct module-player evidence instead of overwriting same-module scenarios.

Latest fetched master is `356b2e0e4d2818901c73bbc6b1788f8d6850356d`; merge it before final promotion and earlier if compatibility requires it.

## Hypotheses / next discriminator
1. Semantic far-feature submissions own the magenta mass.
2. A different draw class owns it: a component renderer (notably far terrain) or the voxel surface pass.

Rerun the temporary exact built-player observer at the ordinary approach. At one camera pose compare full rendering, no semantic-far, no component renderers, no voxel-surface, then restored rendering; inventory live material/shader support and count exact error-magenta pixels. Suppression frames are diagnostic only. Do not make another shader/geometry fix until this isolates the owner.

## Remaining gates
Implement the smallest demonstrated production fix and focused regression, remove temporary isolation instrumentation, and rerun exact module/player plus production SceneIssue validation. Require production-quality full-rendering captures, 92/92 grounded traversal, supported summit/dialogue, no exceptions, and unchanged costs. Then make the ordinary baker emit the matching manifest, promote only the visually accepted payload/manifest, prove clean-checkout consumption, complete every task/criterion, close directly `open -> closed`, sync current master, and promote only through PR + auto-merge.
