# Plan

## Acceptance
Built `VoxelShowcase` must show a substantial grounded mountain with a readable winding ascent, normal grounded traversal from approach/base to summit, a visibly supported dragon placeholder, and proximity dialogue `Hello, I'm Mr. Dragon.` Require source-matched startup bake, green focused acceptance, and human-reviewed approach/base/middle/upper/summit/dialogue captures from the real player.

## Proven causes / fixes
The original mountain footprint overlapped castle-owned feature-suppression regions. Moving `ShowcaseMountainDragonLayout.OriginX` one 512-voxel region west removed the overlap but increased effective mountain authoring from six active regions to all nine (+50%). Output-equivalent rasterizer fast paths now skip/provide whole-block operations only for proven canonical-empty/Uniform `Carve + Box` and fully interior `Fill`/`FillIfEmpty + Frustum` cases; Mixed, clipped, boundary, and other modes retain prior semantics and logical write accounting.

Run `33290377597` proved the nine-region bake could complete once at 236 s / 11,199 MB, then exposed an authored landing obstruction. `MountainDragonRampLandingProgramTests` and the geometry fix now keep 30-voxel end landings flat/clear, narrow alternating X ramps to the interior continuity span, and add the level-0 base landing.

## Current failed gate / discriminators
Exact run `33291011394` for the ramp-fixed source is diagnostic only. Cache miss bake was killed at 241 s (240 s limit), peak 11,482 MB. The preserved bake log proves semantic generation, payload/manifest writes, forced imports, `AssetDatabase.SaveAssets`, and the baker success log all completed; Unity then logged `Batchmode quit successfully invoked`, entered compiler-server shutdown, and was killed by the outer watchdog before process exit. Focused acceptance was therefore skipped even though the payload existed.

The built player launched with that source-matched payload, ran 60 s, exited 23 at waypoint `0/15`, and emitted four stationary captures. The route cause is now proven: normal spawn is `(44.6,-22.0)` inside the castle-owned `x=-1..1,z=-1..1` region rectangle, while waypoint 0 is `(-146.2,-38.0)`, so the first straight `AutoWalk -> CharacterMotor.Step` leg cuts west through legacy castle geometry. The replay harness never teleports and has a 55 s route timeout inside the workflow's 60 s replay ceiling.

## Next implementation
1. **Assignment-owned evidence route only:** detour south to `(44.6,-54.0)`, west just beyond the castle boundary to `(-56.0,-54.0)`, then proceed diagonally to the existing normal-approach waypoint. This keeps every mountain ramp/turn/summit coordinate and grounded-Y expectation unchanged. Horizontal route length becomes ~495.01 m, +32.74 m / +2.71 s ideal at replay sprint. Set the route default hold to `0` so only explicit evidence-capture holds remain; ideal transit+holds is ~47.16 s, leaving ~7.84 s inside the unchanged 55 s route timeout and ~12.84 s inside the workflow ceiling.
2. **Bake margin:** do not add another speculative voxel/rasterizer optimization. The current evidence says authoring completes and normal Unity teardown is the final elapsed-time tail. Inspect the batch bake completion/teardown contract and use the smallest editor-only change that produces a clean sub-240 s process exit *after* world disposal and asset persistence, with a focused discriminator/regression where practical. If no such safe teardown change is provable, isolate a measured authoring hot path before touching shared runtime rasterization again.

## Integration / blast radius
Current feature merge `71cbf734fef7ed5d7a7bcac29bf736eee172297a` has current `origin/master` `61d03336390ed9079498b183217cbf0ecf0abcd2` as its second parent and verified ancestor (`behind_by=0`). Master-side changes were disjoint Kentridge work inherited without Mountain Dragon conflict. Route repair is assignment-owned JSON only: two transit waypoints and removal of idle non-capture holds, with no world-generation/runtime behavior change. Any bake completion change must remain editor/batch scoped and preserve payload bytes, manifest provenance, asset import/save ordering, and normal interactive bake behavior.

## Remaining gates
Apply and statically verify the route-data repair; restore a meaningful clean-exit margin under the 240 s bake contract; keep `tasks.md` current. Then re-check latest-master ancestry and use only `ci-test/fixes/agent-4` for one exact-parent final request. Require bake to exit cleanly under 240 s with meaningful margin, focused acceptance green, complete grounded `AutoWalk -> CharacterMotor.Step` traversal, and reviewed captures including `Hello, I'm Mr. Dragon.` Only then perform pending/closed metadata and non-force merge to current `origin/master`.