# Plan

## Acceptance
Built `VoxelShowcase` must show a substantial grounded mountain with a readable winding ascent, normal grounded traversal to a usable summit, the explicitly allowed cube dragon visibly supported, and proximity dialogue `Hello, I'm Mr. Dragon.` WorldBuilder/shared modules own geometry and interaction. `AGENTS.md` requires player-visible work to be `production-quality`; only dragon art has the issue-specific placeholder allowance. Closure also requires a source-matched checked-in startup bake and exact built-player evidence.

## Proven state
Architecture, grounded traversal/headroom/support, castle-suppression avoidance, sparse upper-dragon bake coverage, raster fast-path equivalence, and exact-CI native bake exit are proven. Run `33310677691` passed structural acceptance and 17/17 normal-movement replay under unchanged 240 s / 14 GiB guards.

Revision 4 (`33314740587`) proved material separation alone was insufficient. Revision 5 (`33316622225`) failed the unchanged bake watchdog after broad duplicate ridges. Revision 6 (`33318216711`, exact source `c597b35512f1...`) recovered functional/cost CI but human review still rejected exposed berms/walls/causeways and the artificial summit as `prototype/blockout quality`; its generated bytes/manifest remain rejected.

## Root cause and revision-8/9 topology
`experiment-010-switchback-core-gap-minimal-repro.md` isolated the third-failure cause: one X-axis ramp kept a fixed Z footprint while the tapered core near face retreated about 69 voxels during each 46-voxel rise. Revision 8 replaced that incapable single-ramp shape with deterministic shell-following subsegments whose X/Y/Z endpoints are shared by support authoring, headroom carving, path surface emission, and evidence coordinates. Revision 9 localized semantic support into bounded shell embankments and exact run `33343346228` passed the unchanged support-cost/provenance regressions.

Exact visual-final `33343768361` then isolated a separate centered-headroom defect: the semantic/naturalized carve inherited shell-retreat depth and emitted a 33-voxel dark trench instead of the configured 16-voxel walking lane. Baseline source `9174af...` corrected the width; `33346316389` proved the width correction and exposed only a stale pre-segmentation carve-count assertion. The regression was aligned to `sum(SegmentCount)+2`; when the segmented implementation then exposed the stale fixed-80 primitive envelope, source `81d3a501...` replaced that local assertion with a route-derived budget while retaining the shared feature cap. Exact run `33359276877` passed the centered-headroom/support regression on `81d3a501...`.

## Module-local validation convention
Focused Mountain Dragon verification owns `Assets/Game/Composition/Showcase/Tests/Scenes/MountainDragonValidation.unity`, a player-safe runtime driver, focused PlayMode test, module manifest, and standalone-player scenario. Production `VoxelShowcase.unity` remains the SceneIssue integration/visual acceptance surface.

`experiment-011-module-local-validation-adoption.md` records the convention adoption. Exact run `33357975697` on production source `220eac9861c5...` completed green through the requested generic `PrimitiveRasteriserWholeBlockFastPathTests`, automatic module planning/player validation, and the SceneIssue standalone-player replay. This closes the generic raster reuse regression; it does not substitute for final production-scene visual acceptance.

A later human review exposed a separate module-validation defect: the focused standalone scene rendered Unity magenta because staged primitives inherited Unity's default primitive material/shader rather than an explicitly packaged player-safe material. Feature source `5270d820a0cf8d95eb7aa77a7c6a164bc6114388` adds only validation-owned resources: a packaged URP-compatible marker shader, runtime material guard, scene wiring, and a focused regression asserting every staged renderer has a supported shader. Exact CI run `33406812093` passed; its standalone module player logged 27 renderers using `Hidden/VoxelEngine/MountainDragonValidationMarker`, and the exact rendered frame shows dark mountain geometry, brown ascent, red cube dragon, and no magenta. This fixes the demonstrated validation-evidence defect only; it is not production `VoxelShowcase` visual acceptance.

## Reusability scope
Semantic mountain material/presentation authoring and generic startup-bake provenance are complete.

1. **Traversal profile — complete.** `MountainLandmarkSpec` consumes `MountainLandmarkTraversalProfile`; baseline and semantic catalogues derive headroom/clearance from it; Showcase owns voxel/body/margin/grade values. The independent non-Showcase `MountainLandmarkTraversalProfileTests` fixture passed exact-source run `33363384438` on `81d3a501...`, including automatic module/player validation and SceneIssue standalone-player replay.
2. **Waypoint replay seam — complete.** Commit `0fc4afba3b54656290b36d184ece02fbefc819cd` adds a narrow driver-owned automated-heading/player-state/walk-speed surface to `VoxelShowcase`; `ShowcaseWaypointReplayHarness` and independent `DeterministicAutoWalkHeadingHarness` consume that public semantic surface with no private reflection and no duplicated 24-degree/second turn compensation. Initial run `33389185447` proved the focused seam and module validation but the automatically derived SceneIssue replay failed because the request omitted the startup-bake provenance prerequisite. The materially corrected retry `33391220613`, using the same CI transport with the production bake prerequisite, completed successfully. This proves the seam and independent consumer without weakening acceptance.
3. **Generic raster wording — complete.** Mountain-specific generic comments were removed, and exact run `33357975697` proved non-Mountain Box/Frustum whole-block reuse without changing behavior.

## Exact accepted-bake handoff
The legacy visual artifact contained rendered captures/logs but not the fresh `Assets/Resources/VoxelShowcase/ShowcaseWorld.bytes` or matching manifest, while the shared workflow uploads only `Artifacts/SingleTest/**`. Changing that shared workflow would be outside this assignment and it also conflicts with current master.

The Mountain-Dragon-only evidence path is validated on exact feature source `f37491efa888...` by targeted run `33371715298`. Its requested production-baker test completed in `159.963 s` under the unchanged `240 s / 14 GiB` contract, automatic module validation passed, and the same-run standalone SceneIssue replay passed. The artifact contains generation and runtime collector copies of `ShowcaseWorld.bytes` + matching manifest; they are byte-for-byte identical at `15,105,067` bytes with SHA-256 `bd3f3e666da4d2ec687313ad1a08992a88bbf87430f2ffde96240774ab5ae62c` and content signature `0xA799B5B8`. This proves the binary-safe promotion path for a future visually accepted candidate; this candidate is evidence-handoff validation only and is not visually accepted.

## Current blockers
The required pre-visual merge remains externally blocked. Refreshed `origin/master` is `0de38ba704be999c13c9c9aa59237efa65405144`. Because master materially advanced, merge probe PR #182 was refreshed against feature head `1fa4efdc51c4d46be2abf12a70f22e6b522556ca`; GitHub still reports `mergeable=false`, and the probe is closed/unmerged again. Its recorded overlap remains shared workflow/guidance, validation tooling, Kentridge validation files, and `FeatureRegionBuild`, all outside this assignment. Per assignment rules, do not resolve or overwrite those unrelated conflicts here. This blocks the next production visual-final request.

No CI request is currently queued or running for agent-4. The most recent exact request, marker-shader validation run `33406812093`, completed successfully. Do not advance the CI transport for documentation-only changes.

## Current gate order
1. Keep the master-merge blocker recorded. If master changes or authorized conflict resolution becomes available, fetch/merge then-current `origin/master` before any new production visual-final request; stop again on conflicts outside this assignment.
2. After the required master merge succeeds, use only `ci-test/fixes/agent-4` for the next exact production visual-final plus same-run built-player evidence. Keep the 240 s / 14 GiB bake guard unchanged and never replace queued/running CI.
3. Human-review exact approach/base/middle/upper/summit/dialogue frames and require `production-quality`. Dedicated module-scene output is focused evidence, not a substitute for production `VoxelShowcase` review.
4. From the final accepted run, record payload size/SHA-256/content signature, commit the exact generated `ShowcaseWorld.bytes` plus matching manifest through the binary-safe Git-object path, and validate clean-checkout consumption/provenance.
5. Only after every acceptance criterion and checkbox is green: fill final issue metadata, move this assignment directly `open -> closed`, merge then-current master, verify ancestry, and non-force push the exact feature head to `origin/master`.
