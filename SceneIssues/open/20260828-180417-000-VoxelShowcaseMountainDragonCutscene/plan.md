# Plan

## Acceptance
Built `VoxelShowcase` must show a substantial grounded mountain with a readable winding ascent, normal grounded traversal to a usable summit, the explicitly allowed cube dragon visibly supported, and proximity dialogue `Hello, I'm Mr. Dragon.` WorldBuilder/shared modules own geometry and interaction. `AGENTS.md` requires player-visible work to be `production-quality`; only dragon art has the issue-specific placeholder allowance. Closure also requires a source-matched checked-in startup bake and exact built-player evidence.

## Proven state
Architecture, grounded traversal/headroom/support, castle-suppression avoidance, sparse upper-dragon bake coverage, raster fast-path equivalence, and exact-CI native bake exit are proven. Run `33310677691` passed structural acceptance and 17/17 normal-movement replay under unchanged 240 s / 14 GiB guards.

Revision 4 (`33314740587`) proved material separation alone was insufficient. Revision 5 (`33316622225`) failed the unchanged bake watchdog after broad duplicate ridges. Revision 6 (`33318216711`, exact source `c597b35512f1...`) recovered functional/cost CI but human review still rejected exposed berms/walls/causeways and the artificial summit as `prototype/blockout quality`; its generated bytes/manifest remain rejected.

## Root cause and revision-8/9 topology
`experiment-010-switchback-core-gap-minimal-repro.md` isolated the third-failure cause: one X-axis ramp kept a fixed Z footprint while the tapered core near face retreated about 69 voxels during each 46-voxel rise. Revision 8 replaced that incapable single-ramp shape with deterministic shell-following subsegments whose X/Y/Z endpoints are shared by support authoring, headroom carving, path surface emission, and evidence coordinates. Revision 9 localized semantic support into bounded shell embankments and exact run `33343346228` passed the unchanged support-cost/provenance regressions.

Exact visual-final `33343768361` then isolated a separate centered-headroom defect: the semantic/naturalized carve inherited shell-retreat depth and emitted a 33-voxel dark trench instead of the configured 16-voxel walking lane. Baseline source `9174af...` corrected the width; `33346316389` proved the width correction and exposed only a stale pre-segmentation carve-count assertion. The regression was aligned to `sum(SegmentCount)+2` without changing topology.

## Module-local validation convention
Focused Mountain Dragon verification now owns `Assets/Game/Composition/Showcase/Tests/Scenes/MountainDragonValidation.unity`, a player-safe runtime driver, focused PlayMode test, module manifest, and standalone-player scenario. Production `VoxelShowcase.unity` remains only the SceneIssue integration/visual acceptance surface.

`experiment-011-module-local-validation-adoption.md` records the convention adoption. Exact run `33357975697` on production source `220eac9861c5...` later completed green through the requested generic `PrimitiveRasteriserWholeBlockFastPathTests`, automatic module planning/player validation, and the SceneIssue standalone-player replay. This closes the generic raster reuse regression; it does not substitute for centered-headroom or final visual acceptance.

## Reusability scope
Semantic mountain material/presentation authoring and generic startup-bake provenance are complete. Remaining reuse boundaries are narrow:

1. **Traversal profile** — `MountainLandmarkSpec` consumes `MountainLandmarkTraversalProfile`; baseline and semantic catalogues derive headroom/clearance from it; Showcase owns voxel/body/margin/grade values. An independent non-Showcase fixture exists, but prior exact attempts ended in compile/runner failures before proving the current source. Keep this task open until a behavioral exact-source gate is green.
2. **Waypoint replay seam** — `ShowcaseWaypointReplayHarness` still reflects private VoxelShowcase state and duplicates AutoWalk turn-rate policy. `experiment-014-waypoint-replay-public-seam-design.md` records the narrow semantic seam and the current safe-edit tooling blocker. Do not rewrite the large scene driver through an unsafe connector operation.
3. **Generic raster wording** — complete. Mountain-specific generic comments were removed, and exact run `33357975697` proved non-Mountain Box/Frustum whole-block reuse without changing behavior.

## Current gate order
1. Leave exact centered-headroom request `33358709551` untouched while queued/running. Its request SHA `5a1de488...` points directly to production source `220eac9861c5...` and targets `MountainPathSupportUsesTaperedMassesWithoutTallRetainingWallBoxes`, which asserts the 16-voxel centered lane, `sum(SegmentCount)+2` carve count, carve-volume budget, and support envelope.
2. If centered headroom is green, record it and run the independent traversal-profile behavioral fixture on the then-current exact feature head. If it fails, fix only the demonstrated cause.
3. Keep the waypoint replay public-seam item blocked until a safe exact-file edit path exists; continue independent acceptance work without weakening that requirement.
4. Fetch/merge then-current `origin/master` immediately before the next exact visual-final request; stop on conflicts outside this assignment.
5. Use only `ci-test/fixes/agent-4` for exact visual-final plus same-run built-player evidence. Keep the 240 s / 14 GiB bake guard unchanged.
6. Human-review exact approach/base/middle/upper/summit/dialogue frames and require `production-quality`. Dedicated module-scene output is focused evidence, not a substitute for production VoxelShowcase review.
7. From the final accepted run, record payload size/SHA-256/content signature, commit the exact generated `ShowcaseWorld.bytes` plus matching manifest through the sanctioned binary path, and validate clean-checkout consumption/provenance.
8. Only after every acceptance criterion and checkbox is green: fill final issue metadata, move this assignment directly `open -> closed`, merge then-current master, verify ancestry, and non-force push the exact feature head to `origin/master`.
