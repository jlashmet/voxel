# Plan — WorldbuildingGalleryTownArchitectureShowcase

- [x] Workflow/reference audit: `SceneIssues/feature-readme.md` is absent; follow coordinator instructions plus `SceneIssues/README.md`. All six `References/MountingForce/map-images/<town-prefix>*.png` families are visual ground truth.
- [x] Selected architecture: six reusable WorldBuilder town programs (Kentridge, Hightown, Moordell, Rossdam, Fairy Village, Orc Village), each with unique material/form/seed semantics, four required roles, and one-voxel (~10 cm) detail units.
- [x] Shared realization: distinct macro forms plus reusable meso/micro windows, doors, roof/eave treatment, timber/masonry joints, supports, organic/stockade assemblies, and Rossdam fortification details. Showcase code only composes the shared authoring surface.
- [x] Regression/evidence harness: production PlayMode coverage proves semantic and physical form diversity; capture-less gallery validation emits 18 named wide/player/close frames from the exact built scene.
- [x] Exact-SHA automated gate: request `c3b2e25d011eb3877f3a7baa81c6e444dfdf9875` over feature `61664de269b4e082e6fcc9c9d7de357ec1e34a77` is green; one focused PlayMode test passed and the player reported `TOWNARCH_AUDIT result=PASS frames=18/18`.
- [x] Stale-bake discriminator: the green run skipped rebaking and used the committed `GalleryWorld.bytes`; default Bake startup restores that image without invoking the six-town expansion, while `GenerateGalleryForBakeBlocking` does invoke the expansion. The 18 frames show the old gallery/terrain at the new town anchors. This falsifies “camera only” as the primary cause.
- [x] Selected parity implementation: after Bake restore, probe six deterministic representative structure anchors with `HasBuiltContentAbove`; if any are absent, author only the six town districts through the shared WorldBuilder path under an 18 M write budget. Generated/bake creation reaches the same ensure path, fresh future bakes naturally skip the repair, and the player audit hard-fails if town content is absent.
- [x] Evidence-subject implementation: Rossdam player/close views target its fortified landmark/gatehouse rather than its residence; all other towns use deterministic representative residence facade/detail anchors. Focused regression locks those anchors.
- [ ] Cost discriminator: the logged ~48.8 M session writes / ~7 s are from the pre-existing full gallery generation pass, not yet a measured six-town cost. Record town-only writes/time under its 18 M budget plus rendered draw/triangle/memory/batching/LOD diagnostics before acceptance.
- [ ] Final gates: require a sanctioned exact-feature-SHA focused regression and exact-scene built-player validation, directly inspect all 18 frames against the references/AAA bar, explicitly confirm visible 10 cm-resolution contribution in every style, then perform pending/closed bookkeeping and final non-force master push.
