# Plan — WorldbuildingGalleryTownArchitectureShowcase

- [x] Read canonical workflow, capture/runtime evidence, and all 6 complete screenshot prefix sets under `References/MountingForce/map-images/`. The issue has no redline/capture regions; the town reference sets and built-gallery runtime evidence are the visual ground truth.
- [x] Reproduce the architectural gap from captured evidence: the existing gallery executes terrain/road/bridge/structure/vegetation/walkway phases but did not previously contain a town-architecture pass. Terrain/layout are therefore not the root cause; the missing abstraction is reusable architecture-level authoring.
- [x] Merge current `origin/master` into `fixes/agent-9` and treat the current `issue.json` as authoritative.
- [x] Keep the six canonical programs reference-driven: Kentridge pastoral timber-frame; Hightown ordered vertical civic stone; Moordell low weathered moorland stone; Rossdam wealthy fortified masonry; Fairy Village organic root/canopy construction; Orc Village asymmetrical heavy timber/stockade/forge language.
- [x] Keep the production boundary in shared WorldBuilder/structure APIs: scenes select style, placement, seed/material mapping and evidence framing only; no showcase-owned direct voxel writer or `.unity` hierarchy construction.
- [ ] Evolve `TownArchitectureProgram` from silhouette/material metadata into explicit reusable form + detail intent. Add stable vocabulary identifiers and roof/form semantics so tests and downstream authorers can distinguish steep/twin/lean-to/organic/fortified/stockade forms without source-string inspection.
- [ ] Build shared bounded architectural-detail authoring helpers on `IStructureAuthoringSession`. Detail helpers must create construction logic—not noise—including recessed framed openings, sills/lintels/mullions/shutters, thresholds/door frames/steps/canopies/hardware, eaves/fascia/ridge caps, chimneys, timber bracing, buttresses, balconies/rails, masonry coursing/quoins/coping, and fortification reveals/parapets.
- [ ] Refactor each town authorer to combine macro massing, meso articulation, and micro 1–3 voxel details through shared helpers. At least one representative building per district must visibly demonstrate multiple meaningful 10–30 cm details.
- [ ] Preserve distinct authored forms physically: use genuinely different bounded primitive compositions for steep/twin/lean-to/organic/fortified/stockade families. Add production-path regressions that compare stable form/detail signatures so future changes cannot collapse forms back to one generic gable/box.
- [ ] Make Rossdam the fortification-detail proving ground: layered coping/parapets, dimensional crenellations, arrow-slit depth/reveals, tower-wall transitions, buttresses, stairs/access, gate framing/hardware. Implement as shared helpers suitable for later castle reuse rather than a gallery-only special case.
- [ ] Finish production gallery integration: six separated walkable districts, readable voxel labels, palette displays, characteristic street/ground treatment, all four building roles, town-specific exterior cues, and consistent neutral lighting. Generated startup and bake creation must compose the exact same town pass.
- [ ] Ensure `WorldbuildingGalleryShowcase` calls only the public WorldBuilder town-architecture authoring surface for district geometry; material mapping, fixed placement/seed, viewpoints and overlays remain composition responsibilities.
- [ ] Add deterministic evidence/tour framing for wide/elevated district views, player-height 2–5 m facade views, and 0.5–2 m close detail views so the exact built-app harness can judge construction rather than only distant silhouettes.
- [ ] Add focused behavioral tests through the real WorldBuilder/structure authoring boundary: six material families, required roles, detail vocabulary, distinct form signatures, Rossdam fortification detail, deterministic fixed seeds, bounded writes, and generated/baked parity where exposed.
- [ ] Add deterministic Unity `.meta` files for every new shared/test script.
- [ ] Validate exact built `WorldbuildingGalleryShowcase` startup/runtime, inspect every district directly against its screenshot family and AAA construction bar, and explicitly answer whether 10 cm voxels contribute visible useful 10–30 cm detail in all six styles.
- [ ] Measure blast radius/cost after the final production geometry exists: bounded voxel writes/build time, material/shader count, draw/triangle impact, memory, batching, streaming/LOD implications, and no regression to existing device budgets. Default external cost remains $0 and must stay below the $3 cap.
- [ ] Only after all implementation/visual/runtime criteria pass: request the single allowed exact-SHA targeted CI on `ci-test/fixes/agent-9`, complete pending metadata/workflow gates, close only this assignment, merge latest `origin/master`, and non-force push that exact feature head to `origin/master`.
